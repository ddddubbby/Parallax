node_modules
.next
out
coverage
.env
.env.local
.env.*.local
*.log
.DS_Store
*.tsbuildinfo
.pgdata
.pnpm-store
test-results
playwright-report
blob-report
playwright/.cache

# M34 Phase-0+ framing evidence: raw model-origin artifacts (full response text,
# extractor inputs) stay out of git (D-096 data governance). Reports (.md) and
# the SHA-256 manifest are committed; raw JSON lives only on the operator machine
# and in evidence archives.
docs/audits/m34/*.json
docs/audits/m34/*.json.bak
docs/audits/m34/*.bak.json
docs/audits/m34/*-coding-review-*.md

# C-11: M35 ACP harness credentials template — holds signer private keys /
# RPC keys on the operator machine, never committed.
.m35-evidence/

# Local design-skill tooling (pulled from an external skills repo) and Claude
# Code's internal worktrees/skills — dev-environment only, not product code.
.agents/
.claude/skills/
.claude/worktrees/
skills-lock.json
