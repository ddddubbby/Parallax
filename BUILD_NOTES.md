# BUILD_NOTES.md - Session Working Memory

> Disposable mid-milestone state for agent handoff. This file answers "what was the last session doing?" — nothing else. Durable facts do not live here: decisions go to the `MASTER_CONTEXT.md` Decision Log, status goes to the `PRD.md` tracker, schema/contract facts go to the spec docs.

## Rules

1. **Append one entry per session**, as step one of the handoff ritual — or immediately whenever stopping mid-task or blocked.
2. **Disposable:** when a milestone merges, delete its entries. Anything still worth keeping must graduate to a canonical doc first; if it can't, it wasn't worth keeping.
3. **No restating:** never duplicate what a commit message, the PRD tracker, or the Decision Log already says. The unique value here is dead ends, unverified work, and the exact next action.

## Template

```
## S-<NNN> / <YYYY-MM-DD> / M<N>
GOAL: <one line: what this session set out to do>
DONE: <verified work, each with the command or check that proved it>
UNVERIFIED: <built but not yet proven; say what proof is missing>
REJECTED: <approach tried and abandoned + why, one line each>
NEXT: <the exact first action for the next session — command or file-level>
GOTCHAS: <environment quirks, surprising behavior, anything that cost >10 minutes>
```

Session numbers increment forever and never reset; omit empty fields except NEXT, which is mandatory.

---

## Entries

## S-077 / 2026-07-11 / M34A: HEYTEA reviewed workflow + two-brand development gate complete
GOAL: Exercise the same human-reviewed M34A workflow on HEYTEA, preserving its unavailable denominator response and testing whether a second category yields a specific actionable gap.
DONE: (1) Project lead approved the nine-association blind codebook before lock (`2026-07-11T06:54:20Z`); official-public/company-authored positioning was revealed afterward because no client brief exists in repo. (2) Codex prepared a 25-row/184-annotation literal-span proposal packet; project lead explicitly approved every row, including `deepseek-a3-r5` as `generation_unavailable`. `pnpm framing:m34a:workflow -- matrix` validated the complete denominator and produced nine descriptive rows (14/25–24/25; no interval/eligibility claim). (3) `pnpm framing:m34a:report` validated ten gap classifications and rendered the client-safe report: legacy cheese-tea/premium associations are already strongly represented; the actionable missing narrative is lighter/lower-sugar/plant-forward localization, while sugar/queue friction is updated rather than denied. (4) `pnpm framing:m34a:workflow -- snapshot` created the C-15 handoff for `deepseek-a2-r3` / `heytea-a-050`, SHA-256 `f1b17f4aa55a4336cb06145210a3811e5f181ddb777b075bfab1f03362a3916d`, labeled `OBSERVED IN 21/25 RESPONSES`. Local evidence is ignored and SHA-anchored in MANIFEST. Both development brands now complete the workflow.
UNVERIFIED: No delayed intra-rater recode, machine discrepancy score, or second-human inter-rater check was run; both reports disclose this. No production schema, UI, migration, or live Simulation approval integration exists. Workflow review time was not instrumented, so bounded operator effort is not yet quantitatively proven.
REJECTED: Treating promo framing as the HEYTEA answer despite evidence for a narrower health/localization gap; excluding the interrupted response from N; converting 24/25 prevalence into a population or stability claim; reviving semantic eligibility.
NEXT: Write the M34A production-integration plan from the proven harness: representation-intent persistence, codebook/review/span/snapshot tables, operator review UI, report rendering, and C-15 provenance wiring. The plan must explicitly include migration sequencing and retain manual semantic judgment. Run one fresh Crocs or Xiaomi workflow pilot only if needed to test review usability/time—not to validate an ontology.
GOTCHAS: Official HEYTEA evidence came from the brand site, current product/privacy material, and a company-authored release; the report must keep the public-source scope disclosure. The recurrence denominator is 25 while available-response scope is 24, and both figures must remain visible.

## S-076 / 2026-07-11 / M34A: Insta360 human-reviewed workflow complete
GOAL: Complete the first real M34A blind-codebook → reveal → full-sample review → recurrence/gap → C-15-snapshot workflow without misrepresenting AI work as independent human coding.
DONE: (1) Project lead approved the eight-association blind codebook before lock (`2026-07-11T06:35:00Z`); official-public positioning was revealed afterward because no client-authored brief exists in repo. (2) Codex prepared a 25-row/121-annotation literal-span proposal packet; project lead explicitly approved all rows. `pnpm framing:m34a:workflow -- matrix` validated 25/25 reviews and produced eight descriptive rows (8/25–20/25; no interval/eligibility claim). (3) `pnpm framing:m34a:report` validated nine gap classifications and rendered the client-safe report; principal gap = direct-to-share flat output without mandatory reframing is absent from the locked codebook, while category-leadership language is unsupported by the scoped official fact sheet. (4) `pnpm framing:m34a:workflow -- snapshot` created the immutable C-15 handoff for response `deepseek-a2-r5` / annotation `insta360-a-049`, SHA-256 `2d515e5a49e99b4931aafc2257816676b4110ef13551eb9bf1b60ef1f2646280`, labeled `OBSERVED IN 20/25 RESPONSES`. Local review artifacts are ignored and SHA-anchored in MANIFEST. (5) Full `pnpm test -- src/core/framing-evidence.test.ts` invocation completed green: 78 files passed / 2 skipped, 523 tests passed / 12 skipped; focused framing suite 12/12.
UNVERIFIED: No delayed intra-rater recode, machine discrepancy score, or second-human inter-rater check was run; report says so. HEYTEA remains uncoded. No production schema, UI, migration, or live Simulation approval integration exists.
REJECTED: Inferring a confidential positioning brief; calling the public-source reveal client strategy; presenting Codex proposals as human-reviewed before explicit row approval; reviving semantic eligibility or medoid selection.
NEXT: Run the same blind human-reviewed workflow on the existing HEYTEA packet. Then assess review time/actionable-gap yield across both development brands and write the production integration plan, explicitly including migration if schema work is authorized.
GOTCHAS: Local `tsx` commands need sandbox escalation for their IPC socket. Gap enum uses `non_actionable` internally while client copy renders “non actionable.” The committed gap report must disclose that public official materials—not a client brief—were the positioning reference.

## S-075 / 2026-07-11 / M34A: human-reviewed framing-evidence harness + bounded development collection
GOAL: Implement D-099's first, non-production workflow slice and exercise it on Insta360/HEYTEA without reopening automated certification.
DONE: (1) Committed `952a2db`: pure `framing-evidence` contracts + 12 focused tests; blind packet/key builder, versioned codebook lock/reveal timestamps, complete-denominator coding validation, recurrence matrix, strict non-blended consistency-check records, gap validation, SHA-anchored C-15 snapshot, synthetic fixtures, bounded collection/report scripts, command docs. (2) Committed `f034440`: interruptions between paid-call reservation and artifact checkpoint are now `generation_unavailable`, raw-text-null denominator entries; never automatic retry. (3) Live dev collection: adopted bare v4 prompts, DeepSeek ungrounded, 5×5 per brand under $5 each. Insta360 25/25 raw + 25 assists (22 ok/3 malformed), ledger $0.078731. HEYTEA 25 denominator / 24 raw + 24 assists (13 ok/1 malformed/10 not requested), one `a3/r5` generation unavailable with its $0.10 reservation retained; ledger $0.139922. (4) Two 10-item blind packets built with separate keys; JSON inspection proved no responseId/providerId/promptVariant leak. Raw artifacts ignored; hashes in MANIFEST; no fact sheet/positioning entered the packets.
UNVERIFIED: No human has made or locked a codebook; no positioning/fact-sheet reveal, full-sample human coding, recurrence claim, gap classification, C-15 production wiring, UI, schema, or migration exists. The live data is workflow-development evidence only.
REJECTED: Retrying the interrupted HEYTEA generation; treating optional/failed span assist as an excluded response; using collection data as automated-instrument validation; starting Crocs/Xiaomi.
NEXT: A human analyst reviews each blind packet, writes a 5–10-association draft codebook, then runs lock → reveal → full-sample coding. Only after that can the harness produce descriptive recurrence and gap reports. Production integration/migration remains deferred until this workflow is proven.
GOTCHAS: Long `tsx` live commands detached from the app-terminal stream while continuing in the background. The collector checkpoints after each call; do not launch a second process for the same run. `--span-assist=off` is an honest, recorded operational choice (`not_requested`), never an evidence failure. The local embedded Postgres test setup cannot allocate shared memory in this sandbox; pure tests run, DB tests self-skip locally.

## S-074 / 2026-07-11 / M34 descope ruling recorded (D-099): M34A human-reviewed framing evidence; certification program retired
GOAL: Record the lead's endorsed-with-amendments descope (automated framing certification → human-reviewed descriptive content analysis) and disposition every v4-era artifact.
DONE: (1) D-099 in MASTER_CONTEXT §9 — full ruling incl. the lead's FACTUAL CORRECTION (v4 stopped at CAL: 771 dedup'd spans vs 60 budget, 12/70 malformed, $0.669 — operational infeasibility, NOT a completed freeze-gate failure; prereg never left ratified-pre-cal), the five amendments (C-15 → provenance-and-disclosure with immutable handoff snapshot + `SINGLE OBSERVED INSTANCE` labeling; structural gates kept in code while semantic eligibility law removed; consistency-check terminology strictly separated — intra-rater ≠ machine discrepancy check ≠ inter-rater, never one blended number, single-analyst engagements say so; coded corpus = option not moat, reuse gated on contractual permission/de-identification/tenant separation/retention/versioned codebooks/no silent training; prompt wording verbatim in methodology sections, prompt SPREAD on headlines), the M34A 10-step workflow, M34B deferral, v5-not-started. (2) §4 C-15 row rewritten to the D-099 form. (3) §12 glossary M34 paragraph rewritten (codebook / coded association / recurrence matrix / gap classification; eligibility+medoid terms marked historical). (4) PRD §8.32 rewritten as FE-1..FE-12 (BF-1..30 retired, history note kept); M34 tracker row → M34A/M34B; progress note added. (5) M34_BUILD_PLAN.md status → SUPERSEDED (body preserved). (6) v4 prereg header → RETIRED-at-CAL with salvage note. (7) Fixtures: framing-protocol.v4 → retired-descope-d099; representation-prompts.v4 → adopted-m34a; blind-frame-extraction.v4 → adopted-m34a-span-assist. (8) heldout-register → CLOSED; Crocs/Xiaomi released from seal, repurposed as honestly-labeled workflow pilots (Insta360/HEYTEA develop the workflow first). (9) PROTECTED_REGISTER: D-094/096/098 → D-099 supersession row (do not resurrect eligibility law/medoid/clustering; `representation` intent + D-068 supersession stand).
UNVERIFIED: No product code — this is a canon pass. M34A build not started.
REJECTED (D-099): building v5; visibility without structural gates; blending consistency-check types; treating coded corpus as auto-reusable; using Crocs/Xiaomi as instrument validation; deleting v4 artifacts.
NEXT: M34A build planning (lives in D-099 + here, below D-090 threshold): sequence ≈ (a) coding-workflow harness (blind packet builder, codebook lock + reveal timestamps, span-assist accept/reject, recurrence matrix + gap report generator) exercised on Insta360 then HEYTEA dev data; (b) operator runs the first real end-to-end M34A study; (c) pilot on Crocs or Xiaomi (workflow pilot, honestly labeled); (d) only then production integration design (representation intent per D-068-supersession, coding tables, migration — design deferred). Also outstanding: v3-era provider billing reconciliation; M35/M36 demo arc remains unblocked per D-098 Option B.
GOTCHAS: The v4 prompts fixture is now the LIVE M34A prompt set — do not edit wording without a protocol-version bump. The span-assist extractor (gpt-5.4-nano) has NO state-machine guarantees by design now; its failures must degrade to human coding of raw text, never block. C-15's snapshot exists because recurrence figures change on recode — the simulation must keep the evidence state it was commissioned on; build the snapshot as copied-at-handoff fields, never a live join.

## S-073 / 2026-07-11 / M34 post-v3 ruling: A+B recorded (D-098), v4 brief drafted, ops loose ends committed
GOAL: Record the lead's A+B scope ruling after v3's rejection; draft the framing-protocol.v4 design brief; close the two uncommitted ops fixes.
DONE: (1) Verified state before recording: D-097 committed; v3 fixture `rejected-not-frozen`; six D-096 instrument fixes present in harness (manifests in v3-run.ts, offsets rejecting 81/39 observations per the gate report); Crocs/Xiaomi untouched. Key register finding: rule 5 burns held-outs only on held-out-DRIVEN changes — v3 died on dev data, so **both held-outs carry to v4 unburned** (addendum appended to `heldout-register.md`). (2) `docs/audits/m34/framing-protocol-v4-brief.md` written: two-failure diagnosis (elicitation sensitivity + coding variance, coupled); span-offset extraction (labels lose identity role); operator-as-blinded-mapper (locked-before-scoring, `mapping_overflow` span budget, self-consistency check; LLM mapper-of-record OUT); zero shared elicitation instruction (hedging → stance tags, `entity_ambiguous`); clause-agreement ablation promoted to a PERMANENT freeze gate; preregistered gate order controls→ablation→gold→mapper-reliability→freeze→held-outs→production; spend accounting binding at harness level. (3) D-098 in MASTER_CONTEXT §9 (partially amends D-094: scope split — descriptive framing profile ships without certification claims, demo arc unblocked; migration 0013 + eligibility + C-15 blocked until v4 passes held-outs; 0013 must ship the v4 span-offset schema). (4) PRD §8.32 marker refreshed (was stale — still said v3 "in design"), M34 tracker row + progress note updated; M34_BUILD_PLAN header re-scoped. (5) Ops commits: shell-free preview launcher (.claude/launch.json) + Settings verify success notice (credentials-panel.tsx), committed separately from canon.
UNVERIFIED: v4 detailed design + preregistration not started — the brief is the boundary document, not the protocol.
REJECTED (D-098): continuing the production build on v3's method; full park; LLM mapper-of-record with better prompts; burning the held-outs; shipping migration 0013 against the v1 label-identity schema.
NEXT: v4 detailed design per the brief → preregister (thresholds, span budget, reliability floors, ablation arms) BEFORE any scoring. Independently: operator may green-light early Crocs/Xiaomi ordinary audits for Option-B value (seal rules in the register addendum). Descriptive-profile productization design can start once v4's extraction contract freezes.
GOTCHAS: PRD §8.32's marker had drifted (referenced v3 as "in design" after D-097 rejected it) — when a protocol version dies, grep PRD/BUILD_PLAN for stale references in the same pass. The gate report notes provider-side billing reconciliation is still owed for v3's overwritten mapper attempts.

## S-072 / 2026-07-11 / M34 Phase 0: v3 recovery instrument + preregistered ablation — NO-GO (D-097)
GOAL: Pass the next M34 gate without forcing a pass: implement D-096's six instrument fixes, preregister v3, run controls/development ablation, and touch Crocs/Xiaomi only after freeze.
DONE: (1) Checkpointed all prior Phase-0 planning/evidence at `183932d`, explicitly excluding unrelated `.claude/launch.json` and credential-UI edits. (2) Built v3 research harness only (no migration/production code): exact contiguous quote→source offsets; unsupported-frame rejection; immutable generation/extraction manifests + raw hashes; response-state denominators; one-run/one-standard-extraction-version organic pin assertion; complete-link concept clustering across dimensions; count/outcome-blind review packets locked before scoring; independently stable multi-concept profile eligibility + deterministic profile medoid; `entity_ambiguous`; bounded concurrency with successful-sibling checkpointing on partial batch failure. Focused tests: 8/8 green; tsc clean. (3) Pre-registered six open admission prompts, two non-admission probes, 5×6 structure, 5/6 + LOO rule, uncertainty ablation decision, controls, held-out gate. (4) Controls: initial harness cross-pooled controls (fixed before brand scoring); initial positive phrase `clog footwear` was broader, replaced with true surface synonym `foam clog footwear`; final four real-path controls all pass at every candidate 0.78–0.88, registered highest selects 0.88. (5) Development Insta360 DeepSeek/ungrounded 30+30 completed; first clause artifact abandoned after Promise.all lost successful siblings on one malformed transport response, preserved/unscored; replacement path checkpointed successes and retried identity once. (6) Blind mapping: one-shot DeepSeek JSON failed twice; one-shot OpenAI later timed out under the wrapper-rule revision; chunked OpenAI path completed but cost $0.50891/$0.514765 and produced 62/58 final groups. (7) Scored only after mapping lock: no-clause = `unstable_profile` (29 ok + 1 insufficient; 81 unsupported frames; strongest concept 4/6); clause = eligible (30 ok; 39 unsupported; one identity 5/6). Registered ablation rule rejects clause because stable sets differ and terminal-abstention improvement is 3.3pp <10; selected no-clause arm fails. v3 rejected-not-frozen. Durable ruling: `docs/audits/m34/phase0-v3-gate.md`; raw hashes in MANIFEST.
UNVERIFIED: Small human-coded gold set not run because the earlier development-ablation freeze condition failed. Provider-side charges for overwritten one-shot mapping experiments and timed-out attempts cannot be fully reconstructed from response metadata; final stored artifacts account for at least $1.1185.
REJECTED: Promoting the passing clause arm; lowering 5/6; manual post-score merges; spending Crocs/Xiaomi on a clause-sensitive method; starting migration 0013 or production M34.
NEXT: Lead ruling required. Do not rerun v3. A v4 proposal must reduce exact-quote rejection and concept-label explosion without a shared elicitation clause, and replace the costly/timeout-prone mapping step with a bounded human-checkable coding protocol; otherwise stop the simulation-baseline gate and reassess M34 scope.
GOTCHAS: Review cost, not generation, dominated (~$1.02 for two final blind maps). Raw v3 JSON is gitignored; MANIFEST hashes are the committed evidence anchors. The untouched held-out register remains valid only if a future frozen protocol reaches it; Crocs/Xiaomi have zero targeted M34 data.

## S-071 / 2026-07-11 / M34 Phase 0: HEYTEA consumer re-audit + held-out registration
GOAL: Resolve S-070's two operator decisions without contaminating v3 validation: create and execute a clean consumer HEYTEA audit; select at least two held-out consumer brands.
DONE: (1) Preserved legacy `Heytea` (`e0435c68-c7b4-41b9-a7ac-f6a32c6e874b`) unchanged and created `HEYTEA Consumer Feasibility` (`d4a65f05-fca2-432b-a3be-39edd59410bd`) as `consumer_venue`, Singapore, two consumer personas. (2) Generated V1 (`562b5396-3664-4fe0-a9ef-49040e50d924`), caught and corrected four `for Choose ...` buyer-goal grammar artifacts before approval, then approved 30 cells. (3) Executed live audit `0241898e-d930-4ab7-b326-4760e5f86b3b`: DeepSeek ungrounded / `deepseek-v4-flash`, k=5, $2 hard cap; 150/150 jobs succeeded, zero retry/dead-letter/skip; 150/150 Evidence Layer extractions valid; actual $0.169391 (generation $0.060138 + extraction $0.109253) vs $0.0993 projection. (4) Read-only aggregate verified organic feasibility: HEYTEA in 36/60 open discovery+consideration responses; 8/12 cells have >=3/5 mentions. Durable record: `docs/audits/m34/heytea-consumer-feasibility.md`. (5) Pre-registered Crocs (clear identity) and Xiaomi (fragmented identity) as SELECTED/UNRUN in `docs/audits/m34/heldout-register.md`; repo search confirmed no other M34 targeted data for either.
UNVERIFIED: No M34 frame extraction or v3 scoring was run on HEYTEA — deliberately blocked until D-096's six instrument fixes and frozen v3. Crocs/Xiaomi remain unrun by design.
REJECTED: Reusing the invalid B2B HEYTEA project; running v2 framing extraction over the new responses; generating held-out responses before v3 freeze; increasing sample size after seeing results.
NEXT: Build D-096's six instrument fixes in order (offsets → immutable manifests → response-state denominators → run/version pinning → four controls through the real pipeline → blinded review), then preregister/freeze v3. Only after the gates in `heldout-register.md` pass, launch matched bounded audits for Crocs and Xiaomi.
GOTCHAS: The UI cost projection understated actual total by ~70.6% ($0.0993 → $0.169391), principally extraction; preserve the $2 cap pattern and fold this evidence into C-2 projection work. Worker heartbeats count a legacy paused HEYTEA run as “active”; a direct DB check confirmed only the new run was executing/spending.

## S-070 / 2026-07-11 / M34 Phase 0: second ruling (D-096) — risk triage, six instrument fixes, shadow-mode C-15
GOAL: Verify and record the lead's risk-disposition ruling extending D-095; execute the immediate items.
DONE: (1) Verified the ruling's empirical claims before recording: 772-frame quote audit reproduced EXACTLY (484 literal / 266 normalized-only / 22 unmatched — the "exact quote" contract is enforced only as type+length; extractor stitches non-contiguous text with ellipses); organic extraction-version fan-out confirmed in code (no latest-version filter, no run pin) though not yet manifested (44/44 distinct); resume identity, hit-derived denominators, analyzer-internal negative fixture all confirmed from source. (2) D-096 recorded in MASTER_CONTEXT §9 (six pre-v3 instrument fixes; review-blinding rule; methodology work incl. framing-profile experiment, small human-coded gold set, uncertainty-clause ablation, `entity_ambiguous` state; post-blind gap-analysis stage into PRD before Phase 1; C-15 SHADOW/ADVISORY-FIRST — partially supersedes D-094 pt-7's hard cut; `PRE-M34 BASELINE` disclosure interim; untrusted-data envelope + embedding/frame-extraction budget accounting queued; not-expand list). (3) Executed now: PRD §8.32 DO-NOT-IMPLEMENT marker on BF-10..14; raw Phase-0 JSON artifacts gitignored with SHA-256 `docs/audits/m34/MANIFEST.md` committed in their place (verified: raw ignored, reports committable); reassessment addendum appended.
UNVERIFIED: The six instrument fixes are RECORDED, not built — no harness code changed this round.
REJECTED (D-096): hard-enforcing C-15 from one held-out pass; ignoring the 22 unmatched quotes; drift automation now; broad human calibration inside M34; re-running v2 to seek a pass; raw model text in git.
NEXT: Build the six instrument fixes in order (offsets → manifests → denominators → run/version pinning → controls-through-pipeline → review blinding), then design framing-protocol.v3 per D-095+D-096 and pre-register it before scoring any data. Operator decisions still pending: Heytea archetype, ≥2 held-out consumer brands.
GOTCHAS: `docs/audits/m34/*.json` are now gitignored — evidence archives / operator machine hold the raw text; MANIFEST.md hashes anchor them. Do not "fix" the 22 unmatched quotes by relaxing matching — the fix is source offsets at extraction time.

## S-069 / 2026-07-11 / M34 Phase 0: two runs, NO-GO ×2, ruling → framing-protocol.v3 research (D-095)
GOAL: Unblock S-068's credential blocker, execute Phase 0 end to end, and resolve the verdict honestly.
DONE: (1) KEK diagnosis: `.env.local` key valid-shaped but wrong; both rows still `active` (not yet poisoned); original unrecoverable (never committed, gitignored, no backup) — operator re-entered fresh keys via Settings, verified by read-only fingerprint probe (booleans only, no plaintext in transcript) + live Verify. (2) `preflightCredentials` guard added to all three `framing:*` scripts (fails exit-3 BEFORE any paid call; distinguishes CredentialConfigError from wrong-KEK; never calls markInvalid — proven: healthy pass / wrong-KEK fail / malformed-KEK fail, rows untouched). (3) `gpt-5.5` rejects any non-default `temperature` on /v1/responses (HTTP 400 param=temperature, A/B/C-probed) — `NEUTRAL_SAMPLING` protocol parameter records the per-engine asymmetry (DeepSeek 0.7 / OpenAI model-default); audit pipeline unaffected (nothing sets temperature). (4) Run 1 NO-GO — instrument: whole-payload Zod voiding on >240-char quotes (5/60 responses lost), 213 labels/264 frames fragmentation, Heytea organic invalid (b2b mislabel). (5) v2 instrument: per-frame salvage + truncation, label-form prompt rules, conjunct-sort+plural-fold normalization, 90s deadlines on every harness call (one live stall observed and killed; resume worked), Heytea gate-excluded, analyze.ts made merge-preserving after it clobbered the sampling record once. (6) Run 2 NO-GO — REAL verdicts: neutral deepseek `prompt_sensitive` (2/6), openai `volatile` (1/6), Insta360 organic correct `sparse` abstention, negative control abstains. (7) Diagnosis via three nested re-scoring tests: eligibility unit malformed (`dimension::label` identity fragments cross-dimension polysemy; per-dimension still 1/6; diagnostic hand-collapse recovers 4/6 & 5/6 — exploratory only, circular on Insta360). (8) Lead ruling recorded as D-095 + `docs/audits/m34/phase0-reassessment.md`; PRD progress note; build-plan Phase-0 status note.
UNVERIFIED: Nothing beyond the harness itself — no production code exists yet; v3 is unbuilt research.
REJECTED (D-095, do not re-propose): threshold loosening; majority-vote dimension resolution; hand-collapse as production clustering; declaring OpenAI eligible; validating v3 on Insta360; keeping v4/v5 in the admission set; freezing C-15 now.
NEXT: Design `framing-protocol.v3` per D-095's five requirements (two-level concept+dimension-distribution ontology; open-paraphrase admission set with v4/v5 as probes, size+threshold re-specified together and pre-registered; four-control suite incl. polysemy + over-merge; brand-level dev/holdout — Insta360 is dev data forever; identity-vs-associative commercial layer). In parallel, operator decisions pending: Heytea archetype correction + consumer re-audit (or drop from feasibility), and choosing ≥2 held-out consumer brands for v3 validation audits. Phase 1 (migration 0013) does NOT start until v3 passes its held-out gate.
GOTCHAS: `pnpm framing:analyze` exits 2 on NO-GO by design (wrapper scripts must not treat it as a crash; preflight failures are exit 3). Never pipe long-running harness commands through `| tail` in background tasks — output buffers until exit and a live run looks dead. `lsof -ti:PORT` returns CLIENTS too — filter `-sTCP:LISTEN` before killing anything (a Zen browser died learning this). Both runs' evidence fixtures (v1/v2) and `.v1-run.bak.json` extraction files must survive any cleanup until M34 closes.

## S-068 / 2026-07-10 / M34 Phase 0: harness + draft protocol (blocked on credential decrypt)
GOAL: Cut `m34-baseline-framing` and execute Phase 0 (retrospective feasibility + protocol freeze).
DONE: (1) Branch `m34-baseline-framing` cut from `main` @ `502c431` (M33-merge), carrying uncommitted planning canon. (2) Drafted six representation prompts + blind frame-extraction schema/prompt + draft eligibility thresholds into `fixtures/framing/{representation-prompts,blind-frame-extraction,framing-protocol}.v1.json` and `scripts/framing-feasibility/protocol.ts`. (3) Built throwaway harness: `extract-frames.ts` (organic), `neutral-mini-run.ts` (6×5×DeepSeek+OpenAI on Insta360, resumable, $8 cap), `analyze.ts` (eligibility + unstable-fixture gate + freeze). (4) Dev-DB probe: Heytea has 7 organic-qualifying cells (≥3/5 mentions); Insta360 organic is sparse (≤1) — neutral mini-run is its path. (5) `pnpm framing:{extract,neutral,analyze}` scripts added.
UNVERIFIED: Live extract/neutral/analyze not run — both active Settings credentials fail to decrypt under current `.env.local` `CREDENTIALS_ENCRYPTION_KEY` (`decryptApiKey` → null for deepseek+openai). Phase 0 GO/NO-GO therefore pending.
REJECTED: none yet.
NEXT: Operator re-enters DeepSeek + OpenAI API keys in Settings (so they encrypt under the current KEK). Then `pnpm framing:extract` → `pnpm framing:neutral` → `pnpm framing:analyze`. On GO freeze protocol and commit Phase 0; on NO-GO stop and escalate (no auto-loosen).
GOTCHAS: Heytea is stored as `category_archetype=b2b` despite being a consumer tea brand — still usable for organic lane. Do not pass API keys on the CLI (C-11). Feasibility scripts import `../../src/env-bootstrap` so `.env.local` loads.

## S-067 / 2026-07-10 / M34: Baseline Framing Integrity (planning + canon, no product code)
GOAL: Turn the operator's revised M34 spec (itself the product of a three-round strategy reassessment) into a detailed 7-phase execution playbook and complete the handoff ritual. This session wrote docs only — zero product code, zero migration applied.
DONE: (1) `M34_BUILD_PLAN.md` written — the execution playbook (Phases 0–6, per-phase goal/files/steps/acceptance/commit, constraint+protected-surface table, risk register, definition of done, explicit out-of-scope list). Earns a standalone file under D-090 (multi-phase, migration 0013 + a second worker state machine, multi-session, >300 lines). (2) Canon: `MASTER_CONTEXT.md` §4 gains C-15 (baseline framing integrity); §9 gains D-094 (the full ruling); §7 docs index + §12 glossary updated (representation intent, framing lane, blind frame extraction, framing baseline, medoid, abstention). (3) `PRD.md`: §8.32 requirements (BF-1..BF-30 + Phase-0 gate), M34 tracker row (Not started), progress note. (4) `PROTECTED_REGISTER.md`: header note that M34's new protected surfaces get added when built (register's existence-verified invariant), plus two supersession rows now (D-068→D-094 sixth-intent, D-079→D-094 coverage-panel/pack-gating). (5) Verified before writing: migration 0009's cell-shape CHECK already tolerates representation cells (`intent<>'simulation'` branch needs only stimulus_id/panel_persona_key null — persona/market allowed null); `DEFAULT_FRAME_ASPECTS` is `Record<Intent,…>` so widening `Intent` is compiler-forced; `metricIntentFilter` (`src/core/semantic.ts:105`) is an if-chain returning `null` (no filter) for unlisted metrics — the silent-inclusion risk the exhaustive policy map closes; approval path is `approveAndCompileResonanceStudy` (`src/db/repositories/resonance.ts:940`); extraction state machine to mirror is `src/db/repositories/extraction.ts` + `src/modules/extraction/service.ts`; intent enum at `src/db/schema/enums.ts:32`.
UNVERIFIED: Everything downstream of planning — no code, no migration, no tests, no feasibility run. The Phase-0 GO/NO-GO is a real gate, not a formality: if the frozen thresholds turn out empirically unachievable on Insta360/Heytea data, the milestone STOPS before C-15 and escalates.
REJECTED (held from the strategy rounds, do not reopen): authored-synthesis baseline (use medoid verbatim); Wilson/CI on 6×5 correlated draws (prevalence descriptive only); operator override of failed eligibility (abstention states are the only escape); early C-15 enforcement (Phase-6-last is deliberate); auto-loosening thresholds on a Phase-0 zero-pass; folding construct-redesign / human-calibration / decision-gate-brief / demo arc into M34 (each is its own later milestone).
NEXT: Cut branch `m34-baseline-framing` from `main` at the M33-merge commit. Run the M34 boot ritual (MASTER_CONTEXT → PRD §8.32 + tracker → `M34_BUILD_PLAN.md` end to end → DEV_GUIDELINES §A → this entry → DESIGN_GUIDELINES for the Framing view/review UI; plus AUDIT_METHODOLOGY + PROTECTED_REGISTER since M34 supersedes D-068/D-079). Then execute **Phase 0 first** — the retrospective feasibility harness over stored Insta360/Heytea responses — and do not write any production threshold or C-15 code until `framing-protocol.v1.json` is frozen and the GO/NO-GO passes. Commit per phase-green boundary (D-092).
GOTCHAS: Migration 0013 adds the `representation` enum value ONLY — a just-added Postgres enum value cannot be USED in the adding transaction (D-066 precedent, 0004/0008 both hit this); seeding representation cells is app code after the migration. The medoid rule, blind-extractor isolation, C-15, `neutral_branded` PM-9 exemption, and `representation` value become PROTECTED_REGISTER surfaces the moment they exist — the build plan §2 lists them so the implementing agent adds them in that milestone's ritual (they're not in the register yet because it only lists existing surfaces). Grounded-mode dependency (R4): no live grounded run has ever executed and Gemini's shape is the open caveat — keep baselines per-mode so M34's critical path does NOT run through RELEASE_CHECKLIST gate 9; a single-mode ungrounded-only baseline is admissible with disclosure.

## S-066 / 2026-07-10 / M33: verification/finish implementation
GOAL: Execute S-065 steps 2–10 on `m33-workflow-verification` after the operator walk resolved the Phase 1 gate to fix-in-place (one new finding: Dashboard Simulation tab-row IA → step 7).
DONE: (2) Mobile drawer → Radix Dialog (D-093) + source-contract test. (3) `UnsavedEditProvider` + Setup dirty signal + sidebar leave confirm + beforeunload. (4) Evidence stimulus/persona filters wired to `listResonanceEvidencePage`. (5) `loading.tsx` for dashboard, run detail, study detail. (6) stimulusi→stimuli (singular/plural swap, not +es); eslint `ignoreRestSiblings`; unused AppMenuSeparator already gone with Radix rewrite. (7) Dashboard Simulation not a sixth pillar tab. (8) `CI=true` Postgres fail-hard; CI lint `--max-warnings 0`. (9) Playwright smoke + axe (`pnpm test:e2e`, ephemeral DB webServer). Incidental axe fixes under (9): empty Actions `<th>` (projects table), hub next-action underline (`link-in-text-block`), demote duplicate matrix H1, axe floor disables `color-contrast`. (10) Verified: lint 0 warnings, tsc clean, vitest 503 passed/12 skipped, e2e 3 passed, `pnpm build` green. PRD M33 → Done; D-093 recorded. Handoff ritual docs synced: `PROTECTED_REGISTER.md` (+drawer, Playwright floor, CI Postgres fail-hard; Simulation wall note updated), `README.md` + `MASTER_CONTEXT.md` §5 + `DEVELOPMENT_GUIDELINES.md` §F/G (`pnpm test:e2e`, max-warnings, Done-requires-walk).
UNVERIFIED: none.
REJECTED: none — evidence filters finished (not descoped); Radix drawer preferred over hand-roll.
NEXT: Commit/push when asked: `M33: M32 verification, focus/edit-state fixes, demo-readiness close-out (D-089, D-092)`. Then M34 hero data/story.
GOTCHAS: Playwright webServer needs full permissions locally (tsx IPC). Do not `git add -A` (Taste skill / brand WIP). E2e discovers seeded ledgerfox-demo, not Insta 360. Handoff ritual requires PROTECTED_REGISTER when a Decision Log row protects a surface — do not skip it next milestone.

## S-065 / 2026-07-10 / M33: demo-readiness assessment + verification/finish plan (planning only, no code written)
GOAL: Turn "polish the product to internal-stakeholder-demo grade" into a scoped, sequenced plan, then produce the buildable M33 execution detail (the first of four workstreams: M33 verify/finish -> M34 hero data/story -> M35 brand alignment -> M36 demo ops/rehearsal). This session did NOT touch any product code — only `MASTER_CONTEXT.md`, `PRD.md`, `BUILD_NOTES.md` (a standalone `M33_BUILD_PLAN.md` was written, then deleted and folded back into this entry — see GOTCHAS).
DONE: (1) Demo-readiness assessment via two parallel Sonnet research agents (brand/site WIP synthesis; ops/QA/provider-readiness synthesis) plus direct work: read-only dev-DB inventory (confirmed Insta 360 as the hero project — completed live audit with comparison n=30 raw count, completed live two-engine resonance study; confirmed openai+deepseek are the only active credentials; found 5 test-debris projects with no archive mechanism) and independent code review of the M32 diff. (2) Code-verified three NEW gaps beyond what M32's own build plan already flagged as unverified: `src/components/shell/operator-shell.tsx`'s mobile drawer sets `aria-modal="true"` but has zero focus management (no focus-trap, no restore-on-close) — confirmed by reading the file directly (no `.focus()`/ref/inert anywhere) and ruled out as a Radix-primitive false-negative (AppDialog/AppMenu both correctly use Radix and were not re-checked as broken); zero `loading.tsx`/`Suspense` boundaries exist under any M31/M32 segmented route, sharpest on Dashboard where `Promise.all`-parallelized-but-still-blocking per-study Simulation staleness/recompute has no loading indicator; Setup has no on-page unsaved-edit signal at all (not just the missing cross-nav guard the ops agent already found). Also ruled OUT two false alarms from the two pre-existing lint warnings: `AppMenuSeparator` unused import is harmless (shell's only AppMenu has no destructive item), `_responses` unused var in `resonance.ts:169` is an intentional destructure-to-discard pattern, just missing `ignoreRestSiblings` in eslint config. (3) Recorded D-089 in `MASTER_CONTEXT.md` §9 (why M33 exists, the three new gaps, the decision gate, no-migration/no-IA-change scope), added the PRD M33 tracker row (status "Not started") + progress note. (4) Codified D-090 (§7 threshold for when a milestone earns its own `M<N>_BUILD_PLAN.md` file) after the operator asked why M32/M33 broke from the M27–M31 no-file precedent; M33 (76 lines, single milestone) didn't clear the threshold, so per the operator's own resolution the file was deleted and its execution detail folded into this entry's NEXT field (D-091 records the fold-back without editing D-089's or the PRD progress-note's already-written text, matching the D-077/D-086 precedent for correcting an old entry's stale internal reference).
UNVERIFIED: The three code-review-found gaps (drawer focus-trap, loading affordance, unsaved-edit signal) themselves are still unfixed — planning/discovery only, no code written yet. RESOLVED since this entry was first written: the operator completed the manual hand-test walk (using a purpose-built interactive checklist artifact mirroring step 1 below) and reported back — every checked item passed clean, no structural rot found, so the Phase 1 DECISION GATE resolves to fix-in-place, not revert-to-M31. One new finding surfaced by the walk itself (not code review): the Dashboard tab row doesn't visually distinguish Simulation from the audit pillars — folded in as step 7 below.
REJECTED: none new this session (the plan's own rejected-alternatives are recorded in D-089: silently deferring the new findings instead of naming them now; deciding the revert-to-M31 question unilaterally here instead of gating it on the actual verification walk; redesigning the IA while touching this code). Also rejected this session: keeping `M33_BUILD_PLAN.md` as a standalone file once D-090 existed — it was the exact boundary case the threshold was written to stop recurring (D-091).
NEXT: Before writing any M33 code, get the operator's manual-test findings (ask directly, or check for a newer BUILD_NOTES entry if they logged one themselves) and fold every confirmed bug into the fix list below — this is an explicit mandatory first step, not optional context. Then create `m33-workflow-verification` from `m32-workflow-ui` and execute, in order:
  0. Read the operator's manual-test findings (see above) before writing any code.
  1. THE WALK M32 NEVER GOT: click-through provider enablement -> intake -> project inputs -> matrix -> run setup -> run detail -> Evidence dashboard -> Simulation study/run/results -> report/export; keyboard-only pass (tab order, Enter/Escape on every menu/dialog, visible focus rings); resize below 1024px and open the sidebar drawer both via keyboard and mouse; deep-link refresh on a `?view=` URL per segmented route; browser back/forward preserves project+view; screenshots at 1280x720 and 1440x900, no horizontal overflow. DECISION GATE: isolated, describable bugs -> fix in place in the steps below. Structural rot (a route that fails to render, a navigation dead-end, lost state on a normal action) -> STOP, escalate a revert-to-M31 call to the operator before writing any fix; M31 is preserved on its own branch specifically as this fallback.
  2. Mobile drawer focus management (`src/components/shell/operator-shell.tsx`): prefer rebuilding on the existing `@radix-ui/react-dialog` primitive (already used by `AppDialog`) restyled full-height/left-anchored, over hand-rolling a second focus-trap; only hand-roll if Radix's overlay/scroll-lock conflicts badly with the current visual spec, and document why. Either way: focus moves into the drawer on open, Tab/Shift+Tab can't leave it while open, focus returns to the hamburger trigger on close. Add the test that's currently entirely absent (zero coverage for `operator-shell`).
  3. Unsaved-edit signal: wire the unsaved-edit context M32's own spec called for (sidebar/local-nav/refresh/tab-close prompt for dirty explicit-edit forms; autosaved intake exempt) across Setup's Edit forms. Independently, add a lightweight always-visible "Unsaved changes" indicator near Save changes/Cancel — build this even if the full navigation guard is fiddly to land everywhere.
  4. Evidence filters: `listResonanceEvidencePage` (`src/db/repositories/resonance.ts`) already accepts `stimulusId`/`panelPersonaKey`; `src/app/projects/[id]/resonance/[studyId]/page.tsx` only passes `providerId`/`page`/`pageSize`. Finish wiring stimulus/persona filter controls into the Evidence view UI — default is finish, not descope, since the backend contract is already built; descope only on a real UI-state conflict, and note it explicitly in this file + the Decision Log if so.
  5. Loading affordance: add a minimal loading state (skeleton/spinner, not new UX design) at least for the Dashboard route (its Simulation section blocks first paint on a parallelized-but-unindicated per-study staleness recompute), and to any other `?view=` switch where a slow await is plausible (Run detail's Events/Extraction/Metrics, Simulation results).
  6. Lint hygiene + typo fix: remove the unused `AppMenuSeparator` import in `operator-shell.tsx`; for `resonance.ts:169`'s intentional `_responses` destructure-to-discard, prefer enabling `ignoreRestSiblings` in the eslint config over a scoped disable comment; fix the "2 stimulusi" pluralization bug at `src/app/projects/[id]/resonance/page.tsx:88` (`{stimuli.length} stimulus{stimuli.length === 1 ? "" : "i"}`).
  7. Dashboard/Simulation tab-row IA (found by the operator's own hand-test, not code review — a genuinely new insight, not a broken M32 promise: M32_BUILD_PLAN.md §5 only ever specified the Simulation *data* wall, "no audit selector, chart, aggregate," never the tab-row's visual treatment). `src/app/projects/[id]/dashboard/page.tsx:35-42` builds one flat `tabs` array — Overview/Presence/Position/Perception/Proof/Simulation — rendered through `LocalViewTabs` with equal visual weight; the data wall is real (`AuditDashboard`/`SimulationDashboard` never share a fetch) but the chrome implies Simulation is a sixth pillar-sibling, contradicting D-077's two-co-equal-layers architecture ("Proof... never a fifth pillar" extends to Simulation not being a sixth). Fix, scoped to this one file (`LocalViewTabs` itself, shared by 6 other pages, needs no change): when `view=simulation`, don't render the pillar tabs at all — replace with a single "← Evidence dashboard" link (the page already shows "Simulation results" as its own H1, so the Simulation tab item is redundant once you're on it). On audit views, give the Simulation tab a visual break from the pillar group (divider/gap) and attach the existing `SimulatedBadge`/`Stamp` next to its label — reuse the already-reserved `SIMULATED` stamp vocabulary (DESIGN_GUIDELINES V-1), never a highlight color (V-2 reserves accent orange for actions only; D-055 reserves the pillar tints for pillar identity only — neither may be repurposed to mean "this is Simulation").
  8. CI hardening (D-092): `scripts/test-db.ts`'s embedded-Postgres startup failure currently degrades to `describe.skipIf(!dbUp)` everywhere, which is correct discipline for a developer machine (D-078) but wrong for a CI required check — a broken CI runner should fail loudly, not silently skip 96+ DB-backed tests and report green. Make startup failure fatal specifically when `CI=true`; leave local behavior unchanged. Also make `pnpm lint` warnings fail CI (currently 0 errors/2 warnings pass; the M32 lint warnings this milestone fixes in step 6 should be the last warnings this repo ever ships silently).
  9. Playwright smoke spec (D-092): one spec covering the critical operator journey — login -> hub -> matrix -> run detail -> dashboard -> study results — plus an `axe` accessibility pass on those same pages. This is a floor replacing "manual QA as the only UI gate," not a coverage program; do not expand scope beyond the critical journey without a separate decision.
  10. Final verification: re-walk step 1 specifically on everything touched in steps 2-9; `pnpm lint` / `pnpm exec tsc --noEmit` / `pnpm test` clean (plus the new Playwright spec green); stop any running dev server before `pnpm build` (D-075); screenshots at both resolutions again. Then the handoff ritual: PRD M33 tracker -> Done + progress note, new BUILD_NOTES entry, Decision Log row only for a genuinely new durable decision (e.g. the Radix-vs-hand-rolled drawer call, or an evidence-filter descope) — do not rewrite D-089. Expected commit message: `M33: M32 verification, focus/edit-state fixes, demo-readiness close-out (D-089, D-092)`.
GOTCHAS: `M33_BUILD_PLAN.md` was written this session, then deleted the same session after the operator asked about the M32/M33 doc-file pattern and codified D-090's threshold — M33 (76 lines, single milestone) didn't clear it, so its content was folded back into this entry's NEXT field per the operator's explicit instruction rather than kept as a file or discarded. If you're looking for "the M33 build plan," it's the numbered list above, not a separate file. Steps 7-8 were added after the operator brought an external repo-quality audit for review (D-092) — three of its claims (the stimulusi bug now in step 6, `fileParallelism: false`, `test-db.ts`'s fail-to-skip design) were independently verified against source before being folded in; the audit's broader team-scaling recommendations were reviewed and mostly rejected (see D-092), only these two concrete, verified engineering items joined M33's scope. The M32 tracker row still reads "Done" — deliberately left unedited (current-state field, but M32's code did in fact ship/merge; the caveat that its own required browser walk never happened lives in M33's row and in D-089, not by rewriting M32's history). Do not confuse "the operator hand-tested the app" (happened, findings not yet captured here) with "this session verified the app" (did not happen — this was a planning-only, code-reading pass). The three DB-inventory findings (Insta 360 as hero, comparison n=30 is a raw count not yet confirmed eligible-n, 5 debris projects) belong to M34's scope, not M33's — don't let them bleed into this milestone's fix list.

## S-064 / 2026-07-10 / M32: verification close-out, commit, and push
GOAL: Close out S-063's open verification gap, commit the branch's accumulated work (M32 code + docs + the separately-flagged brand/site WIP), and push `m32-workflow-ui` to origin.
DONE: `pnpm lint` (0 errors, 2 pre-existing unused-var warnings in `operator-shell.tsx`/`resonance.ts`, not addressed — out of scope for a commit/push task); `pnpm exec tsc --noEmit` clean; full `pnpm test` 502 passed/12 skipped (77 files); stopped the running dev server (PID owning `.next`) per D-075, then `pnpm build` green, 21 routes. `M32_BUILD_PLAN.md` status header flipped to implemented; `PRD.md` M32 tracker row flipped to Done with a progress note. Two commits: (1) brand/site WIP (`BRAND_PLAYBOOK.md`, `BRAND_SITE_GUIDE.md`, `site/`, `public/brand/`) as its own commit, unrelated to M32 engineering; (2) `M32: segment operator workflow UI (D-088)` — all M32 code, `M32_BUILD_PLAN.md`, and the doc updates (`README.md`, `MASTER_CONTEXT.md` D-087, `PRD.md`, `PROTECTED_REGISTER.md`, `BUILD_NOTES.md`). Pushed `m32-workflow-ui` to `origin` with upstream tracking.
UNVERIFIED: No interactive browser walk this session — `preview_start` failed with a sandbox-level `getcwd` permission error unrelated to the repo (not a code issue; retried once, same failure). Relying on S-061's prior manual QA of the M31 base plus S-063's own typecheck/vitest coverage of the new nav/views/run-label modules. If the operator hits a runtime issue the type/test layer wouldn't catch (e.g., a client-only hook ordering bug), that's the residual risk.
REJECTED: none new.
NEXT: A live browser walk of the pushed branch (sidebar, Setup views, run setup, `/resonance` library + `[studyId]` workspace, Dashboard simulation wall) is still owed — do it next session or once `preview_start`'s sandbox issue is resolved. Two pre-existing lint warnings (unused `AppMenuSeparator`, unused `_responses`) are cheap cleanup, not urgent.
GOTCHAS: `Taste skill/` (an 87KB `taste SKILL.md`, a Claude Code skill definition unrelated to this product) sat untracked alongside the real WIP — deliberately excluded from every commit and flagged to the operator rather than silently dropped or committed. Never `git add -A`/`git add .` per repo convention; every stage this session used explicit paths.

## S-063 / 2026-07-10 / M32: operator workflow UI (implementation)
GOAL: Execute D-088 / `M32_BUILD_PLAN.md` on `m32-workflow-ui` — sidebar shell, segmented views, provider-readiness runs, Simulation library/detail, Dashboard/Report walls.
DONE: Phase 1 shell + URL parsers + retired horizontal nav. Phase 2 Projects table, Settings providers/defaults + Add/Rotate dialogs, Setup `?view=`, Matrix overview/pillar views + version selector. Phase 3 Configure run with credential/secondary readiness, Advanced injection, dynamic Start labels, richer run list, run detail `?view=overview|events|extraction|metrics`. Phase 4 `/resonance` library + `/resonance/[studyId]` workspace. Phase 5 Dashboard audit vs `view=simulation` wall, Report one-section + Export menu. Verified: `tsc --noEmit` clean; focused vitest (views/nav/run-labels/wall) green; eslint on touched runner/report/matrix paths clean.
UNVERIFIED: Full `pnpm test` / `pnpm build` / browser walk still needed before marking PRD M32 Done. Setup still one `setup-client.tsx` (sectioned by view, not yet split into `sections/` files). Dirty-edit navigation prompt not wired. Evidence filters on study detail may still be incomplete vs plan.
REJECTED: none new vs D-088.
NEXT: Run full `pnpm test` + `pnpm build` (kill any Next owning `.next` first, D-075). Browser walk Insta 360. Then PRD tracker → Done, `M32_BUILD_PLAN.md` status header → implemented, commit `M32: segment operator workflow UI (D-088)` when asked.
GOTCHAS: Do not `git add -A` (brand/site WIP). Port 3000 often occupied. Ephemeral Postgres needs non-sandbox for wall tests.

## S-062 / 2026-07-10 / M32: operator workflow UI architecture (planning and handoff)
GOAL: Turn the full operator workflow review into a buildable M32 plan: replace stacked horizontal navigation, segment long work surfaces, make edit state explicit, and preserve C-4/C-12/C-13 boundaries.
DONE: Approved `M32_BUILD_PLAN.md` as the detailed execution source. Recorded D-088 and PRD 8.31/M32 tracker. The plan covers one left sidebar; typed URL view state; accessible menus/dialogs/fields; Projects/Settings; intake, Setup, Matrix; provider-readiness run setup and run detail; Simulation library/detail/results/evidence; Dashboard and Report segmentation; docs/tests/browser QA. `PROTECTED_REGISTER.md` now states M31's separate-page and C-12 wall protections in their M32 sidebar/detail-route form. Checked the standalone-plan risk against the M21-M26 precedent and D-086 taxonomy: M32 now states its limited execution-playbook ownership, and the generic boot ritual explicitly requires a named approved milestone plan.
UNVERIFIED: No product code, dependency installation, tests, build, or browser QA ran in this planning session.
REJECTED: Route-segment renames; a merged Setup/Matrix form; pooled audit and Simulation views; eager all-study results on `/resonance`; always-open persisted text fields; a migration.
NEXT: Create `m32-workflow-ui` from `m31-workspace-hierarchy`, then execute M32 in the six ordered phases in `M32_BUILD_PLAN.md`: navigation foundation; Projects/Settings/edit semantics; intake/Setup/Matrix; Runs; Simulation; Dashboard/Report/verification.
GOTCHAS: Read D-088 and the updated protected rows before removing M31 navigation components. Preserve legacy `#study-<id>` anchors while new links use `/resonance/[studyId]`. Do not `git add -A`; the brand/site WIP remains untracked. Do not run `pnpm build` while a dev server owns `.next`.

## S-061 / 2026-07-10 / M31: project workspace hierarchy (implementation)
GOAL: Build D-087 end to end on `m31-workspace-hierarchy` — hub, 4-item subnav, Setup sub-tabs, Runs grouping, walled Dashboard Simulation summary, C-12 wall-test extension.
DONE: (1) Hub `src/app/projects/[id]/page.tsx` + pure `workspaceHubSections` / `summarizeSimulationStudy` in `src/core/workspace.ts`. (2) Subnav → Setup/Runs/Dashboard/Report; project name → hub; `/matrix`+`/resonance` mark Setup active. (3) `SetupSubnav` on setup/matrix/resonance (separate pages, C-4). (4) Runs grouped by `matrixKind`. (5) Dashboard `SimulationSummarySection` (ΔPI per engine, SIMULATED, link to `/resonance#study-<id>`); audit `DashboardClient` untouched. (6) Breadcrumbs/projects-list/error fallback → hub. (7) `resolveProjectStage` completed-sim nextPath → `dashboard`. (8) Wall test: same-project audit vs resonance selector exclusion. Verified: `getProjectPipelineState` already resonance-aware (S-060 gotcha closed). `pnpm lint`/`typecheck` clean; `pnpm test` 493 passed / 12 skipped; `pnpm build` green. Manual walk on Insta 360: hub statuses, Setup tabs, Runs Audit/Simulation headers, Dashboard Simulation cards (deepseek +0.57 / openai +0.15).
UNVERIFIED: none for the D-087 scope.
REJECTED: none new — held the two D-087 rejections (no matrix/setup merge; no pooled metrics view).
NEXT: Nothing pending from M31. Open parallel track remains M10 deploy + grounded-provider gates (`RELEASE_CHECKLIST.md`).
GOTCHAS: Browser tool clicks on Next Link sometimes no-op'd during QA — direct `browser_navigate` worked. Port 3000 was already occupied; used 3001 for the walk. Do not `pnpm build` while a Next process still holds the shared `.next` (D-075) — killed the M31 `pnpm dev` before build.

## S-060 / 2026-07-10 / M31: project workspace hierarchy (planning only, no code written)
GOAL: Turn the operator's workspace-hierarchy proposal (single project hub, Simulation demoted from a peer tab) into a finalized, buildable nav plan and hand it off — this session did NOT touch any code, only `MASTER_CONTEXT.md`/`PRD.md`/`BUILD_NOTES.md`.
DONE: Read the current nav surfaces before proposing anything: `src/components/project-subnav.tsx` (flat 6-item pill nav, no hub — `/projects/[id]` has no `page.tsx` at all, only `layout.tsx`), `src/app/projects/[id]/runs/page.tsx` (confirmed it ALREADY lists audit and resonance runs together with a `matrixKind === "resonance"` -> `SimulatedBadge`), `src/app/projects/[id]/resonance/page.tsx` (confirmed it conflates study-definition wizard + results rendering in one page), `src/app/projects/[id]/dashboard/page.tsx` (audit-only, `fetchRunOptions`/`fetchDashboardData`). Checked `PROTECTED_REGISTER.md` for anything blocking — nothing protects the subnav's current 6-tab shape by name; D-077's protected row (`resonance` route segment must not be renamed) directly informed keeping `/matrix` and `/resonance` as route segments in the new plan. Finalized plan recorded as `MASTER_CONTEXT.md` D-087 (full detail there — read it first, this entry only summarizes) and a new `PRD.md` M31 tracker row (status "Not started").
UNVERIFIED: Everything — no component was written, no page created, nothing run against the dev server. The plan is a design decision only.
REJECTED (both from the operator's original proposal, both replaced in D-087, do not re-propose): (1) folding the prompt matrix board into the Setup page as one merged form/page — risks blurring the C-4 mutable-input-vs-frozen-evidence line; use sub-tabs on separate pages instead. (2) a literally single/unified Dashboard view mixing measured and simulated metrics — direct C-12 risk (measured and simulated may never share a chart or selector); use a walled Simulation section with its own study selector that links through to the existing Simulation results page.
NEXT: Start from `MASTER_CONTEXT.md` D-087 (the finalized plan) and this repo's boot ritual (§8: MASTER_CONTEXT -> PRD.md M31 section -> `DEVELOPMENT_GUIDELINES.md` §A -> this entry -> `DESIGN_GUIDELINES.md`, since this is UI-facing). Build order suggestion (not binding, re-plan if a step reveals a better order): (a) hub page `src/app/projects/[id]/page.tsx` + wire breadcrumbs/projects-list links to it instead of `/matrix` — lowest risk, purely additive, unblocks manual QA of the rest; (b) subnav collapse in `project-subnav.tsx` to Setup/Runs/Dashboard/Report, with active-state logic mapping `/matrix` and `/resonance` pathnames under the "Setup" pill; (c) a shared sub-tab row component (Inputs | Prompt matrix | Simulation studies) rendered at the top of `/setup`, `/matrix`, `/resonance` pages; (d) Runs page: group the existing `runs` array by `matrixKind` under two `<h2>` headers, no query change needed (`listRunsWithProgress` already returns `matrixKind`); (e) Dashboard: new walled Simulation section — likely a server component listing approved studies via `listResonanceStudies`/`getResonanceStudyResults` summarized (headline ΔPI per provider group, D-080 shape) with a "full results ->" link to `/resonance#study-<id>`, rendered clearly separated from the audit dashboard content with its own heading and `SimulatedBadge`; extend the C-12 wall test suite to assert this section never reads from audit metric scopes. Plan before any multi-file edit per this repo's session rules; re-read `PROTECTED_REGISTER.md` before renaming or removing anything, not just before this planning pass.
GOTCHAS: The plan intentionally leaves the Simulation section on Dashboard as summary-with-link-through, not full embedded results tables (D-087's own rejected-alternatives line calls this "deferred, not rejected outright" — if the operator asks for richer inline sim data mid-build, that's an in-scope judgment call for this milestone, not scope creep). `getProjectPipelineState` (used by the existing `ProjectNextAction` banner and proposed for the new hub's per-section status lines) is audit-only-aware per D-071's OX-2 fix history — verify it already includes resonance pipeline booleans before assuming the hub can read it as-is; if it doesn't, that's a real gap to fix in this milestone, not a pre-existing one to work around. No migration is expected anywhere in this plan; if implementation reveals a genuine need for one, stop and get it confirmed rather than assuming D-087's "no migration" line was wrong.

## S-059 / 2026-07-09 / M30: whole-repo cleanup audit
GOAL: Run the M30 whole-repo cleanup audit end to end: research/classify first (Passes 0-5), then execute the findings as separately-committed batches, per `AUDIT_METHODOLOGY.md`.
DONE: (1) Plan artifact `AUDIT_METHODOLOGY.md` written — four-tier action label (`Delete`/`Merge`/`Archive-Relabel`/`Keep — Protected`) and the document-class taxonomy (current-state / append-only history / proposals-plans, §4) that governs every later fix. (2) Pass 0: `PROTECTED_REGISTER.md` — 29 protected surfaces across 85 Decision Log entries (D-001..D-085), each a thing that looks dead/redundant but was explicitly argued into existing; consulted before every later `Delete`/`Merge` proposal. (3) Killed the pre-D-077 "marketing funnel / Upper-Mid-Lower Funnel" framing that MASTER_CONTEXT §1, PRD §1 Vision, README's opening line, and the glossary's "funnel stage" entry still led with, rewriting to the Evidence Layer / Simulation Layer framing D-077 actually established; backfilled PRD.md §§8.23-8.30 with requirement IDs for the M21-M28 work that had shipped with only Decision Log + tracker coverage (commit `ef04ffb`). (4) Passes 1-5 produced `AUDIT_DOC_DRIFT_MATRIX.md` (20 numbered findings), `AUDIT_REGISTER.md` (94 exports across the 7 largest files reviewed, exactly one dead export found), `AUDIT_REPO_INVENTORY.md` (repo-root classification + the vestigial-`.gitkeep` finding), `AUDIT_STRUCTURE_MAP.md` (C-7 boundary re-check, clean; 7 structural-split proposals, none executed). (5) Executed the findings as 6 ordered commits on `m30-repo-cleanup-audit`: Commit 1 (`b3f0252`) synced the remaining current-state doc drift (DD-1/2/3/5/6/10/11/12/15/16 — DD-4/7/8/9/13/14 were already fixed by `ef04ffb` and skipped after re-verification); Commit 2 (`55bdb82`) added status headers to the three executed proposal/plan docs (DD-17/18/19), correcting `M21_M26_BUILD_PLAN.md`/`LAYERS_AND_EVIDENCE_ONLY_EVALUATION.md`'s factually-false "untracked, uncommitted, NOT canon" self-descriptions; Commit 3 (`fd65dcb`) removed 18 of 19 tracked `.gitkeep` files whose directories already held other tracked content, keeping `public/.gitkeep` (the one directory with no other tracked content, since `public/brand/` is untracked operator WIP); Commit 4 (`be836f7`) deleted the one confirmed-dead export, `listRuns` (superseded by `listRunsWithProgress`, D-048); Commit 5 (`6a30fe2`) committed the 4 audit docs for the trail and added a docs/audits disposability convention to `AUDIT_METHODOLOGY.md` §8.
VERIFIED: Per-commit gates as specified — Commit 1: `tsc --noEmit` clean, grep-verified no remaining stale phrases in every edited section. Commit 4: `rg "listRuns\("` re-confirmed exactly one hit (its own definition) before deleting; `tsc --noEmit` clean; runner-related tests 43/43; full suite 488 passed / 12 skipped (73 files) — matches the M29 baseline exactly. Commit 6 (this close-out): see the PRD.md M30 tracker row and progress note for the final full-gate results (tsc, lint, full test suite, build-or-skipped).
NEXT: Nothing pending from this milestone. M10's own close-out (deploy, remaining live providers, Gemini grounding caveat) remains the only open parallel ops track, unaffected by this audit.
GOTCHAS: `AUDIT_STRUCTURE_MAP.md`'s 7 structural-split proposals (one per Pass-3 hotspot file, confidence ranging high for `setup-client.tsx`/`report-templates.ts`/`metrics.ts` to low for `worker/index.ts`) are deliberately NOT executed by this audit — they are graduated into D-086's text as named future proposals before the working doc is deleted in this same close-out commit, per the methodology's "pure-move-only, separate PR" rule for structural splits.
