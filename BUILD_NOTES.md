> LIFECYCLE: ACTIVE · ROLE: RECORD · OWNS: disposable per-session working memory; truncated at milestone merge (D-025)

# BUILD_NOTES.md - Session Working Memory

> Disposable mid-milestone state for agent handoff. This file answers "what was the last session doing?" — nothing else. Durable facts do not live here: decisions go to the `MASTER_CONTEXT.md` Decision Log, status goes to the `PRD.md` tracker, schema/contract facts go to the spec docs.

## Rules

1. **Append one entry per session**, as step one of the handoff ritual — or immediately whenever stopping mid-task or blocked.
2. **Disposable:** when a milestone merges, delete its entries. Anything still worth keeping must graduate to a canonical doc first; if it can't, it wasn't worth keeping.
3. **No restating:** never duplicate what a commit message, the PRD tracker, or the Decision Log already says. The unique value here is dead ends, unverified work, and the exact next action.

## Template

```
## S-<NNN> / <YYYY-MM-DD> / M<N>
GOAL: <one line: what this session set out to do>
DONE: <verified work, each with the command or check that proved it>
UNVERIFIED: <built but not yet proven; say what proof is missing>
REJECTED: <approach tried and abandoned + why, one line each>
NEXT: <the exact first action for the next session — command or file-level>
GOTCHAS: <environment quirks, surprising behavior, anything that cost >10 minutes>
```

Session numbers increment forever and never reset; omit empty fields except NEXT, which is mandatory.

---

## Entries

## S-122 / 2026-07-31 / M51 operator UI honesty and remediation

GOAL: Complete and harden the reviewed M51 UI/UX roadmap on top of M50.
DONE: Six roadmap phases implemented with no migration in `ecec3e7`. Review hardening now uses pillar-first deterministic prompt samples; canonical findings orchestration in `src/modules/analysis/findings.ts` with executed dashboard/report freshness tests and C-12 checks; source-specific study dirty clearing; saved off-page baseline restoration backed by a project-scoped repository lookup; dead-letter ownership failures returned as `ActionResult` and client remediation guarded by independent pending state plus `finally`; live confirmation always shows known configuration; active run-mode call sites use `RunModeStamp`; forecast E2E assertions cover calibrating presence and ready/recalibrating/paused/offline/terminal absence. The main Playwright gate found the advisory checklist’s warn text below AA (4.34:1/4.02:1); explicit existing ink tokens fixed it and the focused axe test passed. Final gates: lint zero-warning; typecheck; docs:check (24 governed root docs); Vitest 900 passed / 12 skipped / 0 failed; production build; `test:e2e` 18/18; `test:e2e:forecast` 4/4.
NEXT: Open the M51 PR after M50 merges, or retarget the branch after M50 lands.
GOTCHAS: GitHub Desktop auto-stashed the M51 tree when switching to M50; restored `refs/stash` onto `m51-ui-ux-roadmap`. Unrelated fact-sheet/debug/site files and the separate Apple-skill deletion remain outside M51 scope.

## S-121 / 2026-07-31 / M50 live-run remaining-time forecast (D-120)

DONE: Replaced the M46 point ETA (historical-seed + EWMA) with a live-run-only p10–p90 remaining-time forecast. Core module `src/core/run-forecast.ts` (calibration floor 10, rolling five-completion windows over latest 20, stale-pace recalibration at 3× slow cadence); `getRunDetail` returns `forecast` from current-run terminal pipeline timestamps only; run page renders ready copy exactly as `Estimated N–M min remaining` with `data-testid="run-forecast"`; dedicated Playwright forecast harness (`pnpm test:e2e:forecast`, port 3101, fixture heartbeat) covers ready/recalibrating/calibrating/paused while the main harness keeps WORKER OFFLINE. D-120 supersedes D-117 ETA portion; PRD §8.38. Gates: lint `--max-warnings 0`, typecheck, docs:check, Vitest 884 passed / 12 skipped, production build, `test:e2e` 18/18, `test:e2e:forecast` 4/4.

NEXT: Merge `m50` when ready.

## S-120 / 2026-07-29 / M49 implementation
GOAL: Ship Resonance Message Lift tests with Buyer response and AI recommendation under one transparent A/B workflow.
DONE: Created `m49` from post-M48 `main`; added migration 0023, prompt-parity compilers, deterministic recommendation parsing, test-type processing/cost/metric dispatch, Message Lift creation/design/Prompts/results surfaces, recommendation-aware reports, and exact-request Evidence JSON. Closed a browser-discovered blank-test gap by seeding the required Current/New message pair at creation. Buyer response and AI recommendation both pass mock generation → extraction → metric/result flows. Final gates: lint zero-warning; typecheck; docs:check; Vitest 866 passed / 12 skipped / 0 failed; production build; Playwright 18/18.
NEXT: Review the `m49` diff, then commit and open the milestone PR.
GOTCHAS: Sandbox Postgres initialization cannot allocate SysV shared memory, so DB-backed gates must run in the allowed environment. The Next.js production build requires network access for the configured Google Fonts.

## S-095 / 2026-07-13 / Lead review + M40 serving-path completion pass
GOAL: Lead (Fable) war-room review of the sprint, then execute the corrective engineering lane. Verified four completeness claims by grep before steering; three failed — STATUS's "no offline engineering remains" was premature.
DONE: **Phase 0** STATUS corrected + ledger rows relabeled to D-092 vocabulary (M38/M40 "In progress", acceptance unmet); surfaced the OPEN OPERATOR DECISION: launch engine scope (AGENT_PRD §5 three-engine mandate vs OpenAI-only MVP — blocks M42). Then, phased commits on `m40`: **A** `core/agent-input.ts` — §2 strict Zod requirement schema (6 fields, extra-field reject, string/non-object/>2KB shape gates first), canonical requirement hash + replay/conflict decision, R7 JSON Schema + parity test. **F** M39 completion — `during_reconcile` crash hook wired into the ledger's reconcile branch + `EFFECT_CRASH_MATRIX` committed artifact (30 cells, every one executed; §6.5 fields per cell). **G** AGENT_PRD §11 compliance — crypto_token runs skip LLM extraction entirely (worker run-config carries project archetype; agent e2e asserts zero extraction rows, 15/15). **B** `core/agent-redaction.ts` — redact_v1, exactly two categories (credential/secret: 64-hex, API-key formats, JWT, entropy tokens, mnemonic-shaped via function-word screen; personal: email/phone/SSN-shaped), offsets+rule+policy+source-hash retained; wired into published report quotes. **C** `core/agent-envelope.ts` (<2KB canonical type/value envelope, 256-bit capability tokens, constant-time verify) + **migration 0018** (additive `report_json` on agent_deliverables) + `publishDeliverable`/`getPublishedDeliverableByCapabilityHash` + public `GET /api/agent-report/[token]` (rate-limited, ETag=digest, noindex, middleware exclusion — the token IS the auth). **D** `core/agent-admission.ts` — §3 economics (fee≤20% from LIVE contract reads, net≥3×COGS, ±$0.01 reconcile) + §4.6 timing (10min funding timeout, 75min runway, expiry−15min submit deadline) + launch capacity defaults + structural checks; price constant deduped to one home. **E** `core/agent-manifest.ts` — immutable offering manifest, C-16-clean description (advice AND risk lexicons), per-section + whole digests, example requirement validates against the Zod source. **H — flake: TWO code-confirmed pollution vectors closed.** Vector 1: `saveCredential` is disable-then-insert per provider (D-020), a GLOBAL mutation; settings/actions.test.ts and extraction/live-pipeline.test.ts both exercise it on deepseek — serialized with a session-scoped advisory lock (`credentials.test-helpers.ts`). Vector 2 (found when the suite failed once more AFTER the lock): my M38 build-run.test left a QUEUED live_validation openai run with no cleanup, and `findActiveLiveRunUsingProvider` matches any queued/running audit-kind live run for deepseek via its extraction SECONDARY — the run is now cancelled in-test. **Post-both-fixes: 4/4 consecutive full runs green (780 passed / 12 skipped / 0 failed).** Honest caveat: the 9-failure trace was never captured verbatim, so the second vector's coverage of all 9 is inferred from mechanism + timing, not observed — if the settings suite ever fails in a full run again, capture the FULL log before touching anything (`pnpm test > log 2>&1`).
UNVERIFIED / STILL OPEN: The real SDK/viem VirtualsGatewayClient + gateway event loop (SSE+poll+reconcile calling ingestEvent/advanceOrder/admission) remain wallet-gated — the new admission/input/envelope functions are pure and tested but NOT yet composed into a serving loop (that loop is the M40-live work). M35 kill-gate still 0/10 (register A1 — existential). A6 COGS unmeasured until the full M38 spike. Launch engine scope needs the operator ruling.
NEXT: Merge `m40` → `geo-agent-v1`. Operator: OpenAI key → live-validate; acp-cli onboarding → 3 values → M35 proofs (STOP if the signer relay fails, per A1); rule on engine scope. Engineering (wallet-gated): compose the gateway loop from the now-complete parts + real VirtualsGatewayClient, then the §6.6 live gate.
GOTCHAS: The middleware matcher gates EVERYTHING except _next/static|_next/image|favicon|health — any new public endpoint must be added to PUBLIC_PREFIXES or it silently redirects to /login (the report endpoint was caught in review). drizzle-kit names migrations randomly (`0018_chunky_vision`) — rename + journal edit, same as 0016/0017. The mnemonic redaction rule uses a function-word screen because @scure/bip39 isn't in the offline store; swap to the real wordlist when network allows.

## S-094 / 2026-07-13 / Docs: verify Virtuals onboarding inputs (D-111)
GOAL: Operator pushback — "most of these [wallet-setup fields] are not required on Virtuals' wizard; fact-check the latest docs and find which are MVP-necessary."
DONE: Fact-checked §5.1 against the pinned SDK (`acp-node-v2@0.1.7` README + `ACP_CONTRACT_ADDRESSES` export, read locally) and live Virtuals docs/web. Confirmed a seller needs only three operator-fetched values (`walletAddress`, `walletId`, `signerPrivateKey`, + optional `builderCode`); registration auto-creates the smart wallet; "+ Add Signer" generates the P256 signer (no manual PKCS#8); contract addresses ship in the SDK (Base 8453 = `0x238E…832E0`) so `ACP_EXPECTED_*` are drift-verification targets not inputs; evaluator optional (zero-address default). Rewrote `AGENT_BUILD_PLAN.md` §5.1 + gateway-secrets list; annotated register A4 (addresses SDK-shipped) + A8 (registration/signer flow verified; sandbox counts still 404); recorded D-111. (`.env.example` BASE_RPC_URL/ETHEREUM_RPC_URL was added in the prior commit.) THEN studied os.virtuals.io/quickstart (operator-supplied) + confirmed `@virtuals-protocol/acp-cli@1.0.24` (published 2026-07-02) on the npm registry: a newer EconomyOS CLI does the whole onboarding (`acp configure` → `acp agent create` → `acp agent add-signer` → `acp wallet address`/`whoami` → `acp wallet topup --chain-id 8453 --method coinbase` → `acp offering create`), fully corroborating D-111. Added both onboarding paths (CLI + dashboard) to §5.1 with the key nuance: the CLI stores the P256 signer in the OS keychain, but our gateway needs the RAW key in `ACP_SIGNER_PRIVATE_KEY` — use the dashboard **Copy Key** to extract it.
NEXT: Unchanged — offline engineering (M36–M40 core) is done; everything left is operator-gated (OpenAI key → M38 live; the 3 Virtuals values + funded test buyer → M40-live gateway wiring + M35 kill-gate; Gemini/Grok keys → full spike; legal → launch).
GOTCHAS: Virtuals whitepaper deep-links 404 heavily (register A8's exact warning) — the pinned SDK README is the authoritative onboarding source, not the whitepaper URLs. Sandbox job counts / review window remain unverified (pages 404) — still an M42-entry live check.

## S-093 / 2026-07-13 / M40 (offline core)
GOAL: Operator directive "run m40 first, I'll sort [wallets] out later." Build the offline-buildable M40 core (transport hardening + gateway boundary + lifecycle orchestrator wired to M39's ledger, fixture-driven).
DONE: **Phase 1** — pinned `@virtuals-protocol/acp-node-v2@0.1.7` (offline from the store, from m35) so the gateway can wrap it behind the §4.4 boundary; `src/core/agent-transport.ts` pure primitives (the raw SDK has an unbounded dedupe set + omits job id from its dedupe key + hides reconnect health): `eventFingerprint` (always includes job id), `BoundedDedupeSet` (10k/24h LRU), `reconnectDelayMs` (bounded exponential + full jitter), `ConnectionState` machine — 9 tests. **Phase 2** — `src/modules/agent/gateway.ts`: the `VirtualsGatewayClient` boundary interface (extends M39's ChainGateway with start/stop/verifyRegistry/sendMessage — the ONLY surface that touches the SDK/chain); `ingestEvent` (in-memory LRU + durable DB fingerprint dedupe, persist before business action); `advanceOrder` reads the CANONICAL on-chain job (never SDK-derived), takes the per-order advisory lock, and steps the lifecycle through M39's effectively-once ledger with §4.6 precedence (delivered-terminal → expiry-refund → set_budget → funded-submit). DB fixture test: full created→budget→funded→submit→completed happy path + event dedupe + admissions gating + expiry refund, each external effect exactly once — 4 tests. My M40 tests green; full suite 716 passed (the 9 failures are the known settings flake, below).
UNVERIFIED / WALLET-GATED (M40 does NOT merge as "done" — the §6.6 live gate is unmet): the REAL VirtualsGatewayClient (wraps AcpAgent/EvmAcpClient + viem for readJob/broadcast/getReceipt/registry) is NOT written — it's the one place that imports the SDK and can only be verified live. The §6.6 merge gate (complete/reject/refund/expiry/restart-recovery observed against hidden $0.01 sandbox jobs + deliverable fetch e2e + settlement ±$0.01) needs operator wallets. The offline orchestrator is proven against a fake ChainGateway; M40-live is a SWAP of the chain impl, not the orchestrator/ledger logic. Leadership is modeled as a process-level singleton (gateway loop holds `withTryLeadership` around its work) — `advanceOrder` itself only takes the per-order lock; the actual gateway-process event loop (SSE subscribe + 15s poll + 60s reconcile) is not built (it needs the live SDK transport).
REJECTED: Writing the real SDK-backed VirtualsGatewayClient untested (young 0.x SDK, API assumptions unverifiable without wallets — the §4.4 boundary + fixture contract is the honest deliverable; the real impl conforms at live time); nesting `withTryLeadership` inside `advanceOrder` (leadership is process-level, not per-order — removed).
NEXT: Merge `m40` → `geo-agent-v1`. **Offline engineering (M36–M40 core) is complete.** Everything left is operator-gated: wallets (real M40 gateway + live sandbox verification + M35 kill-gate + M41/M42), OpenAI key (M38 live validation), Gemini/Grok keys (full M38 spike), legal acceptance (launch). When wallets exist: write the real VirtualsGatewayClient conforming to the boundary interface in `gateway.ts`, build the gateway-process loop (SSE+poll+reconcile calling `ingestEvent`/`advanceOrder`), and run the §6.6 live lifecycle.
GOTCHAS: The SDK exports the `ApprovalRequired`/`ApprovalRejected`/`ApprovalTimeout` errors (§5.1 `APPROVAL_REQUIRED` is a launch-blocking fault — the restricted signer policy must be attached, else the SDK waits ~5 min for human approval). The known settings-test flake (`settings/actions.test.ts`, task_a9867727) recurred under M40's added DB parallelism — 9 of its own credential tests fail intermittently in a full run, pass in isolation/re-run; NOT an M40 regression. bigint drizzle default = `sql\`0\`` not `0n`.

## S-092 / 2026-07-13 / M38 (OpenAI-only slice)
GOAL: Operator directive "do everything, real API test using OpenAI only, leave Grok/Gemini empty." Build the live path for M38 with OpenAI only.
DONE: The OpenAI grounded adapter already existed (web_search + normalized url_citations, in LIVE_FACTORIES). Added: (1) viem live RPC metadata reader `createRpcMetadataReader` (chainId/bytecode/name/symbol/decimals, bytes32 fallback via pure `decodeErc20String`, bounded timeout, env-gated on BASE_RPC_URL/ETHEREUM_RPC_URL) — the M38/M40-deferred resolver reader; (2) `buildAgentRun` parameterized with `engines`/`runMode`/`repetitions` so an OpenAI-only `live_validation` run (k=2, 40 planned) is creatable — production default stays 3-engine k=5 mock; (3) `pnpm agent:live-validate` harness — real worker against live grounded OpenAI over a real token, small k, hard cost cap, then builds the mechanical report + C-10 grounding check (grounded answers must carry citations); refuses without BOTH a stored OpenAI credential AND `--confirm-spend`. Offline tests: resolver 22 (incl. bytes32 decode), build-run 4 (incl. OpenAI-only live_validation run creation, no spend), harness spend/confirm guards smoke-tested. Full suite **712 passed / 12 skipped / 0 failed**; typecheck + eslint clean. Two commits.
UNVERIFIED: **The live OpenAI run has NOT been executed** — it needs an operator-entered OpenAI key in Settings (C-11; I cannot enter keys or spend). Once entered, `pnpm agent:live-validate … --confirm-spend` runs it. The full M38 acceptance (900 samples = 3 assets × 3 engines × 100) is NOT met — this is an OpenAI-only slice; Gemini's adapter exists (`createGoogleProvider`) but is intentionally left unwired per the directive, and xai stays metadata-only (no LIVE_FACTORIES entry). C-10 is enforced as a report-level grounding check in the harness (per-engine citation coverage), NOT yet as a job-level hard-fail — that lands with M40's dedicated agent gateway path (the shared worker/adapter is left untouched to avoid audit-product regressions).
REJECTED: Enforcing C-10 empty-citation hard-fail inside the shared OpenAI adapter (would change audit-product behavior + tests — deferred to the agent gateway); adding a provider-id field to GenerationRequest (frozen, C2); running the live spike myself (no key access, real spend — operator's to run).
NEXT: Merge `m38` → `geo-agent-v1`. Operator runs the live OpenAI validation (above). Next engineering milestone is **M40 (ACP gateway live in sandbox)** — needs the `@virtuals-protocol/acp-node-v2` pin from `m35` merged in (it is NOT on geo-agent-v1) + the operator wallets; the M39 effects ledger is already proven against a fake ChainGateway, so M40 swaps in the real viem/SDK gateway, not the ledger logic. Full M38 spike waits on Gemini + Grok keys.
GOTCHAS: **Known flaky test (pre-existing, not M38):** `src/modules/settings/actions.test.ts` intermittently fails ~9 of its own credential-encryption tests under full-suite parallel load (it deletes the process-global `CREDENTIALS_ENCRYPTION_KEY` mid-test); passes 13/13 in isolation and on re-run (712/0 on retry). Spawned a background task to harden its isolation — do NOT treat it as an M38 regression. viem `hexToString` on a bytes32 leaves trailing NULs — `decodeErc20String` strips them before sanitization or a valid token (e.g. MKR) would be rejected as containing control chars.

## S-091 / 2026-07-13 / M39 (COMPLETE)
GOAL: M39 — commerce persistence + effectively-once effects, offline $0 (§6.5). The last operator-independent milestone.
DONE: All six phases, committed at each green boundary. **P1** schema `src/db/schema/agent.ts` + migration `0017_agent_commerce` (7 §4.3 tables + enums; new CREATE TYPE so no ALTER-ADD-VALUE trap; verified fresh+existing DB). **P2** `src/db/repositories/agent-commerce.ts` — idempotent order ingest, event fingerprint dedup, a DB-backed `EffectStore`, transaction-scoped advisory locks (leadership + per-order), admissions kill switch, deliverable/settlement upsert. **P3** `src/core/agent-order-state.ts` — §9 three-machine separation (acpFromChain / EXEC_TRANSITIONS / resultMapping), §4.6 result mapping (delivered⇒completed regardless of sparse evidence; expiry⇒refund+attribution; pre-funding⇒reject; post-funding⇒refund; evaluator⇒terminal). **P4** `src/core/agent-effects.ts` — the §4.5 nine-step `executeEffect` (pure, parameterized by EffectStore + ChainGateway); read-chain-before-broadcast is what makes crash-after-broadcast safe; ambiguous⇒unknown (never blind-retry); P0 freeze on would-be overwrite. **P5** DB integration test proving unique-constraint once-ness under concurrency, event dedup, advisory-lock dual-instance serialization, DB-backed ledger applies once. **P6** completed the enumerated matrix. **Acceptance (§6.5, every cell not a sample):** `agent-effects.test.ts` 32 tests — every effect type × every crash point (after insert/broadcast/record/confirm) applies the external effect EXACTLY ONCE across restart, + ambiguous-landed/not-landed, reverted, P0, blocked, DB-outage, out-of-order/dropped; `agent-order-state.test.ts` 14; `agent-commerce.test.ts` 5 (DB). Full suite **709 passed / 12 skipped / 0 failed**; typecheck + eslint clean; docs:check green.
UNVERIFIED: The ChainGateway is an interface with in-memory/inline fakes only — the REAL viem-backed gateway (readJob, broadcast, getReceipt against Base) lands in M40, where the ledger runs against live sandbox jobs. No network touched in M39 by design. The effects ledger's precondition/isApplied semantics model a faithful-but-simplified ACP job (enough to exercise every crash/injection cell); M40 replaces the fake chain, not the ledger logic.
NEXT: Merge `m39` → `geo-agent-v1`. **Engineering is now fully operator-blocked** — every remaining milestone needs operator inputs: M38 (OpenAI/Gemini/xAI API keys in Settings + ~$25 live spend for the 900-sample spike), M40 (the M35 wallets: funded Base RPC, seller/test-buyer Privy wallets, restricted signer, hidden offering), M41/M42 (Render deploy, funded buyer, Virtuals onboarding, recorded legal risk-acceptance). Next code work is M38's live adapters (xAI/OpenAI/Gemini grounded paths) the moment API keys exist, or M40's VirtualsGatewayClient once wallets exist.
GOTCHAS: bigint drizzle default must be `sql\`0\`` not `0n` (serializer). Advisory locks are transaction-scoped (`pg_advisory_xact_lock`) NOT session-scoped — a pooled connection would otherwise not hold the lock across the callback; `withTryOrderLock` runs the callback inside the same transaction. The dual-instance test uses an explicit acquire→signal→barrier to make the race deterministic. The ledger NEVER trusts SDK-derived state — `chain.readJob()` before every action is load-bearing, not defensive padding.

## S-090 / 2026-07-13 / M37
GOAL: Sprint M37 (mechanical extraction + metrics + report, $0) end-to-end on a fresh `m37` branch (cut after merging m36 → geo-agent-v1). Operator directive: fast build, keep tests.
DONE: All AGENT_PRD §6–§10 mechanical pipeline as pure core functions (C-5). **Phase A** — descriptor_v1 + advice_prose_v1 + refusal_v1 lexicons; offset span finders; C-C length-preserving name/ticker masking (`maskIdentity`/`maskedLexiconHits`): a token named "AI Corp"/"RugRadio" cannot manufacture a hit from its own name. **Phase B** — identity classifier (§8: matched/namesake/ambiguous/absent, address-anchored, explorer-anchored chain signals) + `deriveRepresentationState`. **Phase C** — per-engine metrics M1–M7, never pooled, lanes B/C never pooled; M6b `descriptorRepeatability` EXCLUDES empty/empty pairs → not_estimable (never reuses core `jaccardSimilarity`); Wilson on proportions only. **Phase D** — deterministic `buildAgentReport` (SHA-256 over canonicalized JSON) + `authoredProseViolations` C-16 guard (quoted evidence exempt). **Phase E** — hand-labeled golden `fixtures/mock-responses/crypto/golden-answers.json` + `agent-golden.test.ts` (per-sample extraction == labels, aggregate metrics exact, idempotent); `modules/agent/report.ts` builds the report from a run's stored responses (lane from variantKey, M7 accounting from jobs). **`pnpm test:agent-mock-e2e` extended: report built from the 300 stored responses — 3 engines, deterministic digest, C-16 clean, accounting collected=100/engine (14/14 checks green).** Full suite **658 passed / 12 skipped / 0 failed**; typecheck + eslint clean. Committed per phase: 95b02e9, c12594b, e6a9058, bdb2c90, + Phase-E commit.
UNVERIFIED: The mock e2e's representation_state renders `not_estimable` because the mock fixtures are B2B LedgerFox text (the crypto token isn't described) — this correctly exercises the honest-absence path but is NOT crypto-shaped evidence. Real descriptor/identity distributions only appear at M38 with live grounded engines over real token answers; the golden fixture is the crypto-shaped correctness anchor until then. No new DB tables added (agent receipt/deliverable tables are M39/M40 per §4.3); the report builder takes identity as a param (persisted asset-identity snapshot lands with agent_orders in M39).
REJECTED: Adding "volatile" to risk_v1 (frozen PRD §6 lexicon has "volatility" only — fixed the golden text instead, not the lexicon); a heavy DB-fixture unit test for the report adapter (the pure golden + the e2e over 300 real responses cover it — A2).
NEXT: Merge `m37` → `geo-agent-v1`, then M39 (§6.5: §4.3 additive migration for agent_orders/events/effects/deliverables/settlements + the enumerated transition/effect matrix — offline, $0, independent of M35). M38 (live grounded engines, xAI adapter, ~$25 spend) needs operator API credentials in Settings; do it when those exist.
GOTCHAS: refusal_v1 is a NEW lexicon beyond PRD §6's four — required for §5's refusal-vs-absent denominator distinction; deliberately narrow so "I can't find anything" stays `absent`, not a refusal. Block comments can't contain `*/` — the `a*/b*/c*` lane comment broke tsc until reworded. Chain-signal detection for Base is explorer-anchored ("basescan") + guarded name mentions because the bare word "base" is common English; if M38's real answers show misclassification, tighten `chainSignals` in agent-identity.ts.

## S-089 / 2026-07-13 / M36
GOAL: Execute M36 (headless audit core, mock-first, $0) end-to-end on a fresh `m36` branch — the full A→E arc from AGENT_BUILD_PLAN §6.2.
DONE: Branch `m36` cut from `geo-agent-v1` @ `0334994` (independent of the M35 harness). **Phase A** — split-statement migration 0016 adds `xai`/`crypto_token` (no in-batch consumer, D-066/D-102 trap avoided); synced the three hand-maintained mirror unions (`providers/types.ts`, `core/runner PROVIDER_IDS`, `core/semantic CategoryArchetype`) + new `AUDIT_ARCHETYPES` so `crypto_token` is type-complete but never in the operator dropdowns; upgrade-path test asserts both enum values on an existing pre-M34 DB (**DB test green**). **Phase B** — pure resolver `core/crypto-resolver.ts` (§PRD-3 steps 5–8) behind injectable reader `modules/agent/resolver.ts` (steps 1–4, viem checksum); `core/agent-lexicons.ts` (risk_v1 + prompt_control_v1, word-boundary + plural fold); 14-case adversarial fixture set — **all rejected pre-budget (44 unit tests green)**. **Phase C** — `core/crypto-prompts.ts` frozen 20-cell `crypto_geo_prompts_v1` (6/8/6), P4 design-time + P3 job-time scans (**14 tests green**). **Phase D** — `modules/agent/build-run.ts` headless contract→project→matrix→run path; mock-mode engine routing (D-110): `isProviderAllowedForRunMode` loosened on the mock side only, `createMockProviderFor(id)` keys D-016 fixtures per engine, metadata-only `xai` registry entry so the worker schedules it. **`pnpm test:agent-mock-e2e` green: 300/300 samples, 3×100 per engine, no dup job_id, per-engine variation, all adversarial rejected + no rows written.** **Phase E** — full suite **609 passed / 12 skipped / 0 failed**; typecheck + eslint clean; docs updated (MASTER_CONTEXT §5, DEVELOPMENT_GUIDELINES §F, DECISIONS D-110). Committed at each phase-green boundary (D-092): 185f955, 0e0567e, 203e958, 2dbb596, + this docs commit.
UNVERIFIED: None blocking. Scope refinement (flagged, accepted): crypto-flavored mock *answer* fixtures were NOT added this milestone — the agent mock run uses the existing audit fixtures (D-016 per-engine keying IS the "per-engine mock fixtures" mechanism, proven in the e2e). Crypto-shaped answers belong to M37's mechanical-extraction golden dataset, consumed directly by extraction tests, and will be authored there to the exact shape those goldens need (A2 — not speculative now).
REJECTED: Three single-engine mock runs and a 100-sample single-engine e2e (both miss the faithful 300/3-engine topology — operator chose mock-mode routing); adding a provider-id field to the frozen `GenerationRequest` (C-2 — bound-factory closure carries the id).
NEXT: Merge `m36` → `geo-agent-v1`, then start M37 (§6.3: versioned lexicons, name/ticker masking C-C, identity classifier, metrics M1–M7 incl. the separate `descriptor_repeatability` helper that must NOT reuse `jaccardSimilarity`, report JSON + C-16 forbidden-phrase suite). M37 is where crypto answer fixtures + golden expectations get authored.
GOTCHAS: Local embedded-Postgres exhausts `kern.sysv.shmmni=32` — reclaim dead segments with `ipcs -m -o` + `ipcrm -m <id>` for NATTCH=0 before running DB tests (recurs from S-088). The mock provider previously hardcoded `"mock"` in the D-016 key — any future multi-engine mock work must use `createMockProviderFor(id)`, not the shared `mockProvider`, or engines return identical fixtures. `xai` is metadata-only until M38: it has NO `LIVE_FACTORIES` entry, so a *live* xai run fails loudly at `resolveRuntimeProvider` by design.

## S-087 / 2026-07-12 / D-109: seven architecture-review corrections applied to M35-M42
GOAL: Adopt the external M35-M42 architecture review (verdict: strong product architecture, less disciplined roadmap) — all seven required corrections, verified against the docs before applying.
DONE: `AGENT_BUILD_PLAN.md`: §9 assumption-to-proof register added (A1-A10, each with proving milestone + failure action); M35 narrowed to 10 technical-only proofs (signer rotation → M41 drill; Butler + DevRel confirmation → M42 launch prerequisites); M38 acceptance now exactly 900 planned samples with per-(asset, engine) recording and capability-contract-before-price model selection; M39 requires the enumerated transition/effect matrix (effect types × crash points + event injections + dual-instance) with zero duplicates across every cell; M41 gains numeric soak thresholds + drills-vs-procedures runbook classification + automatic admissions freeze; M42 split into engineering completion (merges) vs external launch prerequisites (tracked in STATUS, never merge-gating). `AGENT_PRD.md` §10: redact_v1 reduced to two machine-testable categories, "illegal to redistribute" removed, unclassifiable-flagged content fails the job closed with post-terminal incident review. `STATUS.md`: launch-prerequisites table added; M42 row renamed. D-109 recorded. docs:check green.
REJECTED (D-109): none of the seven — each verified as a real defect; keeping signer rotation in M35 (fresh-wallet convenience loses to single-concern discipline); a separate assumptions doc (register lives in the build plan).
NEXT: Unchanged — start M36 on `geo-agent-v1` (AGENT_BUILD_PLAN §6.2); operator wallet setup unblocks M35 in parallel.
GOTCHAS: The register rule cuts both ways — any doc treating A1-A10 facts as settled must cite the register row, and docs:check does NOT enforce that (review-time discipline). M38's 900-sample count assumes the 20-cell matrix survives M36/M37 unchanged; if cell count changes, update §6.4's arithmetic in the same commit.

## S-086 / 2026-07-12 / M-numbering restored: Gates 0-D re-cut as milestones M35-M42 (D-108)
GOAL: Execute the operator's ruling — continue the repo-global M-counter, and break the one-shot gates into smaller, independently verifiable milestones.
DONE: D-108 recorded (mapping, rationale, rejected alternatives). `AGENT_BUILD_PLAN.md` §6 rewritten as M35–M42 with per-milestone merge-when acceptance + dependency map; all in-body Gate references swept (M35/M38/M40/M41/M42). `STATUS.md` rewritten with a milestone ledger; current state shows the load-bearing discovery: M36 (mock-first headless core) is UNBLOCKED and can start now — only M40+ waits on the operator wallet setup that blocks M35. `AGENT_PRD.md` freeze/verify references now name M35/M38/M42. `MASTER_CONTEXT.md` §7 index row + §10 updated (M-counter is repo-global, parked track closed at M34A). `scripts/docs-check.ts` STATUS field names updated (Current milestone / Milestone state). `pnpm docs:check` green.
REJECTED (D-108): keeping Gate vocabulary with a written rationale; one giant M35-with-phases; reopening PRD §11; per-milestone plan files (D-090 arc precedent).
NEXT: Start M36 on `geo-agent-v1` (enum migrations → resolver → matrix + scans → programmatic run path → mock fixtures → 300/300 mock e2e), per AGENT_BUILD_PLAN §6.2. Operator in parallel: wallet/account setup to unblock M35 (§5.1).
GOTCHAS: BUILD_NOTES S-084/S-085 entries still say "Gate 0" — append-only records, left as written; D-108 is the translation key. The docs-check validator now requires the exact STATUS.md field names "Current milestone"/"Milestone state" — renaming those fields again means updating scripts/docs-check.ts in the same commit.

## S-085 / 2026-07-12 / Documentation governance (D-107) — control plane, history boundary, validator
GOAL: Execute the operator-approved documentation-governance cleanup (external critique adopted with revisions) so the repo's doc set stops accreting and the active product has an on-disk plan and tracker.
DONE: Five commits on `geo-agent-v1`: (1) `STATUS.md` (active control plane) + `AGENT_BUILD_PLAN.md` (Gates 0–D recovered from the final reviewed chat plan, reconciliation record in its §8) + boot ritual routes through STATUS first + §10 status-home corrected. (2) Decision Log D-001..D-106 extracted verbatim to `DECISIONS.md` (diff-proven text-identical) + supersession register (12 edges) + handoff ritual amended (STATUS at step zero, decisions+edges to DECISIONS.md, archive-in-merge-commit, docs:check before handoff). (3) Eight executed/superseded plans → `docs/history/` with lifecycle headers, bodies verified byte-identical to pre-move blobs; lifecycle metadata headers on all 19 governed root docs (PRD + CALIBRATION_PROTOCOL = PARKED, BRAND docs = ACTIVE); §7 index rewritten to match reality. (4) `scripts/docs-check.ts` + `pnpm docs:check` + CI step: validates headers, history dispositions, STATUS fields/links, one-active-plan, local links, register integrity — failure modes tested (rogue root doc, wrong history lifecycle, broken link, backward edge all caught; first real run caught the register's forward-reference to unrecorded D-107). (5) D-107 recorded; PROTECTED_REGISTER row added. Final: docs:check green, tsc clean, eslint clean excluding the stray `.claude/worktrees/` copy (pre-existing, absent in CI checkouts).
REJECTED (D-107): `product/<slug>/` directories (deferred — path churn vs immutable references, single active track); a duplicated current-rulings table (second home for every rule); moving parked canon to history (parked ≠ historical); archiving BRAND docs (active brand); convention without CI enforcement.
NEXT: Unchanged from S-084 — Gate 0 per `AGENT_BUILD_PLAN.md` §6.1, blocked on operator wallet/account setup (STATUS.md is now the authoritative pointer for this).
GOTCHAS: `pnpm lint` currently sweeps a leftover `.claude/worktrees/strange-carson-2839bc/` working copy (1,400+ findings, none ours) — CI is unaffected (clean checkout); clean it up locally or ignore-pattern it if it bites. The docs:check header regex requires the metadata line to be the FIRST line of a governed file — new root docs must start with `> LIFECYCLE: …` or CI fails (that's the point).

## S-084 / 2026-07-12 / Product pivot: Resonance GEO agent (D-106) — park M34A, architecture frozen, clean plate for build
GOAL: Operator-directed pivot session. Park the Resonance product at its shipped M34A state, redeploy the Evidence Layer as an autonomous crypto AI-perception audit agent sold on Virtuals ACP, and clear everything blocking the new build so implementation can start from a clean tree.
DONE: (1) Three rounds of plan review: an initial 10-day war-room draft (`AGENT_PIVOT_PLAN.md`, now superseded), an operator-submitted external plan critiqued and partially adopted/rejected, a second operator-submitted plan fact-checked against live Virtuals/Base/vendor sources and the repo (zero-evaluator auto-settlement confirmed in `acp-node-v2`'s `acpAgent.ts` source; two enum-migration traps documented from D-066/D-102 history flagged; the `representation`/`discovery` intents already shipped by M34A were pointed out as reusable, no new intent value needed), then a final operator ruling resolving 7 open disagreements (zero-evaluator kept, B+C lane pooling rejected, `discovery_category` reinstated as query-context-only, name/ticker masking algorithm specified, two-metric repeatability spec, redaction scoped to deterministic illegal-content categories only, commercial criteria split out of engineering acceptance). `AGENT_PRD.md` rewritten to the ruling as the frozen product contract (plain-language, MUST/NEVER rules, exact 20-prompt matrix, per-lane metric tables, migration-trap warnings — written for lower-capability implementing agents per operator instruction). `AGENT_STRATEGY_MEMO.md` split out to hold kill/scale criteria and GTM, explicitly non-binding on engineering gates. (2) Park ritual executed: `docs/audits/m34/v4-cal-report.md` (untracked M34 CAL-1/CAL-2 report) committed to `m34-baseline-framing`; MASTER_CONTEXT.md updated in place (new C-16 descriptive-only / C-17 hostile-buyer-input constraints in §4; D-106 recorded in §9) and committed; `m34-baseline-framing` merged into `main` (fast-forward, 27 commits, D-095 through D-106); tagged `resonance-m34a-parked` at the merge point; new branch `geo-agent-v1` cut from `main`; `AGENT_PIVOT_PLAN.md` (status flipped to SUPERSEDED, body preserved as pivot-rationale record), `AGENT_PRD.md`, and `AGENT_STRATEGY_MEMO.md` committed as the founding commit of the new branch.
UNVERIFIED: No code exists yet — this session is planning/canon/repo-state only. Everything in `AGENT_PRD.md` §13 (zero-evaluator settlement, contract addresses, graduation process numbers) is source-cited but MUST be re-verified live at Gate 0/Gate D per the plan's own discipline — third-party pricing aggregators and the whitepaper's reorganized doc tree were both proven unreliable during this session's fact-checking and must never be treated as canonical without a live check.
REJECTED: Deleting or archiving the Simulation Layer/M34A code (parked, not removed — C-12's walls make this free); an LLM extraction layer in v1 (mechanical lexicons only, M34 lesson); accepting buyer-supplied project facts in the serving path (adversarial-buyer misinformation-register poisoning risk); pooling natural/anchored representation lanes for one completeness gate (operator's final pushback — prompt-frame rule one level down); buyer self-evaluation as an ACP settlement fallback; folding commercial kill/scale criteria into engineering acceptance gates (operator's ruling #7 — moved to `AGENT_STRATEGY_MEMO.md`).
NEXT: Gate 0 (protocol feasibility) on `geo-agent-v1` per the reviewed 0→1 Virtuals ACP build plan: fresh seller/test-buyer wallets, prove `AcpAgent.start()` + ACP-only signer policy + zero-evaluator auto-settlement + funded-rejection/refund + permissionless expiry claim on hidden Base-mainnet $0.01 jobs, before writing any production ACP code. Do not start Gate A (headless product / grounded engine adapters) until Gate 0 passes live.
GOTCHAS: `discovery_category` MUST NEVER be written to `category_archetype` — they are different concepts on different tables (agent projects always use archetype `crypto_token`; `discovery_category` lives on the agent order row and only selects a Lane-A prompt pack). Postgres enum additions (`xai` on `provider_id`, `crypto_token` on `category_archetype`) each need their own migration statement with no same-transaction consumer, per the D-066/D-102 trap — this repo has been burned by this exact mistake twice already. `src/core/extraction.ts`'s `jaccardSimilarity()` returns `1` for empty/empty sets and MUST NOT be reused unchanged for the new `descriptor_repeatability` metric (empty/empty must be `not_estimable`).

## S-059..S-083 / 2026-07-09..2026-07-12 / M30-M34A: merged to main, entries truncated per Rule 2 (D-025)
GOAL: (retroactive) These 25 sessions covered M30 (whole-repo cleanup audit), M31 (project workspace hierarchy), M32 (operator workflow UI architecture), M33 (verification/focus-state/demo-readiness close-out), and M34/M34A (Baseline Framing Integrity, descoped to human-reviewed framing evidence across D-094-D-105). All five milestones are merged to `main` and marked Done in `PRD.md` §11.
DONE: Durable content already graduated to canonical homes and is not repeated here — Decision Log entries D-086 through D-105 in `MASTER_CONTEXT.md` §9 carry full rationale for every ruling; `PRD.md` §11 progress notes carry per-milestone summaries; `M32_BUILD_PLAN.md`, `M34_BUILD_PLAN.md`, `M34A_PRODUCTION_PLAN.md`, `M34A_ASSURANCE_PLAN.md`, and `docs/audits/m34/*.md` carry full execution/acceptance detail for their milestones.
NEXT: Nothing pending from M30-M34A. Current work is S-084 above (Resonance GEO agent pivot, `geo-agent-v1` branch).
GOTCHAS: This truncation follows the same precedent set at the M30 cleanup audit (D-086, `BUILD_NOTES.md`'s own Rule 2): entries for a merged milestone are deleted once their content has graduated elsewhere, keeping this file small and disposable rather than an unbounded log. That precedent was skipped for five consecutive milestone merges (M31-M34A never got pruned) before this pass restored it — future merges should prune in the same handoff-ritual step that merges the branch, not as a separate catch-up pass.

## S-096 / 2026-07-07 / Brand site: Apple-design polish pass (Sonnet subagent, Fable supervised)
GOAL: Audit site/ against the newly-installed apple-design skill (.agents/skills/apple-design); fix real defects (P1) and adopt polish (P2), scoped to CSS/JS only, zero copy changes.
DONE (Sonnet agent ac5e805c45fe66666, Fable-authored brief): P1-1 zoom-through transition was silently broken — motion.js's scrollIntoView used behavior:"auto" while styles.css sets global scroll-behavior:smooth, so the intended instant cut-jump was actually smooth-scrolling under the opacity blackout; fixed to behavior:"instant". P1-2 primary CTA contrast was 2.90:1 (cream-on-orange, fails AA); the agent's own fix (.btn.primary color->ink, 5.73:1) plus a self-caught second bug: .nav .links .btn had higher specificity and was silently re-breaking the same fix on the header CTA specifically — both fixed. P1-3 zero :active press-feedback existed anywhere (Apple's #1 rule: respond on pointer-down); added to .btn/.tlink/.nav links. P1-4 prefers-reduced-transparency and prefers-contrast were unhandled on the translucent nav (distinct from the already-handled prefers-reduced-motion); both added. P2: nav zoom double-fire guard (module-level flag in motion.js), nav backdrop-filter now transitions alongside background (materializes rather than paint-swaps) + saturate(180%), scroll-edge gradient fade added alongside the existing hard divider, body font px->rem for Dynamic-Type respect, tlink/nav-link hit areas padded (gap reduced 28->20px to compensate, net-neutral spacing).
VERIFIED (independently, not just the subagent's report): tag balance clean, brace count matches (164/164), index.html git diff empty (zero copy drift), CTA counts unchanged (3+1), zero em dashes, zero banned vocab, recomputed ink-on-accent-hover contrast myself (4.63:1, still AA-pass — agent didn't explicitly check this one), confirmed all 9 fixes live on the running :8080 preview via curl (not just source grep).
NEXT: P3 (optional, not executed): make the hero cone pointer-draggable with real spring physics (1:1 tracking, velocity handoff, critically-damped return, slight bounce only on flick) per the apple-design skill's core thesis — offered as a "showpiece" anti-slop signal, operator has not requested it yet.
GOTCHAS: BUILD_NOTES.md entries are not in strict chronological/numeric file order (S-084/085 pivot entries sit after S-093-095 in file position) — this session appended at true EOF using the next unused number rather than trying to reconcile ordering; not this session's concern to fix.

## S-097 / 2026-07-07 / Mobile review: fixed a real horizontal-overflow bug (Fable, no subagent)
GOAL: Operator asked "do we have a mobile version?" — checked the actual responsive CSS (breakpoints exist at 720/860px, correct viewport meta) then verified live at a 375px mobile viewport via the browser pane rather than trusting source alone.
DONE: Found and root-caused a real bug: `.statstamps` (the four k=5/Wilson/n>=30/never-mix stat cards in #method) used `grid-template-columns: repeat(2, 1fr)` with no mobile override. CSS Grid's `1fr` track does not shrink below its content's min-content width by default, so the longest stamp text ("Measured and simulated data never mix") forced the grid — and therefore #method, and therefore the whole page via viewport auto-expansion — wider than the 375px screen; this visibly clipped the fixed nav's CTA button off-screen. Root-caused via DOM measurement (per-section scrollWidth comparison) rather than guessing from the screenshot, since screenshots in this sandbox are unreliable after any scroll/resize (compositor desync, not a site bug — confirmed via get_page_text/computed-style cross-checks matching expected content exactly every time regardless of screenshot state). Fixed with the standard one-line correction: `repeat(2, minmax(0, 1fr))`. Checked `.steps` and `.bento` for the same footgun — both already have correct mobile handling (`.steps` intentionally horizontal-scrolls via scroll-snap below 860px; `.bento` collapses to 1 column below 860px) — `.statstamps` was the sole offender.
VERIFIED: post-fix `document.documentElement.scrollWidth === window.innerWidth` (375/375, overflow:false) and `#method`'s own scrollWidth === clientWidth (no internal overflow) at a clean measurement; nav CTA button fits within viewport on a repeat check.
NEXT: Product/design decision for the operator, not fixed this session: on mobile (<860px), all in-page nav links (Studies/Evidence/Method/Pilot/Investors) are simply `display:none` with NO replacement (no hamburger menu, no mobile drawer) — mobile visitors can only reach sections by manual scroll or the hero's two CTAs. Site still fully functions (nothing broken), just less navigable than desktop. Flagged for the operator to decide: add a mobile menu, or accept scroll-only nav as sufficient for a single-page brand site.
GOTCHAS: Screenshot capture in the browser-pane sandbox degrades to blank/stale after the first scroll or resize event on a tab (confirmed repeatedly, cross-tab); `resize_window` viewport readback also drifted across separate javascript_tool calls (375→453→465 on the same tab without any code change) even though the underlying DOM/CSS state was correct and stable — treat single screenshots as unreliable here and always cross-verify overflow/layout bugs via `scrollWidth`/`getBoundingClientRect` rather than pixels.

## S-098 / 2026-07-07 / Mobile hamburger menu (Sonnet subagent, Fable supervised, real bug caught+fixed)
GOAL: Operator: "call the apple design skill, learn how to design this element, and add the mobile hamburger" — closing the S-097 gap (mobile nav links had no way to be reached). Design translated from .agents/skills/apple-design/SKILL.md: translucent Stage-material panel (matches nav.scrolled language), scrim for "dim to focus," transform-origin anchored to the trigger (top-right), classic 3-bar-to-X morph, CTA always visible outside the collapsible panel, 44px min tap target, reduced-motion/transparency/contrast fallbacks extending the existing nav.scrolled pattern, Escape/scrim-click/re-tap all close (never trap the user).
DONE (Sonnet agent a7191a727ea161677): restructured nav markup (#nav-menu wraps the 5 section links only, CTA stays a direct sibling), hamburger button + bar-morph CSS, scrim as a true sibling of #nav for the `.nav.menu-open ~ .nav-scrim` selector, motion.js: toggle/aria-expanded/focus-first-link-on-open/Escape/scrim-click/resize-to-desktop-safeguard, integrated with the existing zoom-through handler via a separate non-conflicting listener (verified: link click closes menu AND still fires the zoom transition, exactly once). Agent's own testing found and fixed a real bug mid-task (nav bar too cramped at 375px once the 44px hamburger was added) with mobile-scoped padding/sizing reductions.
DONE (Fable independent verification caught what the agent's own report missed): re-tested overflow myself post-agent and found `document.documentElement.scrollWidth(526) > clientWidth(375)` with the menu CLOSED — contradicting the agent's "scrollWidth===clientWidth===375" claim. Root-caused via getBoundingClientRect scan + computed-style inspection: `#nav` (position:fixed;left:0;right:0) was computing to width:455px instead of 375px — a flexbox intrinsic-sizing footgun (the `<nav class="links">` flex item's default `min-width:auto` was resisting shrink below its content's natural size), the SAME class of bug as the `.statstamps` CSS Grid overflow from S-097, just flexbox's version. Empirically isolated via live style injection before touching the file (tested `min-width:0` on candidate selectors, confirmed `.nav .links{min-width:0}` alone was necessary+sufficient) then applied the single-line fix directly to styles.css. Re-verified from a clean file reload (not the live injection): zero overflow with menu closed AND open.
VERIFIED (all via live DOM measurement + one screenshot for ground truth, not just trusting either report): desktop 1280px pixel-parity (hamburger display:none, 5 links inline, zero overflow); mobile 375px hamburger exactly 44x44; menu open -> aria-expanded true, focus on first link ("Studies"), zero overflow in the open state too; Escape closes + returns focus to hamburger; scrim click closes; link click closes menu AND fires the existing zoom-through (main.zoom-out class observed); screenshot ground-truth showed the panel/scrim/X-morph all rendering correctly even while getComputedStyle().opacity read a stale "0" for scrim/nav-menu (a sandbox JS-introspection lag, not a real bug -- confirmed by the visual capture showing full opacity, consistent with the viewport-drift and stale-screenshot quirks logged in S-046/S-097). Zero copy changes (all 5 link labels + CTA text byte-identical via grep), zero new hex colors beyond the two pre-approved rgba values, zero em dashes in copy (4 pre-existing em dashes are in file-header code comments, not site copy), tag balance and brace balance both clean.
NEXT: Nothing pending on the mobile nav. Remaining known gap from S-097 (deck errata per playbook §10.1) still open, unrelated to this work.
GOTCHAS: This sandbox's JS DOM introspection (getComputedStyle, window.innerWidth) can desync from the true rendered page after repeated interaction on a long-lived tab -- confirmed again this session (opacity read "0" while a screenshot proved it was genuinely 1). Rule of thumb going forward: scrollWidth/clientWidth comparisons stayed reliable throughout every test; individual computed-style property reads and innerWidth did not -- prefer the former, and fall back to an actual screenshot as ground truth when the two disagree, rather than trusting either report (subagent or raw JS) alone.

## S-099 / 2026-07-13 / M43 activation
GOAL: Isolate and activate the authenticated Resonance web UI refinement milestone without touching the operator's dirty agent/brand work.
DONE: D-112 governance was committed once on `geo-agent-v1` (`620148c`) with docs:check green; branch `m43` was created from that commit in `/private/tmp/Parallax-m43`; the original worktree's modified/untracked file list remained unchanged. Added and committed (`173619b`) the single active 530-line `M43_BUILD_PLAN.md`, branch-local PRD/STATUS/handoff, and byte-identical approved Apple skill. Installed 708 packages from the local pnpm cache with the lockfile frozen. Baseline `pnpm test:e2e`: main journey, projects/hub axe, and mobile drawer passed (3/4).
UNVERIFIED: Baseline M34A journey test failed only at `operator-journey.spec.ts:110`: the assertion maps `['durability', 'SINGLE OBSERVED INSTANCE']` to two option elements, but the control correctly renders a placeholder followed by one option containing both fragments. The complete M34A flow reached the Simulation selector; fix the brittle test assertion in Phase 6 and re-run. Phase 2 shared UI and in-app browser demo have not run yet.
NEXT: Start `PLAYWRIGHT_PORT=3143 pnpm exec tsx scripts/playwright-webserver.ts`, open `/projects` in the in-app browser, and begin Phase 2 primitives/shell work.
GOTCHAS: M43 owns presentation only. A discovered need for a migration, API/action-payload change, agent-path change, measurement change, or methodology change is a stop line, not adjacent cleanup. Next.js warns that `/private/tmp/package-lock.json` can confuse output-tracing root inference; do not delete or modify that unrelated external file.

## S-100 / 2026-07-13 / M43 Phase 2
GOAL: Start the safe live demo and bring shared operator primitives, loading/error states, overlays, tabs, shell, focus, motion, contrast, and dirty navigation to a phase-green foundation.
DONE: Port-3143 harness ran with disposable seeded Postgres, demo-only disabled auth, no worker/provider/spend; dedicated visible in-app tab stayed on `/projects`, `/login`, project hub, and Setup as work landed. Commits: `f879a07` shared foundations/shell, `8bbd95a` mechanical operator-action contrast, `3c3596a` separately isolated hub wayfinding. Added size-stable `Button.pending`, `InlineStatus`, contextual static `PageLoading`, `AppConfirmDialog`, origin-consistent menu/dialog/drawer motion, 44px narrow controls, single-line scrollable tabs, dirty-tab/sidebar confirmation, focus restoration, pending sign-out, recovery-heading focus, and strict contrast fixes. Interactive: 1280 desktop sidebar/no overflow; 390 drawer trap/Escape/trigger restoration; row menu Escape/restoration; Setup tab rail 335px viewport/566px content with no page overflow; dirty local-tab dialog blocked navigation then discarded correctly; login ink dossier labeled and focused. Automated: `pnpm typecheck` green; focused ESLint green; `pnpm test` 782 passed/12 skipped; `pnpm test:e2e` 6/6 including unsuppressed axe on projects/hub, mobile restoration, dirty tabs, M34A handoff, and reduced-motion transform removal.
UNVERIFIED: Full-repository `pnpm lint --max-warnings 0` still reports the pre-existing agent-owned unused `CATEGORY_ARCHETYPES` import in `src/core/semantic.test.ts`; M43 did not cross the agent-core ownership boundary to silence it. The final 1440/768/200%-zoom sweep and every route group remain Phase 7 work.
NEXT: Continue Phase 3 on `/projects`, intake, and Setup. First fix the observed 390px project-table page overflow with a labeled internal scroll region and add result-count/filter-empty semantics.
GOTCHAS: The live demo and Vitest/Playwright harnesses all use embedded Postgres port 5433, so pause the demo for automated suites, keep the in-app tab open, then restart port 3143 and reconnect the same URL. Browser console retains historical dev-overlay entries even after recovery; current routes recompiled and reloaded cleanly.

## S-101 / 2026-07-13 / M43 Phase 3
GOAL: Refine the projects library, all eight intake steps, project hub, and every Setup view with assumption-free responsive, state, focus, and handoff behavior.
DONE: Commits `afd6db4` (isolated shared form/touch accessibility) and `fa3e70d` (projects/intake/Setup) complete Phase 3, following the earlier isolated hub commit `3c3596a`. `/projects` now reports filtered counts, distinguishes no-match from an empty library, keeps Resume/Open permanently visible, gives row menus 44px triggers, and contains its semantic table in a labeled local scroll region. Intake now has a URL-backed responsive progress rail with current/completed/future semantics, exact 800ms autosave feedback (`SAVING…`, `SAVED HH:MM:SS`, `SAVE FAILED — RETRY`), retry without input loss, first-invalid-field focus, size-stable pending actions, narrow stacking, logical focus after tag add/remove, 44px tag removal, and sentence-case review warnings. Setup now provides action-keyed pending/success/warning/error feedback without dimming the whole section, preserves input on failure, focuses the first field on edit entry, prevents competing edit contexts, restores persisted values on Cancel, keeps archive/unarchive reversible without confirmation, and names an attribute in the permanent-removal confirmation. Interactive evidence: `/projects` at 390 had app body 375/375 with table region 341/704; intake at 390 had app body 375/375 and progress rail 343/987; empty validation focused Project/client name with three `aria-invalid` controls; autosave created a resumable id and displayed `SAVED 20:13:47`; Setup edit focused `SpendPilot name`, disabled other edit contexts, Cancel restored `SpendPilot`, and the removal dialog named `easy implementation`; Setup at 1280 retained the 248px sidebar, a 977/977 tab rail, and no app-body overflow. Automated: focused ESLint and `pnpm typecheck` green; `pnpm test:e2e` 9/9 green, including unsuppressed axe, narrow table containment, intake validation/autosave/resume, Setup cancel/confirmation, drawer/tabs/reduced motion, and the full seeded M34A journey. Full Vitest reached 781 passed/12 skipped with one agent-owned budget test losing its queued job to full-suite interference; the unchanged test passed 10/10 immediately in isolation. No route, API/action payload, schema, metric, methodology, cost, export, agent, or `site/**` change entered the diff.
NEXT: Start Phase 4 on `/projects/[id]/matrix?view=overview`, then Presence/Position/Perception and version states before moving through run list, configuration, progress, events, extraction, and metrics. Keep the port-3143 tab on the active surface.
GOTCHAS: The development-only Next.js portal can add a root scrollbar in screenshots even when the application body is exactly contained; verification therefore records body containment and the labeled table region separately. The full-suite budget flake is in the agent-owned runner test and is not a Phase 3 UI regression; do not change agent core to hide it.

## S-102 / 2026-07-13 / M43 Phase 4
GOAL: Refine every Prompt Matrix and run-library/configuration/operation state without changing prompt, projection, run, extraction, or metric behavior.
DONE: Commits `52f292a` (isolated pending-button accessible-name fix) and `7851ae0` (matrix/runs) complete Phase 4. Matrix now has one page heading, persistent version/state and used/remaining capacity, action-specific pending/error feedback, named confirmations for immutable approval and removal, focused single-cell editing with Cancel restore, dirty-navigation protection, and no edit affordance on approved versions. Run configuration now leads with mode, exposes semantic provider/generation controls, exact `LOADING`/`READY`/`UNAVAILABLE`/`OVER CAP` projection states, exact credential resolution, stable pending layout, an accessible advanced-mock disclosure, and duplicate-start prevention. Run operation now keeps last-known data during polling failure, offers retry, suppresses replayed recovery motion, gives each action independent pending feedback, confirms cancellation consequences, exposes progressbar semantics, announces terminal completion once, wraps event/error content, contains extraction/metric tables, and presents worker/pause warnings as readable inline status. Interactive: the matrix showed one heading and `40 used · 10 remaining`, approval named immutability, edit focused `Edit v1 prompt`, dirty tab navigation opened the shared discard dialog, and removal named `v1 · VP Finance · United States`; run configuration showed `READY`, then `OVER CAP` with Start disabled, and live-validation showed exact missing-provider guidance plus `UNAVAILABLE`; at 390px the app stayed 390/390 and primary controls were at least 44px; the safe mock run stayed queued with `WORKER OFFLINE`, exposed a semantic 25-job progressbar, required cancellation confirmation, then announced `Run cancelled.` with terminal `cancelled`/`PARTIAL` state. Automated: `git diff --check`, focused ESLint, and `pnpm typecheck` green; `pnpm test:e2e` 11/11 green; focused UI/matrix/run Vitest 7/7 green. No worker/provider call, live spend, API/action payload, schema, metric, methodology, cost, export, agent, or `site/**` change entered the diff.
NEXT: Start Phase 5 on `/projects/[id]/dashboard?view=presence`, refine run switching and every audit pillar before the evidence edge sheet, Simulation summary, report builder, and print view.
GOTCHAS: The demo database intentionally runs without a worker, so queued and cancelled states are safe visual fixtures rather than live processing. The seeded completed run reports 25 planned jobs but 5 succeeded; M43 preserves that frozen fixture/behavior and does not reinterpret it in presentation code.

## S-103 / 2026-07-13 / M43 Phase 5
GOAL: Refine every audit dashboard pillar, the evidence edge sheet, Simulation summary, report builder, exports, and print preview without changing evidence or report payloads.
DONE: Commits `67a5147` (isolated shared evidence accessibility), `9ab036f` (dashboard/evidence plus opt-in disposable visual fixture), `4b4af68` (report/print), and `52139d9` (operator-journey coverage) complete Phase 5. The dashboard now preserves and labels last-valid evidence while run switching or retrying, retains value/n/CI/limitation/evidence affordances, replaces convincing small-n sentiment graphics with explicit limitations, exposes semantic table equivalents for visual evidence, and reaches raw responses in two clicks through a focus-trapped edge sheet with reliable trigger restoration. Misinformation correction keeps input after failure and reports row-specific pending/error state. Report switching names date, `AUDIT`/`SIM`, mode, and state; dirty edits use shared navigation protection; Save affects one section; regeneration names replacement consequences; keyboard export restores focus; and the full-viewport paper preview uses only established typography while preserving the existing payload. The port-3143 harness alone opts into a completed $0 mock audit (200 responses, 354 recomputed existing metrics), with no worker/provider/spend and no ordinary-test seed change. Interactive: at 1280 the Proof route showed Citation Share 40.0% (n=70), Accuracy 95.5% (n=200, CI 92–98%), two source domains, and nine review rows; evidence list/detail/back/close restored the invoking button; at 390 the dashboard, edge sheet, and print document stayed within 390px; report edit focused the textarea, dirty navigation confirmed, save retained `edited`, regeneration confirmed replacement, and Escape from Export restored its trigger. Automated: `git diff --check`, focused ESLint, and `pnpm typecheck` green; `pnpm test` 782 passed/12 skipped; `pnpm test:e2e` 13/13 green with unsuppressed axe across Proof and report (axe first caught low-contrast metadata, which was corrected rather than suppressed). No route, API/action payload, schema, metric, methodology, cost, export payload, agent, or `site/**` change entered the diff.
NEXT: Begin Phase 6 at `/projects/[id]/resonance`, then the study workspace/results/evidence and the complete Framing Evidence workflow. Keep the live browser on the active surface and verify C-12/C-13/C-15 separation and immutability.
GOTCHAS: The rich Phase 5 fixture is gated by `M43_UI_FIXTURES=true` inside the disposable Playwright server only. The in-app demo remains live on port 3143; pause it before any test command that starts embedded Postgres on port 5433.

## S-104 / 2026-07-13 / M43 Phase 6
GOAL: Refine the complete Simulation and Framing Evidence journeys while preserving their frozen M34A contracts, epistemic separation, immutable boundaries, and conservative reporting.
DONE: Commits `4e6e3f0` (isolated shared warning-stamp contrast), `5bf0d61` (Simulation), `d3d5553` (Framing Evidence), and `5bffce2` (journey coverage) complete Phase 6. Simulation creation now preserves failed input, exposes selected blank/template modes, prevents duplicate submission, and restores focus; the design wizard saves study-owned fields before advancement, focuses the first blocker, protects dirty state, reports item-specific framing work, names delete/approval consequences, and removes every edit affordance after approval. The disposable M43-only fixture adds an approved B2B study whose measured baseline references an actual completed audit response and whose two mock variants each have 30 stored responses; it uses the existing recomputation path with no worker, provider call, scoring change, or spend. Results keep engines URL-backed and separate, expose n, PMF, baseline, deltas, segments, deterministic excerpts, methodology, and epistemic stamps; evidence filters preserve each other, reset pagination, and retain `SIMULATED` on every item. Framing Evidence now exposes the full blind packet, row/stage-specific dirty and pending state, named confirmations for codebook lock, denominator completion, and immutable handoff, logical add/remove focus, Escape restoration, valid no-actionable-gap handling, live-audit-only handoff eligibility, and full-verbatim language that never calls a response representative. The paper report adds responsive screen chrome and accessible export controls without changing its export payload. Interactive: the Simulation library showed the approved `M43 positioning clarity study`; Results showed `SIMULATED`, `MODEL-IMPLIED`, `UNCALIBRATED`, `COMPARATIVE`, mock provenance, 30 draws per variant, and separated rankings; Evidence showed 60 responses with preserved filters and 25/page; Framing discovery showed all five blind items and all three masking instructions. Automated: focused ESLint, `git diff --check`, and `pnpm typecheck` green; `pnpm test` 782 passed/12 skipped; `pnpm test:e2e` 14/14 green with unsuppressed axe, responsive containment, focus restoration, immutable-boundary confirmations, C-12/C-13/C-15 coverage, and B2B/historical regressions. Axe found the established warning stamp at 4.42:1 on paper-2, so the shared semantic warning treatment was corrected to ink rather than suppressed. No route, API/action payload, schema, metric, methodology, cost, export payload, agent, or `site/**` change entered the diff.
NEXT: Review Settings and debug, reconcile the latest phase-green `geo-agent-v1`, run the complete gate list plus final 1440/1280/768/390 and 200%-zoom browser walkthrough, record compact evidence, and stop the disposable demo at handoff.
GOTCHAS: The completed Simulation fixture remains gated by `M43_UI_FIXTURES=true` in the port-3143 harness; ordinary tests do not seed it. The demo is currently stopped so its embedded Postgres cannot contend with Phase 7 verification.

## S-105 / 2026-07-14 / M43 Phase 7 verification handoff
GOAL: Close Settings/debug, reconcile the phase boundary, run every automated and interactive gate, prove the final responsive/zoom behavior, and leave a merge-ready assumption-free handoff.
DONE: Commits `7b930e4` (isolated Settings), `1852fd7` (isolated debug console), `5fa6708` (disposable fixtures and browser coverage), and `fcd6100` (worktree-safe standalone tracing) complete all M43-owned Phase 7 work. Settings now keeps provider secrets write-only, focuses invalid input without clearing it, uses row-specific pending/error/success state, names permanent deletion, restores the row trigger, contains its semantic table, and presents read-only defaults cleanly at 390px. Debug now exposes stale heartbeat, safe repair success and rejection, local scroll regions, wrapped events/errors, visible ink-surface actions, and unsuppressed accessible contrast. The disposable harness overrides any inherited credential key with fixed public test material, adds no-worker mock debug rows, and never verifies a provider or spends. `geo-agent-v1` remained exactly `620148c`, the M43 anchor, so the required phase-boundary reconciliation was a verified no-op with no collision files. Scope audit: 84 changed files and no `site/**`, migration, provider, worker, agent-core, dependency, or lockfile change. Automated green: `pnpm docs:check`; `pnpm typecheck`; focused M43 ESLint; `pnpm test` 105 files / 782 passed / 12 expected skipped; `pnpm test:golden` 36/36; isolated `pnpm test:mock-e2e` completed 500 jobs in 23.6s with eight stale locks reclaimed, 56 retries, and 499 unique responses plus one expected dead letter; `pnpm test:e2e` 16/16 with unsuppressed axe; and `pnpm build` including standalone packaging. Interactive final: `/projects` at 1440×900 was 1440/1440; Proof at 1280×800 was 1280/1280; Simulation deltas at 768×1024 was 768/768; debug at 390×844 was 390/390 with 356/736 and 356/608 labeled table regions; true 200% browser zoom reached device scale 2 and kept the representative Simulation ranking at 1280/1280. Reduced motion, keyboard, focus restoration, touch targets, and major-route axe coverage are green in the 16-test browser suite. The final visible screen showed the approved `M43 positioning clarity study`, `SIMULATED`, `MODEL-IMPLIED`, `UNCALIBRATED`, `COMPARATIVE`, one engine, n=30 per variant, PMFs, and draw-floor labels. The port-3143 server and disposable database were stopped.
UNVERIFIED / BLOCKED: Full `pnpm lint --max-warnings 0` reaches exactly one pre-existing warning: unused `CATEGORY_ARCHETYPES` at agent-owned `src/core/semantic.test.ts:6`. The build is successful but reports the same non-fatal warning. M43 did not alter the test or suppress the rule because that would violate its explicit agent-core ownership stop line; the milestone therefore remains active rather than claiming the all-green completion gate. The first unqualified `pnpm test:mock-e2e` invocation followed that script's fallback to the local dev database, created/used its dedicated `m4-e2e` fixture, and exposed that database's missing `xai` enum before the worker crashed; no migration or cleanup was applied there. The gate was rerun green against the explicit disposable `parallax_test` database on port 5433.
NEXT: Agent owner removes the unused import on a phase-green `geo-agent-v1` commit. At the next M43 phase boundary, incorporate only that integration commit, rerun full lint and build, then mark M43 complete, archive `M43_BUILD_PLAN.md`, prune M43 session notes, reconcile integration `STATUS.md`, and merge `m43` into `geo-agent-v1`. Do not merge while the completion gate is red.
GOTCHAS: `pnpm test:mock-e2e` does not boot its own database; always set `DATABASE_URL=postgres://postgres:postgres@localhost:5433/parallax_test` while the repository's ephemeral `pnpm test:db` is running. The ordinary Playwright/Vitest harnesses already manage their own disposable databases. The in-app browser tab still points at the final representative route, but its isolated server is intentionally stopped at handoff.

## S-106 / 2026-07-14 / Adopt `main` as integration trunk (Opus, operator "do all 5")
GOAL: Restructure the repo so web (UI) work and agent work advance in parallel and merge through a shared trunk (`main`), per the operator's assessment request. Executed the agreed 5-step plan.
DONE: (1) Removed the unused `CATEGORY_ARCHETYPES` import at `src/core/semantic.test.ts:6` that held `pnpm lint --max-warnings 0` red — the sole blocker on M43's completion gate and, once `main` is trunk, on all CI. Also added `.claude/**` to eslint ignores: Claude Code nests full worktree checkouts under `.claude/worktrees/` (each with its own `.next`), which had flooded local `pnpm lint` with 1474 false positives from nested build artifacts (CI clean-checkout never saw them; a teammate running lint locally would have). Verified: full `pnpm lint --max-warnings 0` and `pnpm typecheck` both green. (2) Committed the S-097/S-098 site mobile-hamburger WIP, and gitignored `.m35-evidence/` (C-11 signer-key template), `.agents/`, `.claude/skills|worktrees`, `skills-lock.json` — confirmed all now `git check-ignore`-matched so no broad add can stage the credentials template. (3) Added D-113 naming `main` the integration trunk (no supersession — instantiates D-112's generic "shared integration branch"); updated STATUS Branch field.
NEXT (this session, in progress): fast-forward `main` to trunk HEAD (pure ff, `main` is ancestor), run `docs:check`, push `main`, add `.github/CODEOWNERS` + branch protection, then reconcile+PR `m43`. `gh` is NOT installed on this machine — PR opening and branch-protection API need `gh` (or the GitHub web UI); flagged to operator.
GOTCHAS: Branch protection modifies repo access-control settings — handed to the operator to apply, not done autonomously. The `.claude/worktrees/strange-carson-2839bc` nested worktree is a local artifact only; it never reaches CI.

## S-112..S-117 / 2026-07-19 / M46: merged to main via PR #7, entries truncated per Rule 2 (D-025)
GOAL/DONE: M46 (D-117) trustworthy progress + Simulation readiness — graduated into `PRD.md` §8.35, `DECISIONS.md` D-117, and `docs/history/M46_BUILD_PLAN.md`.
NEXT: Nothing pending from M46. Current work is M47 (D-118) on `m47`.
GOTCHAS: Archive/prune ran on `m47` P0 because PR #7's merge commit left the D-025 ritual owed (same pattern as M44/M45 → M46 P0).

## S-118 / 2026-07-20 / M47 P0: transition feedback — governance (Fable)
GOAL: Open M47 (D-118): reachable route loading, same-segment pending tabs, redundant refresh removal.
DONE: Branch `m47` cut from `main@ec9f2f8`. Archived `M46_BUILD_PLAN.md` to `docs/history/` (DISPOSITION EXECUTED). Pruned M46 BUILD_NOTES (S-112–S-117). Appended D-118 (no supersession). Created compact `M47_BUILD_PLAN.md` (D-112 tracker). STATUS/PRD §8.36/MASTER_CONTEXT index retargeted to M47.
NEXT: P1–P4 implementation (loading boundaries, pending tabs, refresh allowlist, gates).
GOTCHAS: M43_BUILD_PLAN.md remains root-ACTIVE (archival still deferred). Cold `next dev` compile time is out of M47 scope.

## S-119 / 2026-07-20 / M47 P1–P4: loading, pending controls, refresh cleanup, gates (Fable)
GOAL: Immediate navigation acknowledgment; remove duplicate refreshes; prove with delayed-RSC Playwright + allowlist test.
DONE: `projects/loading.tsx` + `[id]/loading.tsx`; sync `ProjectLayout` Suspense split so layout data awaits are reachable; `LocalViewTabs`/`ReportRunSwitcher` `useTransition` + `InlineStatus`/`aria-busy`; removed 11 `router.refresh()` after `revalidatePath` (kept login + framing-batch terminal); allowlist Vitest; `e2e/transition-feedback.spec.ts` (cookie-gated `e2eNavDelay`); Chrome desktop/390/reduced-motion covered by existing journey specs.
VERIFIED: lint 0-warn; docs:check; typecheck; Vitest 853/0 (12 skipped); Playwright 18/18; `pnpm build` green.
WARM-NAV SAMPLE (`pnpm build` then `next start`, library→workspace click, wall-clock to first loading/`aria-busy`): not separately timed this session — Playwright presence checks prove acknowledgment under stretched RSC; if production warm nav still feels >~500ms after merge, open a measurement follow-up (D-118), do not expand M47.
NEXT: PR `m47` → `main`; archive plan + prune M47 notes in merge commit.
GOTCHAS: Async layouts that await before returning never show a sibling Suspense — must split sync shell + async chrome. Prefetch can hide loading UI in tests; cookie-gated server delay is more reliable than route interception.

## S-121 / 2026-07-30 / M49 local migration-drift repair
GOAL: Repair the local M46/M49 migration drift behind the post-approval “Something went wrong” route without losing approved Marriott provenance or spending queued live runs.
DONE: Stopped the worker and preview; made an offline 145 MB `.pgdata` backup at `/private/tmp/parallax-pgdata-backup-20260730-2337`; added a local-only guarded repair runner that reads and hashes canonical `0021`, skips only the already-satisfied `brand_order_json` statement after exact drift checks, applies the rest transactionally, verifies the M46 end state and byte-preserves all 35 populated approved brand-order rows, then stamps the canonical hash. The populated DB exposed a second blocker in applied-history `0022`: tuple `CASE` literals fail as `text` versus `unknown`; the runner executes the same 15 canonical mappings with explicit text predicates and stamps the unchanged canonical `0022` hash without editing that migration. Normal `pnpm db:migrate` then applied `0023`. Verified canonical hashes for `0021`–`0023`, M46 batch/lock schema, all M49 study columns/check constraint, both Marriott live runs still paused with 175 queued / 0 succeeded / $0 spent, the Marriott project and run-detail routes returning 200, and a clean worker heartbeat. Gates: focused ESLint, `pnpm typecheck`, migration metadata + populated upgrade-path Vitest (9/9), `pnpm docs:check`, and `git diff --check` green.
REJECTED: A lone `0024` repair (unreachable while `0021` failed); rewriting applied `0021`/`0022` (migration-history violation); dropping the orphan `brand_order_json` (would destroy 35 approved frozen values); stamping `0007` (Drizzle 0.44.7 advances from the latest `created_at`, so the historical hash drift is not replayed).
NEXT: Review and commit the M49 branch diff, then deploy through the normal production path; keep both Marriott runs paused until the operator explicitly chooses whether to resume or cancel them.
GOTCHAS: The orphan `brand_order_json` came from an out-of-band `ALTER` (C-6 violation), not a partial Drizzle apply. Empty migration tests did not evaluate `0022`’s tuple `CASE`; the regression now inserts a matching populated comparison-template row. This session deployed only the repaired local preview and worker, not Render production.

## S-122 / 2026-07-31 / Message Lift: long message name action-row containment
GOAL: Fix the Message Lift editor action row overflowing when a New message's editable name is long.
DONE: The shared `Button` keeps its pending label in the layout to prevent loading-state shifts. This card passed `Saving ${label}` as that label, so each typed character widened the resting Save button and could push Delete off-screen. Replaced it with the stable, still descriptive `Saving message` label and allowed the two action controls to wrap as a narrow-viewport safeguard. Added a source contract that prevents reintroducing the name-dependent pending label.
VERIFIED: `pnpm vitest run src/components/ui-contracts.test.ts` (4/4), focused ESLint, `pnpm typecheck`, and `git diff --check` passed. The focused test harness logged its expected sandbox inability to start disposable Postgres; this source-only test passed independently.
NEXT: No follow-up required for this UI bug.
GOTCHAS: Do not make this card's pending text depend on an editable message label unless the `Button` primitive's sizing behavior is deliberately redesigned and verified across its callers.

## S-123 / 2026-07-31 / Provider proxy transport + Marriott Message Lift recovery
GOAL: Restore results for completed Marriott Message Lift run `cffd5856` after all 60 SSR extractions dead-lettered with `OpenAI embeddings request failed: fetch failed`, and prevent the same local-network failure on future runs.
DONE: Isolated the host-network cause: macOS routes HTTPS through `127.0.0.1:7890`, while Node fetch ignored that system setting and direct DNS for `api.openai.com` resolved to unrelated addresses. Verified the local proxy itself reaches OpenAI (expected unauthenticated HTTP 401). Added Undici `EnvHttpProxyAgent` support to the shared provider transport, activated only when standard `HTTP(S)_PROXY` variables are configured and only for provider requests; direct production behavior remains unchanged without those variables. Documented the optional env contract and configured the gitignored local `.env.local` for port 7890. A live authenticated `text-embedding-3-small` probe succeeded (1 vector, 1536 dimensions, 5 tokens). Re-scored the existing 60 immutable provider responses as extraction version 2, preserving the dead-lettered version-1 history, then rebuilt 15 metrics without repeating generation calls. Restarted the local worker with the repaired transport.
VERIFIED: Latest extraction state is valid for 60/60 responses; run cost moved from $0.019809 to $0.019938 (only $0.000129 additional embedding cost); Results now renders DeepSeek scores (Current 3.45, New 3.41) and lift -0.04 at n=30 / Enough samples. Focused proxy + live-adapter Vitest 16/16, focused ESLint, `pnpm typecheck`, and `git diff --check` passed. Local worker heartbeat resumed after restart.
NEXT: Commit the M49 branch changes and deploy through the normal production path. Render needs no proxy variables unless its own network requires them.
GOTCHAS: Node workers do not inherit macOS System Settings proxy configuration. Set `HTTPS_PROXY`/`HTTP_PROXY` explicitly for local provider traffic; do not replace the official provider base URL with a local CONNECT proxy, and do not silently fall back to a different scoring method.

## S-124 / 2026-07-31 / M49 consolidation and release gates
GOAL: Consolidate the complete M49 Message Lift milestone into one reviewable branch while preserving unrelated local work, then prove it is ready for a pull request.
DONE: Audited every tracked and untracked change against D-119 and the M49 plan; retained the Message Lift schema, protocols, deterministic recommendation pipeline, results/report/evidence surfaces, compatibility coverage, guarded local migration-drift repair, provider proxy transport, and long-name action-row fix. Kept the unrelated venture-builder fact sheet, generated outputs, M46 debug helper, and local Vercel state outside the milestone. Updated the control plane with the authoritative acceptance counts.
VERIFIED: `pnpm lint`, `pnpm typecheck`, `pnpm docs:check` (22 governed docs), `pnpm test` (117 files, 869 passed / 12 skipped), `pnpm build`, and `pnpm test:e2e` (18/18) all pass. The Playwright suite covers both M49 result semantics and the exact-prompt/fixed-settings buyer-response path.
NEXT: Review and merge the M49 pull request into `main`; archive `M49_BUILD_PLAN.md` and prune M49 session notes in the merge ritual, not on this feature branch.
GOTCHAS: The two Marriott live-audit runs remain intentionally paused. This consolidation does not authorize resuming provider spend or deploying Render production.
