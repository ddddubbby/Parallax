# MASTER_CONTEXT.md - Parallax

> The single canonical context file for this repository. Every AI coding session and every human contributor starts here. It contains project identity, non-negotiable constraints, durable decisions, and session rituals. Product scope lives in `PRD.md`; implementation rules live in `DEVELOPMENT_GUIDELINES.md`.

---

## 0. How to use this file

1. Read this file fully at session start. Then read the document relevant to the task.
2. Docs beat chat memory. If a conversation contradicts these documents, the documents win until the change is explicitly agreed and written into the Decision Log.
3. One fact, one home. Identity, constraints, decisions, and rituals live here. What to build lives in `PRD.md`. How to build lives in `DEVELOPMENT_GUIDELINES.md`.

---

## 1. What Parallax is

Parallax is an internal, operator-facing web tool that audits how AI systems describe, rank, recommend, cite, and misrepresent brands. It runs a curated prompt matrix across LLM provider APIs with repeated sampling, extracts structured metrics from stored raw answers, verifies AI claims against a client fact sheet, and generates an editable audit report.

It is used by one operator, usually a consultant, to produce paid client audits. It is not a SaaS product: no client logins, no billing, no marketing pages, and no white-label theming in MVP. The end client only receives exported reports and evidence packs.

## 2. Why it exists

AI assistants have become decision intermediaries between brands and buyers. Consultants sell "how does AI see your brand" audits at roughly $3k-$15k. Parallax's differentiator is statistical honesty: LLM outputs are probabilistic, so audit prompts are sampled repeatedly and metrics ship with confidence intervals plus stability signals. Never build features or write copy that promise guaranteed AI rankings. The product measures distributions, verifies claims, and preserves evidence.

## 3. Methodology in one screen

- A prompt cell is one resolved prompt: intent x persona x market x phrasing variant.
- Each audit-grade cell runs k=5 repetitions per selected engine-mode. Validation mini-runs may use k=2 and must be labeled validation-only.
- Metrics are rates or averages over samples and are reported with Wilson confidence intervals where applicable.
- Mock is provider #0 and remains permanently registered for tests, demos, and failure injection.
- The first live dry-run provider is DeepSeek, because its official API is OpenAI/Anthropic-compatible and currently offers low-cost text generation. MiniMax is the second candidate provider after account/API-key details are confirmed.
- The long-term target provider set remains OpenAI, Anthropic, Gemini, and Perplexity, added later through the same provider interface.
- Grounded means the provider/API path supplies web-grounded output with normalized citations. If a provider cannot supply citations, its runs are ungrounded live validation runs and must not be mixed into grounded aggregates.
- Metrics: Mention Rate, Recommendation Rate, Share of Voice, Avg First Position, sentiment, attribute-association matrix, citation share, accuracy rate, and Stability Index.
- Checkable claims about the client brand are matched against the client fact sheet and reviewed in the misinformation register.
- Aggregate findings and metric claims render only where n >= 30 eligible samples. Cell-level findings (for example lost-shortlist) are exempt from the threshold but always carry a directional-only label, as do low-stability clusters. Eligible samples are defined in `DEVELOPMENT_GUIDELINES.md` E2.

## 4. Hard constraints - never violate

| ID | Constraint |
|---|---|
| C-1 | A run processes at most 50 prompt cells. Enforced at matrix approval, run creation, and in the worker. Default audit matrix: 40 cells, bottom-funnel weighted. k=5 is protected for audit-grade runs; cut coverage before repetitions. |
| C-2 | Three nested cost guards: C-1 structural cap, per-run dollar cap, and per-provider daily budgets. The guards cover all paid LLM calls — generation and extraction alike (D-022). |
| C-3 | Raw responses are immutable. Re-extraction creates new versioned extraction rows. Every reported number traces to stored raw text. |
| C-4 | Approved matrices are frozen. Edits create a new matrix version. Runs reference exactly what was sent. |
| C-5 | Metrics are disposable: pure functions over extractions, recomputable idempotently. |
| C-6 | Schema changes happen only via migration files in git. Database dashboards are read-only for inspection. |
| C-7 | Modules communicate only through the database and typed interfaces. The UI never imports providers. `/src/core` imports nothing from other project layers. |
| C-8 | Never scrape consumer chat UIs. Official APIs and SERP/API vendors only. Permanent rule, not a deferral. |
| C-9 | Mock runs are first-class but always flagged (`run_mode: mock`, MOCK badge) and never mixed into live aggregates. |
| C-10 | Provider grounding capability is explicit. Grounded runs are blocked for providers that cannot return citations through an approved API path. |
| C-11 | LLM provider API keys are never stored in source files, `.env.example`, `render.yaml`, fixtures, logs, or client-visible payloads. The operator enters them in the authenticated Settings UI; the server encrypts them at rest and only the server/worker can decrypt them. |

## 5. Stack snapshot

Hosting target: Render-only, with one `render.yaml` defining a Next.js web service, a background worker, and Postgres. Sanctioned fallback if DB cost matters: Render compute plus Supabase free Postgres.

Stack: Next.js 15 + TypeScript + Tailwind + shadcn/ui, Drizzle ORM, Zod, Vitest, pino, `run_events`, and Sentry.

| Command | Does |
|---|---|
| `pnpm dev` | Run the Next.js app locally |
| `pnpm worker` | Run the polling worker locally |
| `pnpm test` | Run Vitest, including golden dataset tests |
| `pnpm db:migrate` | Apply Drizzle migrations |
| `pnpm db:studio` | Inspect data with Drizzle Studio |

This table is a snapshot of daily-driver commands. The canonical, complete command list — lint, typecheck, seed, and per-milestone acceptance commands — lives in `DEVELOPMENT_GUIDELINES.md` section F.

## 6. Repo map

```
/src/app                 Next.js routes and pages, UI-only entrypoint
/src/components          Presentational components, no fetching/business logic
/src/core                Domain types, Zod schemas, constants, pure math/rules
/src/modules/intake      Project intake and autosave orchestration
/src/modules/matrix      Prompt template rendering, allocation, approval
/src/modules/runner      Run creation, job planning, cost guards
/src/modules/extraction  Structured extraction and claim matching
/src/modules/analysis    Metrics, findings, stability calculations
/src/modules/report      Report section generation and export helpers
/src/providers           LLMProvider interface, mock, live provider adapters
/src/db                  Drizzle schema, migrations, repositories
/src/worker              Polling worker entrypoint
/fixtures                Mock responses and golden expectations
/scripts                 Local verification and seed scripts
```

## 7. Documents index

| File | Owns |
|---|---|
| `MASTER_CONTEXT.md` | Identity, constraints, decisions, rituals |
| `PRD.md` | Product scope, user journey, requirements, milestones |
| `DEVELOPMENT_GUIDELINES.md` | Architecture, provider contracts, schemas, tests, workflow |
| `DESIGN_GUIDELINES.md` | Visual language: tokens, typography, surfaces, motion, component rules, visual guardrails |
| `ENGINEERING_SPEC.md` | Detailed schema, lifecycle states, provider matrix, seeds, acceptance commands |
| `RENDER_DEPLOYMENT.md` | Render Blueprint assumptions, secret model, first-deploy checklist |
| `RELEASE_CHECKLIST.md` | Go-live gates and the per-audit delivery/archive record (D-043) |
| `BUILD_NOTES.md` | Disposable per-session working memory for agent handoff; truncated at milestone merge |
| `README.md` | Quick orientation and local setup pointer |
| `fixtures/` | Demo project, mock response manifest, golden expectation manifest |

Split a section into a separate file only when it exceeds roughly 300 lines or changes at a clearly different cadence. Record the split in the Decision Log.

## 8. Session rituals

Boot ritual for implementation sessions:

> Read `MASTER_CONTEXT.md`, then the section for milestone M<N> in `PRD.md`, then `DEVELOPMENT_GUIDELINES.md` section A, then the active milestone's entries in `BUILD_NOTES.md`. For UI-facing milestones (M2, M3, M6, M7), also read `DESIGN_GUIDELINES.md`. Summarize the plan in <=10 bullets and list expected files to touch. Wait for confirmation before editing.

Handoff ritual:

> Append a session entry to `BUILD_NOTES.md` (template inside that file). Append decisions made this session to `MASTER_CONTEXT.md` section 9. Append a 3-line progress note to the current milestone in `PRD.md`. Give the commit message. If stopping mid-task or blocked, write the `BUILD_NOTES.md` entry immediately, even without the rest of the ritual.

Session rules: one active session at a time, one branch per milestone, plan before any multi-file edit, and any schema-change plan must say the word migration.

## 9. Decision Log

| ID | Date | Decision | Why | Alternatives rejected |
|---|---|---|---|---|
| D-001 | 2026-07 | Single TypeScript codebase, two entrypoints: app and worker | One mental model; shared Zod/Drizzle types | FastAPI/Python backend |
| D-002 | 2026-07 | Mock-first at exactly one boundary: the `LLMProvider` interface | Zero-cost E2E validation; mock remains permanent test/demo mode | Mocking DB; live-first development |
| D-003 | 2026-07 | 50-cell cap per run; k=5 protected for audit-grade runs; default 40 cells bottom-funnel weighted | Bounded cost and runtime; repetitions carry statistical validity | Cutting k; uncapped matrices |
| D-004 | 2026-07 | Render-only hosting for app, worker, and Postgres | One vendor/dashboard/bill | Vercel + Railway + Supabase as default |
| D-005 | 2026-07 | Consolidate the original plan into three canonical docs | Solo builder; fewer files stay maintained | Seven-file doc set |
| D-006 | 2026-07 | Adopt the four Karpathy coding principles as project law | Reduces plausible-but-wrong AI implementation drift | Ad-hoc style guidance |
| D-007 | 2026-07-02 | First live validation provider is DeepSeek; MiniMax is the second candidate; Western audit providers come later | DeepSeek and MiniMax expose compatible API paths and are cheaper for dry-run validation | Starting live work with four expensive providers |
| D-008 | 2026-07-02 | Grounded capability is provider-declared and enforced by run planning | Avoids mixing cheap ungrounded validation with grounded audit claims | Treating all provider output as equivalent |
| D-009 | 2026-07-02 | Add an M0.5 execution-readiness gate before product coding | Engineering teams need schema, states, provider capabilities, seeds, and acceptance commands before implementation | Starting M1 from high-level product docs only |
| D-010 | 2026-07-02 | Split execution contract into `ENGINEERING_SPEC.md` | Detailed schema and provider specs exceed the intended size/cadence of general development guidelines | Keeping all details in `DEVELOPMENT_GUIDELINES.md` |
| D-011 | 2026-07-02 | Malformed-output layering: any usable text a provider returns is stored as an immutable response and the job succeeds; content-level malformedness is handled at the extraction layer (partial-but-extractable sets `malformed: true`, unusable text dead-letters). The `malformed_output` job error is reserved for transport-level failures where nothing is stored | PRD treated malformed output as a retryable job error while guidelines dead-lettered it at extraction; with unique `job_id` on responses, a stored-then-retried job would collide. One layer must own content problems | Retrying jobs on content-level malformedness; relaxing response immutability/uniqueness |
| D-012 | 2026-07-02 | Daily budgets: global `PROVIDER_DAILY_BUDGET_USD` default plus optional `<PROVIDER>_DAILY_BUDGET_USD` overrides | C-2 requires per-provider budgets but only a single env var was specified; global-with-override keeps one knob while satisfying the constraint | One global budget only; mandatory per-provider vars |
| D-013 | 2026-07-02 | No exports table or state machine in MVP; Markdown, print-HTML, and CSV/JSON exports are synchronous downloads | The spec defined export states with no backing table; synchronous rendering is simpler and sufficient at MVP scale | Async export queue with `exports` table |
| D-014 | 2026-07-02 | Eligible sample = stored response whose latest extraction is valid (or QA-reviewed) with `refusal: false`; refusals and dead-letters are excluded from all metric denominators and reported separately | "Eligible samples" appeared in every rate metric but was never defined; metrics are pure functions so the definition is the implementation | Counting refusals in denominators; per-metric ad-hoc eligibility |
| D-015 | 2026-07-02 | The n >= 30 threshold applies to aggregate claims only; cell-level findings (lost-shortlist) render at any n with a mandatory directional-only label | A cell yields only k x engine-modes samples, so required cell-level findings were mathematically unreachable under a blanket n >= 30 rule | Blanket threshold; raising k |
| D-016 | 2026-07-02 | Mock fixture selection is keyed by a stable hash of `(resolved_text, provider_id, rep_index)`, never row UUIDs | UUID-keyed selection is not reproducible across re-seeds or fresh clones, undermining golden dataset guarantees in CI | Seeding by `cell_id` UUID; fixed hard-coded UUIDs in seeds |
| D-017 | 2026-07-02 | Provider credentials are website-entered settings encrypted in Postgres, not provider API-key environment variables | The operator does not want DeepSeek or other LLM API keys exposed through the codebase or deploy config; Settings UI is the intended control point | Provider API keys in `.env.example`, Render env, or hard-coded config |
| D-018 | 2026-07-02 | Add a Render Blueprint skeleton with web, worker, and Postgres but no LLM API keys | First deploy should be reproducible from the repo while keeping provider credentials operator-entered after login | Manual Render setup only; putting provider keys in Render env |
| D-019 | 2026-07-02 | Adopt the "machine-age evidence dossier" visual language in `DESIGN_GUIDELINES.md`: ink/paper dual surface, one signal-orange accent, mono-first typography, dossier metadata framing, silk-smooth motion budgets, and visual guardrails V-1 to V-12 | Operator taste (cyberpunk-refined, Bitcoin-native, visually smooth) codified once so UI sessions do not drift; badge and CI visibility rules get design-level enforcement | Ad-hoc styling per session; neon cyberpunk theme; global dark-only theme |
| D-020 | 2026-07-02 | Provider service config precedence: env vars (`<PROVIDER>_BASE_URL`, `<PROVIDER>_DEFAULT_MODEL`) are defaults; a non-null `base_url`/`default_model` on the provider's `provider_credentials` row overrides them. At most one `active` credential per provider, enforced by a partial unique index | The same fact lived in two homes with no precedence, and `(provider_id, label)` uniqueness allowed several active keys with no selection rule for the runner | DB-only config; multiple active credentials with precedence logic |
| D-021 | 2026-07-02 | Credential crypto and KEK lifecycle: AES-256-GCM with per-row nonce; fingerprint is SHA-256 of the raw key; rows carry `key_version` for future KEK rotation; decrypt failure marks the row `invalid` and prompts re-entry in Settings, never crashes the worker; the Render env group holding `CREDENTIALS_ENCRYPTION_KEY` must never be deleted/recreated — recovery from KEK loss is re-entering keys | Unpinned crypto invites per-session improvisation; a regenerated KEK silently orphans every stored credential | Unversioned ciphertext; crashing on decrypt failure; external KMS in MVP |
| D-022 | 2026-07-02 | The "diagnostic engine" term is retired; the extraction engine is a Settings-configured provider+model resolved through the same provider registry and credential service as generation. Extraction calls are counted in projected run cost, the per-run cap, and daily budgets. Mock runs and CI use fixture-backed extraction, never a live extraction engine | Extraction is a second layer of paid LLM calls that was invisible to every cost guard, and golden tests require deterministic extraction | Uncapped extraction spend; live extraction calls in CI; leaving "diagnostic engine" undefined |
| D-023 | 2026-07-02 | Confidence-interval methods are per metric: Wilson only for per-sample proportions (Mention Rate, Recommendation Rate, Accuracy Rate). Share of Voice, Citation Share, Avg First Position, and Stability Index ship in MVP as point estimates explicitly labeled as having no interval; bootstrap intervals are post-MVP | Wilson is statistically invalid for count ratios and means; shipping wrong intervals contradicts the statistical-honesty differentiator | Wilson everywhere; blocking MVP on bootstrap implementation |
| D-024 | 2026-07-02 | MVP production-hardening set: rate-limited login with constant-time password comparison and session expiry; worker heartbeat with staleness surfaced in Debug; post-audit evidence archive (EX-3 export plus database dump stored off-Render); additive-first migration discipline; `claims_found.reviewed_at` for the evidence chain | The shared password now guards spendable API credentials; Render workers have no health checks; managed-Postgres backup retention is thinner than the C-3 evidence promise | Deferring all hardening past the pilot; retrofitting review timestamps after M1 |
| D-026 | 2026-07-03 | Intake drafts persist as step-keyed raw JSON in `projects.intake_draft_json` (migration 0001); rows normalize into the intake tables in one transaction only when intake completes, replacing prior rows | Normalized tables have NOT NULL and unique constraints that partial mid-step drafts cannot satisfy; a JSON scratch column keeps autosave lenient while completion stays strict | Lenient nullable intake tables; client-side-only draft state; per-step normalized upserts with partial data |
| D-027 | 2026-07-03 | Test-only failure injection lives on `audit_runs.debug_failure_injection_json` (nullable jsonb: `{rate, errorType}`), applied by the worker before calling the provider, never threaded through `GenerationRequest` | MK-5/RN-5/RN-6 need deterministic, DB-visible chaos injection across the app/worker process boundary (C-7); job-level transport errors cannot be produced by mock fixture content (D-011), and injection is a test concern, not a provider concern | A generic `debug_flags` key-value table; injection parameters inside the provider interface; random fixture-driven failure only |
| D-028 | 2026-07-03 | `GenerationRequest` gains an optional `repIndex?: number`, ignored by real provider adapters, read only by MockProvider for D-016 fixture selection | The interface as originally specified in `DEVELOPMENT_GUIDELINES.md` C2 had no way to vary the sample within a cell's k repetitions; without it every rep would deterministically select the same fixture, making repeated sampling meaningless in mock mode and stability index untestable | Smuggling rep index through `promptText`; a mock-only side-channel outside the interface; computing selection in the worker instead of the provider |
| D-029 | 2026-07-03 | `audit_runs.debug_failure_injection_json` becomes `{ generation?: {rate, errorType}, extraction?: {invalidRate} }` — the extraction service applies `extraction` before validating, independently of D-027's `generation` config which the worker applies before calling the provider | SM-2/SM-3 (extraction retry/dead-letter) need the same deterministic, DB-visible chaos injection D-027 gave job-level failures, but extraction validation failures are a different failure class on a different code path than generation transport errors | A second column; overloading D-027's flat `{rate, errorType}` shape for both failure classes (ambiguous which layer it targets) |
| D-030 | 2026-07-03 | MT-7 Stability Index's `metrics` row uses a composite `scope_key` (`cellId\|providerId\|mode`), not the bare cell id | The metric is defined "across reps in the same cell and engine-mode" (PRD MT-7); a cell run under two engine-modes produces two independent stability groups, and a bare cell-id key collided on the `metrics` unique index the first time a real multi-mode run (M4's e2e run) was recomputed | Bare cell-id scope key; averaging across modes into one cell-level value |
| D-031 | 2026-07-03 | SM-4 alias resolution applies to citations too: extraction engines emit observed brand names in `citations[].cited_for_brand_ids`, and our code resolves them to canonical ids. `extractions.extracted_json` stores the RESOLVED extraction (canonical brand ids on brands and citations, duplicates collapsed) — the unresolved engine output is reproducible from `raw_text` plus the engine version | Engines cannot know our row ids, so unresolved citation references made Citation Share structurally zero; storing the resolved record gives metrics and drill-downs one consistent source | Storing raw engine output and re-resolving on every metric read; a separate resolved-extraction column |
| D-032 | 2026-07-03 | DB-1's funnel heatmap is intent (funnel stage, rows) x persona (columns), Mention Rate per cell, backed by a new `metrics` scope `intent_persona` (`scopeKey = "<intent>|<personaId>"`) computed alongside every other scope in the same disposable recompute pass | "Funnel heatmap" was underspecified — two axes weren't named. Persona is the more commonly actionable segmentation for positioning work; market is the natural second axis if a future milestone wants a toggle | Market as the column axis; a three-axis (intent x persona x market) grid; querying brand_mentions ad hoc instead of persisting a scope |
| D-033 | 2026-07-03 | Two RB-1 findings-engine interpretations: (1) "high-intent" for lost-shortlist cells means `comparison` and `validation` intents specifically — the two that most directly represent a bottom-funnel decision moment, not `discovery`/`consideration`/`objection`. (2) Report `generated_md` is produced by deterministic templates over already-computed metrics/findings data, never an LLM call — no "report-writing engine" exists or is planned for MVP | Neither was specified. Templates keep RB-5's cautious, non-promissory tone a property of the code (verified by a forbidden-phrase test) rather than a hope about model output, and match the fixture-backed determinism the rest of mock mode already relies on (D-022) | Treating all five intents as high-intent; an LLM-generated report draft with a tone-compliance prompt |
| D-025 | 2026-07-02 | Add `BUILD_NOTES.md`: append-only per-session working memory (goal, verified/unverified work, rejected approaches, exact next action, gotchas), written as step one of the handoff ritual and read in the boot ritual. Entries are disposable — deleted at milestone merge after anything durable graduates to a canonical doc | Commits and the PRD tracker capture milestone-level state but not mid-milestone dead ends, unverified work, or the next action — the expensive things for a fresh agent to re-discover | A broad ways-of-working/SOP handbook; putting session state in the PRD tracker; relying on git history alone |
| D-026 | 2026-07-02 | Intake drafts persist as step-keyed raw JSON in `projects.intake_draft_json` (migration 0001); rows normalize into the intake tables in one transaction only when intake completes, replacing prior rows | Normalized tables have NOT NULL and unique constraints that partial mid-step drafts cannot satisfy; a JSON scratch column keeps autosave lenient while completion stays strict | Lenient nullable intake tables; client-side-only draft state; per-step normalized upserts with partial data |
| D-034 | 2026-07-03 | Login sessions are stateless signed cookies: `SESSION_COOKIE` holds `base64url(JSON) + HMAC-SHA256` over `{issuedAt, expiresAt}` under `SESSION_SECRET`, verified in `middleware.ts` (`runtime: "nodejs"`, required since `node:crypto`'s `timingSafeEqual`/`createHmac` aren't fully Edge-compatible) | ST-6 needs expiry and tamper-resistance for a single shared operator password; a `sessions` table adds a migration and a cleanup job for a fact (is this token valid) that HMAC verification answers without a DB round trip | A `sessions` table with server-side revocation; JWT with a third-party library |
| D-035 | 2026-07-03 | Split the provider registry in two: `src/providers/registry.ts` stays a static, metadata-only map (safe for run-creation cost/capability display, `generate()` must never be called on it) and `src/modules/runner/provider-resolver.ts` adds `resolveRuntimeProvider`/`resolveExtractionCredentials`, which decrypt a real credential and construct a genuinely-callable instance — used only by the worker | The pre-M8 registry's DeepSeek entry had no credentials at all (`apiKey: ""`); giving it real decrypted keys would mean every UI page that lists providers for display also decrypts credentials it never uses, widening the blast radius of a leak for no benefit | One registry that always resolves credentials; decrypting lazily inside the static registry's `generate()` |
| D-036 | 2026-07-03 | D-022's "Settings-configured provider+model" for extraction has no dedicated picker or settings table in MVP — extraction reuses the response's own generation provider's credentials (`resolveExtractionCredentials(response.providerId)`), and ST-4's other defaults (run caps, daily budgets) ship as a read-only Settings display sourced from env (D-012), not a new editable table | No `app_settings` table exists anywhere in `ENGINEERING_SPEC.md`, and D-022's own "no separate key path" phrase already implies reuse rather than a second credential; inventing a settings table this late in MVP for a single-provider (DeepSeek) reality adds a migration for a choice with no second option to choose from yet | A dedicated `app_settings` singleton table with an extraction-provider picker; per-run extraction-engine override in the run-creation UI |
| D-037 | 2026-07-03 | Per-provider daily-budget enforcement (C-2/D-012) runs inside the worker's existing `afterJobFinished`, right after the cost-cap/failure-rate breaker check: for each provider in the run's `selectedProvidersJson` (skipping `mock`), sum today's spend (`responses.cost_usd` + `extractions.cost_usd`, joined on provider) and reuse `checkCostCap` against the env-configured budget; a trip pauses the run via the same `circuit_breaker_paused` path, reason `daily_budget_exceeded` | The breaker mechanism, pause semantics, and run event already existed for the cost cap; a parallel enforcement path would duplicate all of it for a check that differs only in what "the cap" means | A separate budget-specific breaker/pause code path; checking budget only at run creation (misses spend accrued by other runs the same day) |
| D-038 | 2026-07-03 | Run mode is an explicit, enforced boundary: `RunCreationInput.runMode` is operator-selected in the run form (mock / live_validation / live_audit); the server validates C-9 in both directions (mock run ⇒ only the mock provider, live run ⇒ never the mock provider), locks k=5 for `live_audit` (C-1), rejects failure injection on live runs (D-027 is a mock-run tool), and rejects selections where every job would be skipped (PV-5 — such a run has no job that can ever finish and sits `queued` forever, repo-level backstop for scripts). The worker independently re-checks C-9 per job via `isProviderAllowedForRunMode` and dead-letters mismatches, since scripts/tests insert job rows without the action | External audit found the run form exposed DeepSeek while `createRun` hardcoded `runMode: "mock"` — real paid generations could be stored, displayed, and aggregated under MOCK semantics, violating C-9 in the worst direction | Deriving run mode silently from the provider set; restricting the UI to mock-only and keeping live runs script-only; trusting creation-time validation alone without the worker guard |
| D-039 | 2026-07-03 | Live-call safety set: (1) every provider call gets `AbortSignal.timeout(WORKER_PROVIDER_TIMEOUT_MS, default 45s)` — kept under the 60s stale-lock window so a hung call can't outlive its job lock and get double-billed via reclaim; the DeepSeek adapter maps both `AbortError` and `TimeoutError` (what `AbortSignal.timeout` actually throws) to the `timeout` error type. (2) Daily budgets fail CLOSED: a set-but-unparseable budget env value becomes $0 (pausing runs loudly), never NaN (which silently disabled enforcement); post-job enforcement overshoot stays bounded by provider concurrency × one call's cost, accepted for MVP. (3) Run cost projection estimates from the matrix version's real average prompt length plus one `estimateExtractionCostUsd()` per planned call for live runs (D-022), instead of an empty prompt and $0 extraction | Audit finding: a hung request past the stale-lock window duplicates paid calls; a budget typo like "25USD" disabled the C-2 guard entirely; projection could pass the cap while real generation+extraction spend exceeded it | Per-claim budget gating (a spend query every 300ms tick); treating unparseable budgets as unset; blocking MVP on token-accurate projection |
| D-040 | 2026-07-03 | Credential `base_url` overrides are allowlisted: HTTPS only, host must be the provider's official API host or the host of the deploy-layer `<PROVIDER>_BASE_URL` (D-020) — pointing at a proxy is a deploy-config decision, not a Settings form field. Separately, all MODEL-DERIVED text interpolated into generated report markdown (claim text, evidence quotes, citation domains) is escaped at the template source (`src/core/md.ts`): HTML entities plus pipe/newline neutralization, since the print/PDF view renders via `marked` + `dangerouslySetInnerHTML` and marked passes raw HTML through. Operator-entered content (edits, brand names, fact sheet) stays unescaped — the operator owns the report | Audit findings: every provider call sends `Authorization: Bearer <key>` to the credential's base URL, making an arbitrary override a one-field key-exfiltration path; and live-mode claim text originates from the open web, making unsanitized markdown an XSS path into the report views | Env-only base URLs (kills the D-020 per-credential override); a DOM sanitizer dependency (jsdom-based, heavy) instead of escape-at-source; escaping operator content too (mangles legitimate `&` in brand names for no threat-model gain) |
| D-041 | 2026-07-03 | The extraction engine is ONE configured provider+model for all live runs (`EXTRACTION_PROVIDER` env, default `deepseek`) — D-022's literal wording, superseding D-036's M8 shortcut of reusing each response's own generation provider. A live run on any provider therefore requires an active credential for the extraction engine too (Settings says so); pointing `EXTRACTION_PROVIDER` at a provider without an extraction adapter fails loudly at the first extraction attempt, before any network call | With four generation providers, per-provider extraction would mean four more JSON-extraction adapters and four times the prompt-compatibility surface for zero measurement benefit — extraction quality should be a constant across the audit, not vary by which engine generated the answer. DeepSeek flash is also the cheapest JSON-capable engine | Per-provider extraction adapters (D-036 extended); an extraction-provider picker UI/table in MVP (env is the Settings-configured knob until a second usable engine exists) |
| D-042 | 2026-07-03 | Provider-down graceful degradation: after every dead-letter the worker checks the failing provider's outcomes in that run; zero successes plus >=`PROVIDER_DOWN_DEAD_LETTERS` (5) dead-letters marks it down — its remaining queued jobs become `skipped` with new error type `provider_down` (migration 0004, additive enum value), a `provider_down` run event is logged, and the run's OTHER providers finish, completing the run as PARTIAL. RN-7's failure-rate breaker then evaluates only non-down providers (`getBreakerCounts`); raw counts still drive partial derivation and display. A provider that succeeded earlier and then degrades is NOT "down" — the run-wide breaker still guards that case | M9's acceptance is "provider-down degrades gracefully": before this, one dead provider in a multi-provider run dragged run-wide failure past 20% and bricked the whole run behind the breaker — pause, resume, fail, pause again — starving the healthy providers | Per-provider pause/resume state on runs (new state machine); keeping the run-wide breaker as the only guard (bricks multi-provider runs); a consecutive-failure counter (harder to compute honestly in SQL than the zero-successes rule, for no added safety) |
| D-043 | 2026-07-03 | M10's "release checklist" is a checked-in artifact, `RELEASE_CHECKLIST.md`: one-time go-live gates (wait-for-CI, backup retention, health/login, keys via Settings, mock -> deepseek-mini -> grounded validation incl. closing the Gemini caveat) plus a per-audit delivery checklist and archive log. D-024's evidence archive is implemented as `pnpm archive:evidence <runId>`: EX-3 bundle read straight from the DB plus a `pg_dump`, written outside the repo; a missing dump exits with a distinct code (2) and an INCOMPLETE warning rather than pretending success. Split into its own file because it changes per delivered audit — a different cadence from `RENDER_DEPLOYMENT.md`'s one-time deploy contract (§7 split rule) | PRD §11 items 9/11 and the M10 acceptance name a release checklist and per-audit archive record, but nothing defined either; D-024's archive commitment had never been built | Folding the checklist into RENDER_DEPLOYMENT.md (wrong cadence); an in-app checklist UI (MVP scope); archiving via authenticated HTTP export (needs a session; the DB is already directly reachable wherever the script can run) |
| D-044 | 2026-07-03 | Extraction spend is a budget concern of the CONFIGURED EXTRACTION ENGINE, not the generation provider (correcting D-037's join-on-generation-provider under D-041): `getProviderSpendToday(p)` adds all of today's extraction cost only when `p == EXTRACTION_PROVIDER`, and the worker appends the extraction engine to the budget-check list for every live run. Cost projection sums over supported (provider, mode) pairs each estimated in its own mode, so grounded search/grounding fees are in the cap check. `createRun` preflights an active credential for every selected live provider AND the extraction engine before it can spend. The archive recomputes metrics (C-5 idempotent) before export so an unopened run's evidence pack isn't hollow. Misinformation claims are reviewable in the dashboard (confirm/correct/re-open), setting `claims_found.reviewed_at` — the executable form of the release-checklist review gate (SM-5: operator overrides beside, never over, extracted values; re-extraction resets review since it produces a new claim) | Independent audit of the M10-prep merge: D-041 made one extraction engine but D-037's budget query still charged extraction to the generation provider — an OpenAI run's DeepSeek extraction both evaded DeepSeek's budget and over-charged OpenAI's (C-2); grounded fees were dropped from projection; a missing extraction key could burn generation money with no metrics; metrics were only computed on demand so archives could ship with zero; and the review gate the checklist requires had no UI at all | Attributing extraction to the generation provider (the bug); a provider column on extractions (migration for a single-engine reality); recompute-on-completion instead of at archive (couples the worker to analysis); trusting the client's review enum values without server validation |
| D-045 | 2026-07-03 | Second independent-audit round, four more real findings fixed: (1) `afterJobFinished` now checks `isRunFinished` BEFORE the cost-cap/budget breaker — a run whose final job crossed the cap used to trip the breaker and pause with zero jobs left to ever finish it, stranding it forever; a finished run always completes regardless of its final cost. (2) A new reconcile-sweep query, `listResponsesWithStaleExtraction`, catches extractions torn between `createPendingExtraction`/`markExtractionRetrying` and the pipeline's next state transition (a worker-crash window the original "no extraction row at all" sweep couldn't see, since a torn row is a row); it re-extracts via a fresh version (AD-2, C-3). (3) Settings Verify now passes `AbortSignal.timeout` to `provider.generate()`, same as every other call site — it was the one path with no deadline at all. (4) CSV export cells starting with `=`, `+`, `-`, `@`, tab, or CR get a leading single-quote guard (CWE-1236) — raw model-origin text (grounded citation titles, extracted claim text) flows into these exports untrusted in live mode, same threat model as D-040's markdown escaping, different sink; JSON export stays raw since it's read as evidence, not opened in a spreadsheet. One finding from this round was investigated and NOT applied: `render.yaml`'s `generateValue: true` for `CREDENTIALS_ENCRYPTION_KEY` was flagged as possibly producing a value of the wrong shape — Render's own Blueprint docs confirm it always generates exactly a 256-bit (32-byte) base64 value, which is precisely what `crypto.ts`'s `loadKey()` already requires and correctly parses; no code change, documented in `RENDER_DEPLOYMENT.md` instead so the question isn't re-opened | Same audit discipline as D-038/D-039/D-040/D-044: verify every claim against the running code (and, for the KEK claim, against Render's own docs) before agreeing, then fix only what's real | Reproducing/trusting each finding without verification (the KEK claim would have been "fixed" incorrectly); a per-claim spreadsheet-safe-mode toggle instead of always guarding (raw model text is untrusted by default in live mode, not opt-in) |
| D-046 | 2026-07-03 | PM-9 is surfaced at three moments, hardest last: a warning (never a block) on the intake review step when `job_to_be_done`/`category` contain tracked brand terms — the first moment all brands are known (step 1 predates brand entry); per-cell badges plus a banner on draft matrix boards via the shared `scanUnbrandedCells` in `src/core/matrix.ts`; and the existing hard approval gate, now refactored onto the same scanner. Intake completion is never hard-blocked on brand terms because branded intents (comparison/validation) legitimately interpolate brands — contamination only invalidates the two unbranded intents | The Heytea pilot hit the approval-time PM-9 error with zero earlier signal: a brand-contaminated `job_to_be_done` (the audit's meta-goal, not a buyer job) flowed silently through intake and generation, and the error named neither the cell nor the source field. The scanner was correct; the timing and attribution were the bug | Hard-blocking intake on brand terms in basics (over-blocks branded intents' inputs); scanning at step-1 submit (brands unknown yet); auto-stripping brand terms from rendered cells (silently changes what the operator approves) |
| D-047 | 2026-07-03 | Non-Next entrypoints (worker, scripts) load env via `src/env-bootstrap.ts` (`process.loadEnvFile` on `.env.local` then `.env`, guarded by `existsSync`), imported FIRST — before any module whose top-level body reads `process.env`. Next auto-loads env; standalone `tsx` processes did not, so anything past `DATABASE_URL`'s hardcoded fallback (notably `CREDENTIALS_ENCRYPTION_KEY`) was silently unset. Separately, `decryptApiKey` now distinguishes a KEK config error (missing/malformed env var → throws `CredentialConfigError`, leaves the row alone) from a genuine ciphertext failure (wrong-but-valid key or tampered data → returns null, caller marks the row `invalid` per D-021) | A live-audit run dead-lettered every job with "No active deepseek credential": the worker (`pnpm worker` = bare `tsx`) had no `CREDENTIALS_ENCRYPTION_KEY`, so `loadKey()` failed, `decryptApiKey` swallowed it into null, and the resolver marked a perfectly good stored credential `invalid` — a local env misconfiguration corrupting shared DB state and tripping the failure breaker. Precedence matches Next (real shell env wins; `.env.local` over `.env`) because `loadEnvFile` never overwrites an already-set var | A dotenv dependency (Node 22 has `loadEnvFile` built in); loading env inside each script separately (easy to forget the import-order requirement); leaving `decryptApiKey` swallowing config errors (the actual corruption path) |

## 10. Current state

Milestone status has exactly one home: the milestone tracker and progress notes in `PRD.md` section 11. Do not record status here.

## 11. Cross-platform wiring

This file stays canonical and tool-agnostic. Tool-specific files are thin pointers only:

- `CLAUDE.md`: import/pointer to this file.
- `AGENTS.md`: one-line instruction to read this file.
- `.cursor/rules/parallax.mdc`: always-on pointer to this file.

Verify each tool's current instruction-file behavior in official docs before relying on it.

## 12. Glossary

cell: one resolved prompt. engine-mode: provider plus grounding mode. rep: one repeated sample. run: execution of an approved matrix across selected engine-modes and repetitions. extraction: structured record parsed from one raw response. dead-letter: a failed job or extraction stored for review. golden dataset: hand-labeled fixtures with exact expected extractions and metrics. mock mode: seeded fixture-backed provider mode. stability index: mean pairwise Jaccard of top-5 brand sets across reps. lost-shortlist cell: high-intent cell where a competitor appears in >=60% of samples and the client appears in <=20%. fact sheet: operator-entered ground truth. matrix version: immutable approved prompt-cell snapshot.

"Validation" is overloaded; always qualify it: validation (intent) is one of the five prompt intents; validation run (`live_validation`, "validation-only") is a cheap k=2 pipeline dry-run that is never client-ready evidence; schema validation is Zod input/output checking. The provider id `google` is the Gemini provider — prose says Gemini, code says `google`.
