# MASTER_CONTEXT.md - Resonance (Parallax engine)

> The single canonical context file for this repository. Every AI coding session and every human contributor starts here. It contains project identity, non-negotiable constraints, durable decisions, and session rituals. Resonance is the product; Parallax is the measurement engine and the repo/package/code name (D-063). Product scope lives in `PRD.md`; implementation rules live in `DEVELOPMENT_GUIDELINES.md`.

---

## 0. How to use this file

1. Read this file fully at session start. Then read the document relevant to the task.
2. Docs beat chat memory. If a conversation contradicts these documents, the documents win until the change is explicitly agreed and written into the Decision Log.
3. One fact, one home. Identity, constraints, decisions, and rituals live here. What to build lives in `PRD.md`. How to build lives in `DEVELOPMENT_GUIDELINES.md`.

---

## 1. What this product is

Resonance is the product umbrella (D-063): an internal, operator-facing web tool organized around two named epistemic layers (D-077) — the Evidence Layer and the Simulation Layer. The Evidence Layer — Parallax, everything built M0-M15 — audits how AI systems describe, rank, recommend, cite, and misrepresent brands: curated prompt matrix across LLM provider APIs with repeated sampling, structured extraction from stored raw answers, claim verification against a client fact sheet, and an editable audit report. Within the Evidence Layer, the Four P pillars (Presence, Position, Perception, Proof) organize the audit's client-facing questions, with Proof as the trust rail under the other three. The Simulation Layer — M16+ — takes measured AI framing as stimulus and simulates, via persona-conditioned synthetic panels scored with Semantic Similarity Rating (SSR, D-064), how buyer segments respond to that framing and to candidate fixes. This two-layer split is a presentation layer over the existing pillar/intent taxonomy, never a data restructure.

Measurement and simulation have different epistemic status and never mix (C-12). The measurement engine's ground truth is the AI itself; the simulation layer is a validated-but-bounded proxy for humans and speaks only in comparisons (C-14).

It is used by one operator, usually a consultant, to produce paid client audits and simulation studies. Through the internal PoC (M20) it is not a SaaS product: the existing shared-password login stays (it guards spendable API credentials, C-11/D-024), and there are no client logins, no multi-user roles, no billing, no marketing pages, and no white-label theming. The end client only receives exported reports and evidence packs.

## 2. Why it exists

AI assistants have become decision intermediaries between brands and buyers. Consultants sell "how does AI see your brand" audits at roughly $3k-$15k. Parallax's differentiator is statistical honesty: LLM outputs are probabilistic, so audit prompts are sampled repeatedly and metrics ship with confidence intervals plus stability signals. Never build features or write copy that promise guaranteed AI rankings. The product measures distributions, verifies claims, and preserves evidence.

## 3. Methodology in one screen

- A prompt cell is one resolved prompt: intent x persona x market x phrasing variant.
- Each audit-grade cell runs k=5 repetitions per selected engine-mode. Validation mini-runs may use k=2 and must be labeled validation-only.
- Metrics are rates or averages over samples and are reported with Wilson confidence intervals where applicable.
- Mock is provider #0 and remains permanently registered for tests, demos, and failure injection.
- The first live dry-run provider is DeepSeek, because its official API is OpenAI/Anthropic-compatible and currently offers low-cost text generation. MiniMax is the second candidate provider after account/API-key details are confirmed.
- The long-term target provider set remains OpenAI, Anthropic, Gemini, and Perplexity, added later through the same provider interface.
- Grounded means the provider/API path supplies web-grounded output with normalized citations. If a provider cannot supply citations, its runs are ungrounded live validation runs and must not be mixed into grounded aggregates.
- Metrics: Mention Rate, Organic Recommendation Rate, Comparative Win Rate, Share of Voice, Avg First Position, sentiment (organic/solicited, never pooled), attribute-association matrix, citation share, accuracy rate, and Stability Index. The prompt-frame rule (D-054, PRD MT-12) governs all of them: a metric never counts a signal the prompt itself planted — presence rates count only unbranded prompts, comparative wins only head-to-head prompts, and objection cells feed no sentiment.
- Checkable claims about the client brand are matched against the client fact sheet and reviewed in the misinformation register.
- Aggregate findings and metric claims render only where n >= 30 eligible samples. Cell-level findings (for example lost-shortlist) are exempt from the threshold but always carry a directional-only label, as do low-stability clusters. Eligible samples are defined in `DEVELOPMENT_GUIDELINES.md` E2.
- Simulation (Simulation Layer, M16+): a resonance study cell is panel-persona x stimulus variant; free-text reactions are elicited (never numeric ratings), scored into 5-point Likert PMFs by embedding similarity against versioned anchor statement sets (SSR, arXiv:2510.08338), and reported as per-variant distributions, point-estimate means (no invented intervals, D-023), and deltas vs a baseline stimulus. The n >= 30 gate applies to variant aggregates; persona slices are always directional-only. Simulated metrics live under `resonance_*` scopes and never enter audit aggregates (C-12).

## 4. Hard constraints - never violate

| ID | Constraint |
|---|---|
| C-1 | A run processes at most 50 prompt cells. Enforced at matrix approval, run creation, and in the worker. Default audit matrix: 40 cells, bottom-funnel weighted. k=5 is protected for audit-grade runs; cut coverage before repetitions. |
| C-2 | Three nested cost guards: C-1 structural cap, per-run dollar cap, and per-provider daily budgets. The guards cover all paid LLM calls — generation and extraction alike (D-022). |
| C-3 | Raw responses are immutable. Re-extraction creates new versioned extraction rows. Every reported number traces to stored raw text. |
| C-4 | Approved matrices are frozen. Edits create a new matrix version. Runs reference exactly what was sent. |
| C-5 | Metrics are disposable: pure functions over extractions, recomputable idempotently. |
| C-6 | Schema changes happen only via migration files in git. Database dashboards are read-only for inspection. |
| C-7 | Modules communicate only through the database and typed interfaces. The UI never imports providers. `/src/core` imports nothing from other project layers. |
| C-8 | Never scrape consumer chat UIs. Official APIs and SERP/API vendors only. Permanent rule, not a deferral. |
| C-9 | Mock runs are first-class but always flagged (`run_mode: mock`, MOCK badge) and never mixed into live aggregates. |
| C-10 | Provider grounding capability is explicit. Grounded runs are blocked for providers that cannot return citations through an approved API path. |
| C-11 | LLM provider API keys are never stored in source files, `.env.example`, `render.yaml`, fixtures, logs, or client-visible payloads. The operator enters them in the authenticated Settings UI; the server encrypts them at rest and only the server/worker can decrypt them. |
| C-12 | Measured and simulated data never mix. Resonance (simulation) rows are structurally separated (matrix `kind`, `resonance_*` metric scopes, run-scoped report sections); audit aggregates, charts, and reports never include simulated samples and vice versa; every simulation surface renders a SIMULATED badge. Enforced by recompute dispatch plus wall tests (same isolation discipline as C-9). |
| C-13 | Simulations are evidence-conditioned: at approval, a study's `measured_ai` stimulus must cite stored raw response ids from the same project — no toggle can bypass this (D-078 removed the operator "unconditioned" toggle entirely). GENERIC is now a historical-only label, rendered truthfully on studies approved before D-078 but unreachable for any new approval. |
| C-14 | Simulation claims are comparative only: rankings and deltas between stimulus variants. Never absolute purchase-intent promises, never sales/ROI predictions, never quoting the SSR paper's accuracy as our own. ΔPI is a Likert-scale survey-construct shift, never framed as purchase probability. Panel persona conditioning: age and income band are the paper-validated axes; location and behavioral profile are prompt context only, never presented as validated segmentation (D-066); no gender/ethnicity conditioning. Enforced in template copy by forbidden-phrase tests (RB-5 pattern). |
| C-15 | Simulation baseline provenance & disclosure (M34A, D-099 — supersedes the D-094 eligibility-certification form; D-096's shadow rollout of that form is moot). A measured Simulation baseline is a **verbatim stored response** linked to >=1 reviewed framing annotation from a locked, versioned codebook. At handoff the stimulus record **immutably snapshots**: codebook version, coding-run version, numerator/denominator, prompt spread, model/mode scope, and review method — and these render wherever the simulation result renders (recurrence figures can change on recode; the simulation retains the evidence state it was commissioned on). Low-recurrence baselines are usable but must carry accurate labels (e.g. `SINGLE OBSERVED INSTANCE`), never "recurring framing." Structural gates enforced in code: raw response exists; exact offset-verified span exists; denominator complete; codebook locked before counting; intended positioning revealed only after lock (reveal timestamped); reviewer identity/method disclosed; prompt/model versions pinned; stimulus links to coded evidence. **No semantic eligibility threshold exists** — no 5-of-6 rule, no leave-one-out, no unique-top, no medoid selection. Historical studies keep `LEGACY BASELINE`/`PRE-M34 BASELINE` rendering; B2B unchanged (no framing workflow). |

## 5. Stack snapshot

Hosting target: Render-only, with one `render.yaml` defining a Next.js web service, a background worker, and Postgres. Sanctioned fallback if DB cost matters: Render compute plus Supabase free Postgres.

Stack: Next.js 15 + TypeScript + Tailwind + shadcn/ui, Drizzle ORM, Zod, Vitest, and `run_events`. pino and Sentry are the observability target, not yet dependencies (D-076/D-081/D-092) — `src/observability.ts`'s `reportError` is the single swap-in seam.

| Command | Does |
|---|---|
| `pnpm dev` | Run the Next.js app locally |
| `pnpm worker` | Run the polling worker locally |
| `pnpm test` | Run Vitest, including golden dataset tests — DB-backed tests run against an ephemeral, auto-migrated+seeded embedded Postgres (`scripts/vitest-global-setup.ts`), never the dev DB (D-078) |
| `pnpm test:e2e` | Playwright smoke + axe floor over the critical operator journey (D-092); boots its own ephemeral DB + Next on :3100 |
| `pnpm test:db` | Boot the same ephemeral test-DB instance standalone in the foreground, for manual poking (mirrors `pnpm db:dev`'s UX; D-078) |
| `pnpm db:migrate` | Apply Drizzle migrations |
| `pnpm db:studio` | Inspect data with Drizzle Studio |
| `pnpm framing:m34a:collect` | Bounded live M34A neutral-evidence collection with raw provenance, span assistance, and harness-side C-2 ledger; never runs semantic eligibility |
| `pnpm framing:m34a:workflow` | Build blind packet, lock codebook, generate recurrence matrix, or create C-15 evidence snapshot from M34A artifacts |
| `pnpm framing:m34a:report` | Render descriptive M34A recurrence and actionable-gap report from locked human-reviewed coding |

This table is a snapshot of daily-driver commands. The canonical, complete command list — lint, typecheck, seed, and per-milestone acceptance commands — lives in `DEVELOPMENT_GUIDELINES.md` section F.

## 6. Repo map

```
/src/app                 Next.js routes and pages, UI-only entrypoint
/src/components          Presentational components, no fetching/business logic
/src/core                Domain types, Zod schemas, constants, pure math/rules
/src/modules/intake      Project intake and autosave orchestration
/src/modules/matrix      Prompt template rendering, allocation, approval
/src/modules/runner      Run creation, job planning, cost guards
/src/modules/extraction  Structured extraction and claim matching
/src/modules/analysis    Metrics, findings, stability calculations
/src/modules/resonance   Simulation Layer studies: compile, SSR scoring, resonance metrics (M17+)
/src/modules/report      Report section generation and export helpers
/src/modules/auth        Login/session (M8, D-034)
/src/modules/settings    Provider credentials UI/service (M8, D-017/D-021)
/src/modules/dashboard   Dashboard data assembly (M6)
/src/modules/setup       Post-intake Setup editing (M27, D-084)
/src/providers           LLMProvider interface, mock, live provider adapters
/src/db                  Drizzle schema, migrations, repositories
/src/worker              Polling worker entrypoint
/fixtures                Mock responses and golden expectations
/scripts                 Local verification and seed scripts
```

## 7. Documents index

| File | Owns |
|---|---|
| `MASTER_CONTEXT.md` | Identity, constraints, decisions, rituals |
| `PRD.md` | Product scope, user journey, requirements, milestones |
| `DEVELOPMENT_GUIDELINES.md` | Architecture, provider contracts, schemas, tests, workflow |
| `DESIGN_GUIDELINES.md` | Visual language: tokens, typography, surfaces, motion, component rules, visual guardrails |
| `ENGINEERING_SPEC.md` | Detailed schema, lifecycle states, provider matrix, seeds, acceptance commands |
| `RENDER_DEPLOYMENT.md` | Render Blueprint assumptions, secret model, first-deploy checklist |
| `RELEASE_CHECKLIST.md` | Go-live gates and the per-audit delivery/archive record (D-043) |
| `RESONANCE_BUILD_PLAN.md` | M16-M20 execution playbook: per-milestone steps, QA gates, critical-bug risk tables (D-063); executed, historical record |
| `M21_M26_BUILD_PLAN.md` | M21-M26 execution playbook, adopted as planning canon (D-077); executed, historical record |
| `M32_BUILD_PLAN.md` | M32 operator-workflow UI execution playbook: sidebar hierarchy, segmented routes, explicit edit states, and verification gates (D-088); approved planning canon |
| `M34_BUILD_PLAN.md` | M34 Baseline Framing Integrity execution playbook (D-094); **superseded by the D-099 descope** — historical record with status header; M34A's smaller plan lives in D-099 + `BUILD_NOTES.md` per D-090 |
| `LAYERS_AND_EVIDENCE_ONLY_EVALUATION.md` | Layer-identity rename and coverage-contract proposal synthesis, adopted as planning canon (D-077); executed, historical record |
| `CALIBRATION_PROTOCOL.md` | SSR human-benchmark calibration survey design, thresholds, and paired-data fixture format (M26, D-082) |
| `BUILD_NOTES.md` | Disposable per-session working memory for agent handoff; truncated at milestone merge |
| `README.md` | Quick orientation and local setup pointer |
| `AUDIT_METHODOLOGY.md` | Standing whole-repo cleanup-audit playbook — four-tier label, document-class taxonomy, review passes (D-086) |
| `PROTECTED_REGISTER.md` | Standing safety net of Decision-Log-protected surfaces; consulted by every future cleanup pass before a `Delete`/`Merge`/rename (D-086) |
| `fixtures/` | Demo project, mock response manifest, golden expectation manifest |

Split a section into a separate file only when it exceeds roughly 300 lines or changes at a clearly different cadence. Record the split in the Decision Log.

A milestone's build plan is a special case of the same rule (D-090). A milestone earns its own `M<N>_BUILD_PLAN.md` file only when its execution playbook clears the split test above — it spans multiple milestones, OR is multi-phase with per-phase acceptance criteria, OR realistically runs across several sessions or agents, OR exceeds ~300 lines. A milestone whose plan is smaller than that keeps its plan in its Decision Log entry plus its `BUILD_NOTES.md` handoff entry (as M27–M31 did), never a standalone file. When in doubt, no file: the Decision-Log-plus-BUILD_NOTES home is the default; a build-plan doc is the exception earned by size or procedural complexity, not a per-milestone habit. The boot ritual's "that milestone's named approved build plan when the PRD or Documents index names one" (§8) is deliberately conditional for exactly this reason — most milestones will not name one.

## 8. Session rituals

Boot ritual for implementation sessions:

> Read `MASTER_CONTEXT.md`, then the section for milestone M<N> in `PRD.md`, then that milestone's named approved build plan when the PRD or Documents index names one, then `DEVELOPMENT_GUIDELINES.md` section A, then the active milestone's entries in `BUILD_NOTES.md`. For any UI-facing work, also read `DESIGN_GUIDELINES.md`. For cleanup/refactor work, also read `AUDIT_METHODOLOGY.md` and `PROTECTED_REGISTER.md`. Summarize the plan in <=10 bullets and list expected files to touch. Wait for confirmation before editing.

Handoff ritual:

> Append a session entry to `BUILD_NOTES.md` (template inside that file). Append decisions made this session to `MASTER_CONTEXT.md` section 9. If a decision logged this session protects an existing surface from removal/rename/merge, also append it to `PROTECTED_REGISTER.md` (D-086) so the register doesn't silently go stale. Append a 3-line progress note to the current milestone in `PRD.md` (and flip the tracker to Done only when D-092's evidenced-walk rule is met). Update command/acceptance tables when scripts or gates change: `README.md`, this file §5, and `DEVELOPMENT_GUIDELINES.md` §F. Update `DESIGN_GUIDELINES.md` only when a visual rule changes; update `RELEASE_CHECKLIST.md` only when a go-live gate changes. Give the commit message. If stopping mid-task or blocked, write the `BUILD_NOTES.md` entry immediately, even without the rest of the ritual.

Session rules: one active session at a time, one branch per milestone, plan before any multi-file edit, and any schema-change plan must say the word migration. Any proposal to delete, merge, rename, or "simplify away" an existing surface must check `PROTECTED_REGISTER.md` (D-086) first; a match is an automatic Keep — Protected, cite the D-number. A milestone touching UI cannot be marked Done in the PRD tracker until its interactive verification has actually run and is evidenced in `BUILD_NOTES.md`; record deferred verification as "Code complete — unverified" instead (D-092 — this is the rule M32 would have needed). Commit at each phase-green boundary within a milestone rather than in one commit at milestone close (D-092).

Operational hazards, every session:
- Never `git add -A` / `git add .` — the operator keeps live untracked WIP in the tree; stage explicit paths only.
- Never `pnpm build` while a dev server is running (shared `.next` corruption, D-075).
- `pnpm test` is DB-isolated since D-078, but never point ad-hoc scripts at the dev DB directly — it holds real operator runs (D-073).
- If `node`/`pnpm` are missing from PATH, bootstrap via nvm before concluding the project is broken.

## 9. Decision Log

| ID | Date | Decision | Why | Alternatives rejected |
|---|---|---|---|---|
| D-001 | 2026-07 | Single TypeScript codebase, two entrypoints: app and worker | One mental model; shared Zod/Drizzle types | FastAPI/Python backend |
| D-002 | 2026-07 | Mock-first at exactly one boundary: the `LLMProvider` interface | Zero-cost E2E validation; mock remains permanent test/demo mode | Mocking DB; live-first development |
| D-003 | 2026-07 | 50-cell cap per run; k=5 protected for audit-grade runs; default 40 cells bottom-funnel weighted | Bounded cost and runtime; repetitions carry statistical validity | Cutting k; uncapped matrices |
| D-004 | 2026-07 | Render-only hosting for app, worker, and Postgres | One vendor/dashboard/bill | Vercel + Railway + Supabase as default |
| D-005 | 2026-07 | Consolidate the original plan into three canonical docs | Solo builder; fewer files stay maintained | Seven-file doc set |
| D-006 | 2026-07 | Adopt the four Karpathy coding principles as project law | Reduces plausible-but-wrong AI implementation drift | Ad-hoc style guidance |
| D-007 | 2026-07-02 | First live validation provider is DeepSeek; MiniMax is the second candidate; Western audit providers come later | DeepSeek and MiniMax expose compatible API paths and are cheaper for dry-run validation | Starting live work with four expensive providers |
| D-008 | 2026-07-02 | Grounded capability is provider-declared and enforced by run planning | Avoids mixing cheap ungrounded validation with grounded audit claims | Treating all provider output as equivalent |
| D-009 | 2026-07-02 | Add an M0.5 execution-readiness gate before product coding | Engineering teams need schema, states, provider capabilities, seeds, and acceptance commands before implementation | Starting M1 from high-level product docs only |
| D-010 | 2026-07-02 | Split execution contract into `ENGINEERING_SPEC.md` | Detailed schema and provider specs exceed the intended size/cadence of general development guidelines | Keeping all details in `DEVELOPMENT_GUIDELINES.md` |
| D-011 | 2026-07-02 | Malformed-output layering: any usable text a provider returns is stored as an immutable response and the job succeeds; content-level malformedness is handled at the extraction layer (partial-but-extractable sets `malformed: true`, unusable text dead-letters). The `malformed_output` job error is reserved for transport-level failures where nothing is stored | PRD treated malformed output as a retryable job error while guidelines dead-lettered it at extraction; with unique `job_id` on responses, a stored-then-retried job would collide. One layer must own content problems | Retrying jobs on content-level malformedness; relaxing response immutability/uniqueness |
| D-012 | 2026-07-02 | Daily budgets: global `PROVIDER_DAILY_BUDGET_USD` default plus optional `<PROVIDER>_DAILY_BUDGET_USD` overrides | C-2 requires per-provider budgets but only a single env var was specified; global-with-override keeps one knob while satisfying the constraint | One global budget only; mandatory per-provider vars |
| D-013 | 2026-07-02 | No exports table or state machine in MVP; Markdown, print-HTML, and CSV/JSON exports are synchronous downloads | The spec defined export states with no backing table; synchronous rendering is simpler and sufficient at MVP scale | Async export queue with `exports` table |
| D-014 | 2026-07-02 | Eligible sample = stored response whose latest extraction is valid (or QA-reviewed) with `refusal: false`; refusals and dead-letters are excluded from all metric denominators and reported separately | "Eligible samples" appeared in every rate metric but was never defined; metrics are pure functions so the definition is the implementation | Counting refusals in denominators; per-metric ad-hoc eligibility |
| D-015 | 2026-07-02 | The n >= 30 threshold applies to aggregate claims only; cell-level findings (lost-shortlist) render at any n with a mandatory directional-only label | A cell yields only k x engine-modes samples, so required cell-level findings were mathematically unreachable under a blanket n >= 30 rule | Blanket threshold; raising k |
| D-016 | 2026-07-02 | Mock fixture selection is keyed by a stable hash of `(resolved_text, provider_id, rep_index)`, never row UUIDs | UUID-keyed selection is not reproducible across re-seeds or fresh clones, undermining golden dataset guarantees in CI | Seeding by `cell_id` UUID; fixed hard-coded UUIDs in seeds |
| D-017 | 2026-07-02 | Provider credentials are website-entered settings encrypted in Postgres, not provider API-key environment variables | The operator does not want DeepSeek or other LLM API keys exposed through the codebase or deploy config; Settings UI is the intended control point | Provider API keys in `.env.example`, Render env, or hard-coded config |
| D-018 | 2026-07-02 | Add a Render Blueprint skeleton with web, worker, and Postgres but no LLM API keys | First deploy should be reproducible from the repo while keeping provider credentials operator-entered after login | Manual Render setup only; putting provider keys in Render env |
| D-019 | 2026-07-02 | Adopt the "machine-age evidence dossier" visual language in `DESIGN_GUIDELINES.md`: ink/paper dual surface, one signal-orange accent, mono-first typography, dossier metadata framing, silk-smooth motion budgets, and visual guardrails V-1 to V-12 | Operator taste (cyberpunk-refined, Bitcoin-native, visually smooth) codified once so UI sessions do not drift; badge and CI visibility rules get design-level enforcement | Ad-hoc styling per session; neon cyberpunk theme; global dark-only theme |
| D-020 | 2026-07-02 | Provider service config precedence: env vars (`<PROVIDER>_BASE_URL`, `<PROVIDER>_DEFAULT_MODEL`) are defaults; a non-null `base_url`/`default_model` on the provider's `provider_credentials` row overrides them. At most one `active` credential per provider, enforced by a partial unique index | The same fact lived in two homes with no precedence, and `(provider_id, label)` uniqueness allowed several active keys with no selection rule for the runner | DB-only config; multiple active credentials with precedence logic |
| D-021 | 2026-07-02 | Credential crypto and KEK lifecycle: AES-256-GCM with per-row nonce; fingerprint is SHA-256 of the raw key; rows carry `key_version` for future KEK rotation; decrypt failure marks the row `invalid` and prompts re-entry in Settings, never crashes the worker; the Render env group holding `CREDENTIALS_ENCRYPTION_KEY` must never be deleted/recreated — recovery from KEK loss is re-entering keys | Unpinned crypto invites per-session improvisation; a regenerated KEK silently orphans every stored credential | Unversioned ciphertext; crashing on decrypt failure; external KMS in MVP |
| D-022 | 2026-07-02 | The "diagnostic engine" term is retired; the extraction engine is a Settings-configured provider+model resolved through the same provider registry and credential service as generation. Extraction calls are counted in projected run cost, the per-run cap, and daily budgets. Mock runs and CI use fixture-backed extraction, never a live extraction engine | Extraction is a second layer of paid LLM calls that was invisible to every cost guard, and golden tests require deterministic extraction | Uncapped extraction spend; live extraction calls in CI; leaving "diagnostic engine" undefined |
| D-023 | 2026-07-02 | Confidence-interval methods are per metric: Wilson only for per-sample proportions (Mention Rate, Recommendation Rate, Accuracy Rate). Share of Voice, Citation Share, Avg First Position, and Stability Index ship in MVP as point estimates explicitly labeled as having no interval; bootstrap intervals are post-MVP | Wilson is statistically invalid for count ratios and means; shipping wrong intervals contradicts the statistical-honesty differentiator | Wilson everywhere; blocking MVP on bootstrap implementation |
| D-024 | 2026-07-02 | MVP production-hardening set: rate-limited login with constant-time password comparison and session expiry; worker heartbeat with staleness surfaced in Debug; post-audit evidence archive (EX-3 export plus database dump stored off-Render); additive-first migration discipline; `claims_found.reviewed_at` for the evidence chain | The shared password now guards spendable API credentials; Render workers have no health checks; managed-Postgres backup retention is thinner than the C-3 evidence promise | Deferring all hardening past the pilot; retrofitting review timestamps after M1 |
| D-027 | 2026-07-03 | Test-only failure injection lives on `audit_runs.debug_failure_injection_json` (nullable jsonb: `{rate, errorType}`), applied by the worker before calling the provider, never threaded through `GenerationRequest` | MK-5/RN-5/RN-6 need deterministic, DB-visible chaos injection across the app/worker process boundary (C-7); job-level transport errors cannot be produced by mock fixture content (D-011), and injection is a test concern, not a provider concern | A generic `debug_flags` key-value table; injection parameters inside the provider interface; random fixture-driven failure only |
| D-028 | 2026-07-03 | `GenerationRequest` gains an optional `repIndex?: number`, ignored by real provider adapters, read only by MockProvider for D-016 fixture selection | The interface as originally specified in `DEVELOPMENT_GUIDELINES.md` C2 had no way to vary the sample within a cell's k repetitions; without it every rep would deterministically select the same fixture, making repeated sampling meaningless in mock mode and stability index untestable | Smuggling rep index through `promptText`; a mock-only side-channel outside the interface; computing selection in the worker instead of the provider |
| D-029 | 2026-07-03 | `audit_runs.debug_failure_injection_json` becomes `{ generation?: {rate, errorType}, extraction?: {invalidRate} }` — the extraction service applies `extraction` before validating, independently of D-027's `generation` config which the worker applies before calling the provider | SM-2/SM-3 (extraction retry/dead-letter) need the same deterministic, DB-visible chaos injection D-027 gave job-level failures, but extraction validation failures are a different failure class on a different code path than generation transport errors | A second column; overloading D-027's flat `{rate, errorType}` shape for both failure classes (ambiguous which layer it targets) |
| D-030 | 2026-07-03 | MT-7 Stability Index's `metrics` row uses a composite `scope_key` (`cellId\|providerId\|mode`), not the bare cell id | The metric is defined "across reps in the same cell and engine-mode" (PRD MT-7); a cell run under two engine-modes produces two independent stability groups, and a bare cell-id key collided on the `metrics` unique index the first time a real multi-mode run (M4's e2e run) was recomputed | Bare cell-id scope key; averaging across modes into one cell-level value |
| D-031 | 2026-07-03 | SM-4 alias resolution applies to citations too: extraction engines emit observed brand names in `citations[].cited_for_brand_ids`, and our code resolves them to canonical ids. `extractions.extracted_json` stores the RESOLVED extraction (canonical brand ids on brands and citations, duplicates collapsed) — the unresolved engine output is reproducible from `raw_text` plus the engine version | Engines cannot know our row ids, so unresolved citation references made Citation Share structurally zero; storing the resolved record gives metrics and drill-downs one consistent source | Storing raw engine output and re-resolving on every metric read; a separate resolved-extraction column |
| D-032 | 2026-07-03 | DB-1's funnel heatmap is intent (funnel stage, rows) x persona (columns), Mention Rate per cell, backed by a new `metrics` scope `intent_persona` (`scopeKey = "<intent>|<personaId>"`) computed alongside every other scope in the same disposable recompute pass | "Funnel heatmap" was underspecified — two axes weren't named. Persona is the more commonly actionable segmentation for positioning work; market is the natural second axis if a future milestone wants a toggle | Market as the column axis; a three-axis (intent x persona x market) grid; querying brand_mentions ad hoc instead of persisting a scope |
| D-033 | 2026-07-03 | Two RB-1 findings-engine interpretations: (1) "high-intent" for lost-shortlist cells means `comparison` and `validation` intents specifically — the two that most directly represent a bottom-funnel decision moment, not `discovery`/`consideration`/`objection`. (2) Report `generated_md` is produced by deterministic templates over already-computed metrics/findings data, never an LLM call — no "report-writing engine" exists or is planned for MVP | Neither was specified. Templates keep RB-5's cautious, non-promissory tone a property of the code (verified by a forbidden-phrase test) rather than a hope about model output, and match the fixture-backed determinism the rest of mock mode already relies on (D-022) | Treating all five intents as high-intent; an LLM-generated report draft with a tone-compliance prompt |
| D-025 | 2026-07-02 | Add `BUILD_NOTES.md`: append-only per-session working memory (goal, verified/unverified work, rejected approaches, exact next action, gotchas), written as step one of the handoff ritual and read in the boot ritual. Entries are disposable — deleted at milestone merge after anything durable graduates to a canonical doc | Commits and the PRD tracker capture milestone-level state but not mid-milestone dead ends, unverified work, or the next action — the expensive things for a fresh agent to re-discover | A broad ways-of-working/SOP handbook; putting session state in the PRD tracker; relying on git history alone |
| D-026 | 2026-07-02 | Intake drafts persist as step-keyed raw JSON in `projects.intake_draft_json` (migration 0001); rows normalize into the intake tables in one transaction only when intake completes, replacing prior rows | Normalized tables have NOT NULL and unique constraints that partial mid-step drafts cannot satisfy; a JSON scratch column keeps autosave lenient while completion stays strict | Lenient nullable intake tables; client-side-only draft state; per-step normalized upserts with partial data |
| D-034 | 2026-07-03 | Login sessions are stateless signed cookies: `SESSION_COOKIE` holds `base64url(JSON) + HMAC-SHA256` over `{issuedAt, expiresAt}` under `SESSION_SECRET`, verified in `middleware.ts` (`runtime: "nodejs"`, required since `node:crypto`'s `timingSafeEqual`/`createHmac` aren't fully Edge-compatible) | ST-6 needs expiry and tamper-resistance for a single shared operator password; a `sessions` table adds a migration and a cleanup job for a fact (is this token valid) that HMAC verification answers without a DB round trip | A `sessions` table with server-side revocation; JWT with a third-party library |
| D-035 | 2026-07-03 | Split the provider registry in two: `src/providers/registry.ts` stays a static, metadata-only map (safe for run-creation cost/capability display, `generate()` must never be called on it) and `src/modules/runner/provider-resolver.ts` adds `resolveRuntimeProvider`/`resolveExtractionCredentials`, which decrypt a real credential and construct a genuinely-callable instance — used only by the worker | The pre-M8 registry's DeepSeek entry had no credentials at all (`apiKey: ""`); giving it real decrypted keys would mean every UI page that lists providers for display also decrypts credentials it never uses, widening the blast radius of a leak for no benefit | One registry that always resolves credentials; decrypting lazily inside the static registry's `generate()` |
| D-036 | 2026-07-03 | D-022's "Settings-configured provider+model" for extraction has no dedicated picker or settings table in MVP — extraction reuses the response's own generation provider's credentials (`resolveExtractionCredentials(response.providerId)`), and ST-4's other defaults (run caps, daily budgets) ship as a read-only Settings display sourced from env (D-012), not a new editable table | No `app_settings` table exists anywhere in `ENGINEERING_SPEC.md`, and D-022's own "no separate key path" phrase already implies reuse rather than a second credential; inventing a settings table this late in MVP for a single-provider (DeepSeek) reality adds a migration for a choice with no second option to choose from yet | A dedicated `app_settings` singleton table with an extraction-provider picker; per-run extraction-engine override in the run-creation UI |
| D-037 | 2026-07-03 | Per-provider daily-budget enforcement (C-2/D-012) runs inside the worker's existing `afterJobFinished`, right after the cost-cap/failure-rate breaker check: for each provider in the run's `selectedProvidersJson` (skipping `mock`), sum today's spend (`responses.cost_usd` + `extractions.cost_usd`, joined on provider) and reuse `checkCostCap` against the env-configured budget; a trip pauses the run via the same `circuit_breaker_paused` path, reason `daily_budget_exceeded` | The breaker mechanism, pause semantics, and run event already existed for the cost cap; a parallel enforcement path would duplicate all of it for a check that differs only in what "the cap" means | A separate budget-specific breaker/pause code path; checking budget only at run creation (misses spend accrued by other runs the same day) |
| D-038 | 2026-07-03 | Run mode is an explicit, enforced boundary: `RunCreationInput.runMode` is operator-selected in the run form (mock / live_validation / live_audit); the server validates C-9 in both directions (mock run ⇒ only the mock provider, live run ⇒ never the mock provider), locks k=5 for `live_audit` (C-1), rejects failure injection on live runs (D-027 is a mock-run tool), and rejects selections where every job would be skipped (PV-5 — such a run has no job that can ever finish and sits `queued` forever, repo-level backstop for scripts). The worker independently re-checks C-9 per job via `isProviderAllowedForRunMode` and dead-letters mismatches, since scripts/tests insert job rows without the action | External audit found the run form exposed DeepSeek while `createRun` hardcoded `runMode: "mock"` — real paid generations could be stored, displayed, and aggregated under MOCK semantics, violating C-9 in the worst direction | Deriving run mode silently from the provider set; restricting the UI to mock-only and keeping live runs script-only; trusting creation-time validation alone without the worker guard |
| D-039 | 2026-07-03 | Live-call safety set: (1) every provider call gets `AbortSignal.timeout(WORKER_PROVIDER_TIMEOUT_MS, default 45s)` — kept under the 60s stale-lock window so a hung call can't outlive its job lock and get double-billed via reclaim; the DeepSeek adapter maps both `AbortError` and `TimeoutError` (what `AbortSignal.timeout` actually throws) to the `timeout` error type. (2) Daily budgets fail CLOSED: a set-but-unparseable budget env value becomes $0 (pausing runs loudly), never NaN (which silently disabled enforcement); post-job enforcement overshoot stays bounded by provider concurrency × one call's cost, accepted for MVP. (3) Run cost projection estimates from the matrix version's real average prompt length plus one `estimateExtractionCostUsd()` per planned call for live runs (D-022), instead of an empty prompt and $0 extraction | Audit finding: a hung request past the stale-lock window duplicates paid calls; a budget typo like "25USD" disabled the C-2 guard entirely; projection could pass the cap while real generation+extraction spend exceeded it | Per-claim budget gating (a spend query every 300ms tick); treating unparseable budgets as unset; blocking MVP on token-accurate projection |
| D-040 | 2026-07-03 | Credential `base_url` overrides are allowlisted: HTTPS only, host must be the provider's official API host or the host of the deploy-layer `<PROVIDER>_BASE_URL` (D-020) — pointing at a proxy is a deploy-config decision, not a Settings form field. Separately, all MODEL-DERIVED text interpolated into generated report markdown (claim text, evidence quotes, citation domains) is escaped at the template source (`src/core/md.ts`): HTML entities plus pipe/newline neutralization, since the print/PDF view renders via `marked` + `dangerouslySetInnerHTML` and marked passes raw HTML through. Operator-entered content (edits, brand names, fact sheet) stays unescaped — the operator owns the report | Audit findings: every provider call sends `Authorization: Bearer <key>` to the credential's base URL, making an arbitrary override a one-field key-exfiltration path; and live-mode claim text originates from the open web, making unsanitized markdown an XSS path into the report views | Env-only base URLs (kills the D-020 per-credential override); a DOM sanitizer dependency (jsdom-based, heavy) instead of escape-at-source; escaping operator content too (mangles legitimate `&` in brand names for no threat-model gain) |
| D-041 | 2026-07-03 | The extraction engine is ONE configured provider+model for all live runs (`EXTRACTION_PROVIDER` env, default `deepseek`) — D-022's literal wording, superseding D-036's M8 shortcut of reusing each response's own generation provider. A live run on any provider therefore requires an active credential for the extraction engine too (Settings says so); pointing `EXTRACTION_PROVIDER` at a provider without an extraction adapter fails loudly at the first extraction attempt, before any network call | With four generation providers, per-provider extraction would mean four more JSON-extraction adapters and four times the prompt-compatibility surface for zero measurement benefit — extraction quality should be a constant across the audit, not vary by which engine generated the answer. DeepSeek flash is also the cheapest JSON-capable engine | Per-provider extraction adapters (D-036 extended); an extraction-provider picker UI/table in MVP (env is the Settings-configured knob until a second usable engine exists) |
| D-042 | 2026-07-03 | Provider-down graceful degradation: after every dead-letter the worker checks the failing provider's outcomes in that run; zero successes plus >=`PROVIDER_DOWN_DEAD_LETTERS` (5) dead-letters marks it down — its remaining queued jobs become `skipped` with new error type `provider_down` (migration 0004, additive enum value), a `provider_down` run event is logged, and the run's OTHER providers finish, completing the run as PARTIAL. RN-7's failure-rate breaker then evaluates only non-down providers (`getBreakerCounts`); raw counts still drive partial derivation and display. A provider that succeeded earlier and then degrades is NOT "down" — the run-wide breaker still guards that case | M9's acceptance is "provider-down degrades gracefully": before this, one dead provider in a multi-provider run dragged run-wide failure past 20% and bricked the whole run behind the breaker — pause, resume, fail, pause again — starving the healthy providers | Per-provider pause/resume state on runs (new state machine); keeping the run-wide breaker as the only guard (bricks multi-provider runs); a consecutive-failure counter (harder to compute honestly in SQL than the zero-successes rule, for no added safety) |
| D-043 | 2026-07-03 | M10's "release checklist" is a checked-in artifact, `RELEASE_CHECKLIST.md`: one-time go-live gates (wait-for-CI, backup retention, health/login, keys via Settings, mock -> deepseek-mini -> grounded validation incl. closing the Gemini caveat) plus a per-audit delivery checklist and archive log. D-024's evidence archive is implemented as `pnpm archive:evidence <runId>`: EX-3 bundle read straight from the DB plus a `pg_dump`, written outside the repo; a missing dump exits with a distinct code (2) and an INCOMPLETE warning rather than pretending success. Split into its own file because it changes per delivered audit — a different cadence from `RENDER_DEPLOYMENT.md`'s one-time deploy contract (§7 split rule) | PRD §11 items 9/11 and the M10 acceptance name a release checklist and per-audit archive record, but nothing defined either; D-024's archive commitment had never been built | Folding the checklist into RENDER_DEPLOYMENT.md (wrong cadence); an in-app checklist UI (MVP scope); archiving via authenticated HTTP export (needs a session; the DB is already directly reachable wherever the script can run) |
| D-044 | 2026-07-03 | Extraction spend is a budget concern of the CONFIGURED EXTRACTION ENGINE, not the generation provider (correcting D-037's join-on-generation-provider under D-041): `getProviderSpendToday(p)` adds all of today's extraction cost only when `p == EXTRACTION_PROVIDER`, and the worker appends the extraction engine to the budget-check list for every live run. Cost projection sums over supported (provider, mode) pairs each estimated in its own mode, so grounded search/grounding fees are in the cap check. `createRun` preflights an active credential for every selected live provider AND the extraction engine before it can spend. The archive recomputes metrics (C-5 idempotent) before export so an unopened run's evidence pack isn't hollow. Misinformation claims are reviewable in the dashboard (confirm/correct/re-open), setting `claims_found.reviewed_at` — the executable form of the release-checklist review gate (SM-5: operator overrides beside, never over, extracted values; re-extraction resets review since it produces a new claim) | Independent audit of the M10-prep merge: D-041 made one extraction engine but D-037's budget query still charged extraction to the generation provider — an OpenAI run's DeepSeek extraction both evaded DeepSeek's budget and over-charged OpenAI's (C-2); grounded fees were dropped from projection; a missing extraction key could burn generation money with no metrics; metrics were only computed on demand so archives could ship with zero; and the review gate the checklist requires had no UI at all | Attributing extraction to the generation provider (the bug); a provider column on extractions (migration for a single-engine reality); recompute-on-completion instead of at archive (couples the worker to analysis); trusting the client's review enum values without server validation |
| D-045 | 2026-07-03 | Second independent-audit round, four more real findings fixed: (1) `afterJobFinished` now checks `isRunFinished` BEFORE the cost-cap/budget breaker — a run whose final job crossed the cap used to trip the breaker and pause with zero jobs left to ever finish it, stranding it forever; a finished run always completes regardless of its final cost. (2) A new reconcile-sweep query, `listResponsesWithStaleExtraction`, catches extractions torn between `createPendingExtraction`/`markExtractionRetrying` and the pipeline's next state transition (a worker-crash window the original "no extraction row at all" sweep couldn't see, since a torn row is a row); it re-extracts via a fresh version (AD-2, C-3). (3) Settings Verify now passes `AbortSignal.timeout` to `provider.generate()`, same as every other call site — it was the one path with no deadline at all. (4) CSV export cells starting with `=`, `+`, `-`, `@`, tab, or CR get a leading single-quote guard (CWE-1236) — raw model-origin text (grounded citation titles, extracted claim text) flows into these exports untrusted in live mode, same threat model as D-040's markdown escaping, different sink; JSON export stays raw since it's read as evidence, not opened in a spreadsheet. One finding from this round was investigated and NOT applied: `render.yaml`'s `generateValue: true` for `CREDENTIALS_ENCRYPTION_KEY` was flagged as possibly producing a value of the wrong shape — Render's own Blueprint docs confirm it always generates exactly a 256-bit (32-byte) base64 value, which is precisely what `crypto.ts`'s `loadKey()` already requires and correctly parses; no code change, documented in `RENDER_DEPLOYMENT.md` instead so the question isn't re-opened | Same audit discipline as D-038/D-039/D-040/D-044: verify every claim against the running code (and, for the KEK claim, against Render's own docs) before agreeing, then fix only what's real | Reproducing/trusting each finding without verification (the KEK claim would have been "fixed" incorrectly); a per-claim spreadsheet-safe-mode toggle instead of always guarding (raw model text is untrusted by default in live mode, not opt-in) |
| D-046 | 2026-07-03 | PM-9 is surfaced at three moments, hardest last: a warning (never a block) on the intake review step when `job_to_be_done`/`category` contain tracked brand terms — the first moment all brands are known (step 1 predates brand entry); per-cell badges plus a banner on draft matrix boards via the shared `scanUnbrandedCells` in `src/core/matrix.ts`; and the existing hard approval gate, now refactored onto the same scanner. Intake completion is never hard-blocked on brand terms because branded intents (comparison/validation) legitimately interpolate brands — contamination only invalidates the two unbranded intents | The Heytea pilot hit the approval-time PM-9 error with zero earlier signal: a brand-contaminated `job_to_be_done` (the audit's meta-goal, not a buyer job) flowed silently through intake and generation, and the error named neither the cell nor the source field. The scanner was correct; the timing and attribution were the bug | Hard-blocking intake on brand terms in basics (over-blocks branded intents' inputs); scanning at step-1 submit (brands unknown yet); auto-stripping brand terms from rendered cells (silently changes what the operator approves) |
| D-047 | 2026-07-03 | Non-Next entrypoints (worker, scripts) load env via `src/env-bootstrap.ts` (`process.loadEnvFile` on `.env.local` then `.env`, guarded by `existsSync`), imported FIRST — before any module whose top-level body reads `process.env`. Next auto-loads env; standalone `tsx` processes did not, so anything past `DATABASE_URL`'s hardcoded fallback (notably `CREDENTIALS_ENCRYPTION_KEY`) was silently unset. Separately, `decryptApiKey` now distinguishes a KEK config error (missing/malformed env var → throws `CredentialConfigError`, leaves the row alone) from a genuine ciphertext failure (wrong-but-valid key or tampered data → returns null, caller marks the row `invalid` per D-021) | A live-audit run dead-lettered every job with "No active deepseek credential": the worker (`pnpm worker` = bare `tsx`) had no `CREDENTIALS_ENCRYPTION_KEY`, so `loadKey()` failed, `decryptApiKey` swallowed it into null, and the resolver marked a perfectly good stored credential `invalid` — a local env misconfiguration corrupting shared DB state and tripping the failure breaker. Precedence matches Next (real shell env wins; `.env.local` over `.env`) because `loadEnvFile` never overwrites an already-set var | A dotenv dependency (Node 22 has `loadEnvFile` built in); loading env inside each script separately (easy to forget the import-order requirement); leaving `decryptApiKey` swallowing config errors (the actual corruption path) |
| D-048 | 2026-07-03 | A runs index page exists at `/projects/[id]/runs` (`listRunsWithProgress`, one grouped query, no N+1), linked from the projects list (beside Matrix/Dashboard) and from the run-detail breadcrumb (`Projects / <name> / Runs / Run <id>`). This reverses the S-018 note that rejected a run-list hub ("runs are reachable via matrix + dashboard selector") | That reasoning only held for COMPLETED runs, which the M6 dashboard surfaces. An in-progress run's queue/progress page (pause/cancel/events/recompute) was reachable ONLY via the post-creation `router.push` — clicking away stranded the run with no path back short of knowing its URL, right when the operator most needs to monitor or pause a live paid run | A per-project "active run" shortcut only (misses historical runs); reusing the dashboard run-selector for progress (dashboard is metrics, not the queue/pause/cancel surface); leaving the URL as the only path (the reported bug) |
| D-049 | 2026-07-04 | The worker's `processJob` separates two failure DOMAINS that previously shared one try/catch: the provider call (resolve+`generate`) and persistence (`recordSuccess`). A provider-domain throw is classified and routed through `handleFailure` (retry/dead-letter, feeds the D-042 provider-down counter). A persistence throw — the provider call already SUCCEEDED and may have cost money — is dead-lettered as a new distinct error type `persistence_error` (migration 0005, additive enum) with a `job_persist_failed` event, and deliberately NOT routed through provider-down; `getProviderOutcomeCounts` also excludes `persistence_error` from its dead-letter tally so a coexisting DB fault can't inflate the count. No retry (a paid call must not be re-billed by a reclaim); the lost sample is excluded from denominators and the run still completes | Same shape as the D-047 credential bug: a DB blip during `recordSuccess` (a constraint violation, a connection drop) was caught by the shared handler, misclassified as a provider `server_error`, and dead-lettered a job whose provider call had already succeeded — and after 5 such misclassifications D-042 would mark a perfectly healthy provider "down" and skip all its remaining jobs. An infra fault reinterpreted as "the provider is bad," corrupting run state. Reachable in plain dev/test, no live key needed | Leaving the shared try/catch (the bug); retrying persistence (risks double-billing the already-paid call, or a unique-`job_id` conflict on a committed-but-unacked row); filtering provider-down by error type only at the count site (the call-site guard + the count filter together are defense-in-depth) |
| D-050 | 2026-07-04 | `createRun` preflights the per-provider daily budget (C-2/D-012) for live runs — `findExceededDailyBudget` over the same set the worker checks (selected providers + extraction engine, D-041) — rejecting at creation if a provider is already over budget; `projectRunCost` additionally returns per-provider `{spentUsd, budgetUsd}` so the run form shows today's spend vs cap (and a "would exceed" warning) before submit | The daily-budget guard existed but fired ONLY in the worker's post-job `afterJobFinished`: an operator could create a run whose provider was already over budget, watch it queue, and only learn it was blocked when it paused on its first finished job with zero progress — the PM-9 "correct rule, terminal-gate-only, no earlier signal" shape, but for spend | Preflighting only the per-run cost cap (misses cross-run same-day spend that the daily budget tracks); computing "would this run push it over" precisely (needs per-provider projected spend; the worker's running-total enforcement already covers mid-run overshoot, so creation only needs the already-over check + a form-level projection warning) |
| D-051 | 2026-07-04 | The Four P's semantic layer (PRD 8.16, M11): every operator- and client-facing surface is organized around four client questions — Presence ("Am I in AI's consideration set?"), Position ("When compared, do I win?"), Perception ("How does AI describe my brand?"), Proof ("Is the story true — and sourced?") — with the confidence machinery (k=5, Wilson, n>=30, stability) framed as the rail under all four, never a fifth pillar. Implemented as a pure `src/core` layer ON TOP of the existing taxonomy: intents map to pillars (discovery/consideration→Presence, comparison→Position, validation/objection→Perception; Proof is fed by every response's claims+citations), and a `METRIC_GLOSSARY` becomes the single source of truth for every metric's label, pillar, question, definition, and D-023 interval caveat. No stored data changes; no intent renames | Operator feedback after the first live pilot: the taxonomy was organized around how the pipeline works, not what the client asks; metrics rendered as raw keys ("attribute_refreshing 0.156") nobody could interpret; prompts, metrics, and report sections had no visible thread back to the client's three core questions | Renaming/replacing the intent taxonomy (breaks PM-2 quotas, golden data, and stored cells for zero measurement gain); per-surface ad-hoc labels (the current state — three places to drift); a fifth "confidence" pillar (dilutes the question-led framing; confidence is a property of every answer, not a question) |
| D-052 | 2026-07-04 | Category archetype template packs (PRD 8.16 AT-1..AT-5, M11): intake asks "how do buyers get this?" (`b2b`, `consumer_product`, `consumer_venue`); `projects` and `prompt_templates` gain archetype columns via migration; three seeded packs share the five-intent structure but phrase prompts in natural buyer language for their archetype, enforced by a per-archetype forbidden-jargon test (RB-5 pattern). Matrix generation filters templates by the project's archetype; approved matrices are untouched (C-4) | The Heytea pilot ran B2B-procurement prompts against a bubble-tea brand ("which Tea drink and bubble tea vendors are worth a demo?") — a prompt no human would type measures a distribution no buyer will ever see, so this is audit validity, not cosmetics | One "neutral" template set (blands every archetype to fix one); LLM-generated per-project templates (non-deterministic, breaks D-016 fixture keying and golden guarantees; can be layered later as operator-approved suggestions); free-text operator-authored templates only (loses the seeded-defaults contract in PRD 8.4) |
| D-053 | 2026-07-04 | Next-stage sequencing: M11 semantic layer -> M12 trust & provenance -> M13 operator UX/demo polish, with M10 close-out (deploy, remaining providers, Gemini caveat) as a parallel ops track. Client-deliverable polish (PDF charts, branding, docx) stays demand-driven after M13 | The semantic layer defines the vocabulary trust features speak ("this claim answers Presence, n=96, DeepSeek, ungrounded") and the structure the UX reorganizes around — building either first would mean rework; trust precedes visual polish because it is smaller and is the product's stated differentiator (statistical honesty). Operator confirmed no external time pressure ("get the product as polished as possible") and that polish budget leans operator-app with demo-flash moments | UX-first (operator's initial ordering — reversed with their agreement: polish without the semantic layer would reorganize screens around vocabulary scheduled to change); trust-first before the semantic layer (evidence annotations would speak raw metric keys) |
| D-054 | 2026-07-04 | The prompt-frame rule (PRD MT-12): a metric may never count a signal the prompt itself planted. Cells carry a frame derived from intent (unbranded = discovery/consideration, enforceable because PM-9 keeps them brand-free; client-branded = validation/objection; comparative = comparison). At cross-intent scopes: Mention Rate, Share of Voice, Avg First Position, Organic Recommendation Rate, and Citation Share count unbranded samples only; the new Comparative Win Rate (MT-13, Wilson) counts comparison samples only; sentiment splits into organic (unbranded mentions) and solicited (validation) groups that are never pooled, with objection cells feeding no sentiment metric at all; attribute association excludes samples whose resolved prompt text contains the attribute phrase (data-driven, catches operator edits); Citation Share additionally requires grounded samples and emits no row where none exist. Intent-pure scopes (intent, intent_persona) are exempt as transparent drill-down. Accuracy Rate and Stability Index stay frame-agnostic (claims and consistency cannot be planted). The drill-through (TP-4) applies the identical filters — evidence must show exactly the metric's denominator. Enforced in `metricIntentFilter` (src/core/semantic.ts) and verified by an independent-SQL spot-check test that encodes the frames separately | Operator vetting after the first live pilot, confirmed by that run's own data: pooled Mention Rate read 0.850 while every branded intent scored exactly 1.000 and the honest unbranded value was 0.564; Avg First Position read 1.484 against an organic 3.400 (branded prompts list the planted brand first); comparison-cell SoV was structurally 1/(1+competitors)=0.250; validation sentiment was 100% positive and objection 52% negative — both planted by prompt design. The audit's numbers were dominated by echoes of its own prompts, the opposite of the statistical-honesty differentiator | Down-weighting instead of excluding out-of-frame samples (unprincipled constant); a comparative rank metric (prompt-order artifact — the template names the client first); pooling sentiment groups with a label; deleting per-intent drill-down rows (transparency loss); renaming the recommendation_rate key (consumers churn for no semantic gain — the glossary label carries the meaning) |
| D-055 | 2026-07-04 | The pillar visual system: pillars are identified by numbered dossier structure (01-04 mono section headers, hairline frames, a 2px structural spine) rendered by shared `PillarSection`/`PillarChip` components, plus four muted structural tints added as tokens (`--color-pillar-{presence,position,perception,proof}`, dark ink-adjacent hues AA-legible on paper). The tints are RESERVED for pillar identity — spines, section headers, chips — and never style actions, verdicts, severity, or emphasis, so DESIGN_GUIDELINES' "exactly one accent" law survives: orange remains the only ACCENT; the tints are structural, like ruled ink on a form. The original M13 (OX-1..6) is split: visual system + subnav = M13, per-competitor analytics = M14 (CS-1..5), explanatory layer = M15 (EL-1..3 + OX-2/5/6), recorded in PRD 8.18-8.18b | Operator feedback on the M11/M12 draft dashboards: the pillar logic existed but was visually inapparent — cards, metrics, and charts read as a flat list; matrix prompts weren't visibly grouped; the matrix page had no path back to the dashboard (symptom of missing project-level navigation, cured by OX-1's subnav rather than a one-off link) | Four full accent colors (breaks the codified one-accent law and V-2); strict monochrome numbering alone (operator explicitly asked for color-or-section distinction — muted structural tints deliver color without a second accent); per-page ad-hoc pillar styling (three surfaces would drift, the M11 lesson) |
| D-056 | 2026-07-04 | Per-brand metric scope (CS-1): `recomputeMetrics` emits `scope_type='brand'`, `scope_key=<brand id>` rows for every tracked brand under the SAME D-054 frames as the client metrics — unbranded mention_rate and avg_first_position, share_of_voice as the brand's slice of unbranded tracked mentions (all brands sum to 1), comparative_win_rate over comparison samples. Three generic tested primitives added to core (`proportion` Wilson, `ratio` point, `meanValue` point) so the per-brand pass reuses tested math. The dashboard replaces the "client vs rest of the field" bar with two ranked per-brand bar charts (Presence mention share, Position head-to-head win rate — client in accent, competitors in stepped ink alphas per DESIGN §9); per-bar drill-through reuses the TP-4 metric machinery scoped to the brand id; the report Position chapter gains a full ranking table and a `brand_metrics` CSV joins the export set. No migration — `metrics` is disposable (C-5), scope_type is already text, one recompute backfills every run | The dashboard could only say "Heytea vs everything else" because M5 computed client-vs-all only; the operator needs to see where the client sits across the whole competitor spectrum (Chagee/Mixue/Koi individually), and the live data proved the value — Chagee actually wins head-to-heads more than the client (0.96 vs 0.92), invisible under the old grouping | Crossing brand with provider/mode/intent scopes (combinatorial blow-up for charts that only need per-brand-overall); a per-brand column or table (migration for disposable data); a comparative RANK metric (prompt-order artifact, already rejected in D-054); leaving competitors as "rest of the field" (the reported gap) |
| D-057 | 2026-07-04 | Post-M14 self-audit fixes (five findings, all verified against running code + live DB before fixing): (1) CS-4 was half-built — only the client's figure was drillable; the competitive-spectrum bars now each carry an `onBarClick` that drills that brand's evidence (SoVChart reads the Recharts payload's `brandId`). (2) `metricResponseLabel` hardcoded "client" for every subject — now names the SCOPED brand (`subjectName` threaded from the resolved brand row), so a competitor bar's evidence reads "Chagee mentioned", not "client mentioned"; this was dormant only because (1) meant nothing but the client was ever drilled. (3) `brand_mentions` gained a partial unique index on `(extraction_id, brand_id) where brand_id is not null` (migration 0007) — the DB backstop behind `collapseDuplicateBrandMentions`, so a future extraction bug can't inflate a per-brand mention_rate above 1.0 and break its Wilson interval. (4) the dashboard Perception section rendered only attributes; a `SentimentSection` now shows the D-054 organic/solicited distributions (never pooled, objection excluded) that previously existed only in the report. (5) the report evidence appendix no longer falls back to an unrelated first-eligible response when a finding has no specific match — it emits no excerpt (the template already says "no excerpt available"), so evidence is never misleadingly attributed | A full code audit requested after M14; the discipline was verify-then-fix (mock e2e green, archetype persistence confirmed wired, per-brand math confirmed against SQL) so only real defects were touched. Separately surfaced but NOT a code fix: the head-to-head chart is correctly gated by the n>=30 guard on the Heytea run (comparison n=25), so the operator's flagship chart is hidden until the matrix carries >=30 comparison samples — an allocation decision, logged for the next live run | Making bars clickable without fixing the label (would ship "client mentioned" on a competitor's evidence); a non-partial unique index (would reject the many legitimate NULL-brand unresolved mentions); pooling the two sentiment groups into one dashboard widget (violates D-054); keeping the evidence fallback (fabricates a finding-to-response link the matcher couldn't justify) |
| D-058 | 2026-07-04 | M15 explanatory layer, core pass (EL-1/2/3): PILLARS metadata gains `whatPromptsDo` and `businessValue`; `pillarMetricLabels(pillar)` derives the "feeds" list from the glossary's own pillar tags (single source, excludes the stability rail, appends the dynamic sentiment/attribute families for perception). A `PillarExplainer` renders these in the MATRIX only (the dashboard shows data, not prose). A live sample-budget panel in the matrix projects each pillar's cells x k=5 (audit-grade floor, one engine-mode) against the n>=30 aggregate gate, with ok/warn stamps — so allocation is a deliberate matrix-time decision, not a dashboard-time surprise (resolves S-024, and would have caught the D-057 head-to-head-hidden finding before the run). Per-cell hover lists the metrics its intent feeds | Operator feedback that the matrix/prompt-generation UI never explained what each pillar does or its business value, and the D-057 audit showed the n>=30 consequence of allocation was invisible until the dashboard | Duplicating the pillar-to-metrics mapping (glossary tags are the single source); showing k x modes with the real run values (unknown at matrix time — the k=5/one-mode floor is the honest projection); putting the explainers on the dashboard too (clutters the data view); a global PM-2 allocation change (the operator rebalances per audit against the visible budget instead) |
| D-059 | 2026-07-04 | M15 completion (OX-2/5/6): a pure `resolveProjectStage(state)` in core maps a project's intake/matrix/run booleans to its single next action; `getProjectPipelineState` gathers the booleans in one pass and a `ProjectNextAction` banner renders in the project layout (below the subnav) on every stage page. `GLOSSARY_TERMS` + a `GlossaryTerm` `<abbr>` component give operator jargon (cell, rep, engine-mode, grounded, eligible) inline definitions, applied on the matrix and run-creation surfaces. `pnpm demo:walkthrough` drives the seeded demo project through an approved matrix + a completed MOCK run + metrics + report so every view is walkable at $0 (idempotent — reuses an existing completed mock run) | The pilot showed the app's own features were undiscoverable (OX-2), the vocabulary was opaque to a non-engineer operator (OX-6), and a cold demo project had empty dashboards/reports (OX-5) — all three block a confident demo | A per-page next-action widget (drifts across pages; the layout is the single home); a heavy tooltip library (a native `<abbr title>` is enough and keyboard/screen-reader friendly); shipping the demo run as seed fixtures (a real mock run through the actual pipeline is honest "real-shaped" data, and stays correct as extraction/metrics evolve) |
| D-060 | 2026-07-04 | (renumbered from a duplicate D-054; prompt-frame rule owns D-054) pnpm build-script allowlisting lives in `pnpm-workspace.yaml`; the stale `package.json` `pnpm.onlyBuiltDependencies` block is removed because pnpm 11 ignores it. The allowlist explicitly permits `@embedded-postgres/darwin-arm64`, `esbuild`, `sharp`, and `unrs-resolver` install scripts | During M11 verification, pnpm 11 recreated `node_modules`, ignored the old package field, generated placeholder allowBuilds, and then failed installs with `ERR_PNPM_IGNORED_BUILDS`. Checking in the workspace-level allowlist makes installs/test runs repeatable on the current toolchain | Running `pnpm approve-builds` interactively (not reproducible in CI/agents); allowing all build scripts globally (too broad); leaving the ignored package field and accepting broken installs |
| D-061 | 2026-07-04 | (renumbered from a duplicate D-055; pillar visual system owns D-055) M12 report evidence excerpts are deterministic: each finding cites the first matching D-014 eligible raw response by response id, falling back to the first eligible response only when the finding is aggregate-only. Generated report aggregate claims carry inline n/provider set/mode set/run date; dashboard metric drill-through uses the same eligible-sample definition and labels each response's numerator/denominator role. No migration | Regenerated reports must stay byte-stable (D-033) while making every claim traceable to stored raw responses (C-3). Sorting by response id gives stable evidence without storing derived excerpt snapshots; using D-014 keeps report, dashboard, and exports on one sample definition | LLM-selected excerpts (non-deterministic and scope creep); random/latest excerpts (report churn); storing appendix rows in a new table (migration for disposable presentation data); showing metric drill-through over all responses instead of eligible samples |
| D-062 | 2026-07-04 | Second independent-audit round, six findings verified-then-fixed. (1 High) The live extractor received raw text + tracked brands + fact claims but NOT the project's desired-attribute list, while metrics exact-match `brands[].attributes` against canonical names — so live synonym/case drift silently under-counted attribute association. Fix: pass the attribute list into the extraction prompt (copy verbatim, never rephrase) AND, as a deterministic backstop, `mapAttributesToCanonical` normalizes every extracted phrase to its canonical name (or drops it) before persist — the SM-4 pattern applied to attributes. (2 Med) Attribute "view evidence" opened a generic scope drilldown; now each attribute renders a clickable chip that drills `attribute_<name>` through the TP-4 metric machinery, so the panel shows the metric's actual denominator. (3 Med) The planted-attribute exclusion used raw `String.includes`, over-excluding short attributes (`AI`, `POS`) inside unrelated words; replaced with a shared `containsPhrase` word-boundary matcher (PM-9's regex) in both recompute and drill-through. (4 Med) Section regenerate cleared `editedMd` but left stale `generatedMd` on screen while exports had the new markdown; the action now returns the fresh markdown and the client updates `generatedMd`. (5 Low) Migration 0007 now dedupes `(extraction_id, brand_id)` before creating the unique index, so a DB with historical duplicates can't block deploy. (6 Low) Renumbered two duplicate decision IDs (a second D-054/D-055) to D-060/D-061 and removed a redundant duplicate D-026 — all 61 IDs now unique | Same audit discipline as D-038/D-045/D-057: each finding reproduced against the running code before fixing. The attribute-drift one is the load-bearing fix — it silently made live positioning gaps look worse than reality, undermining the statistical-honesty differentiator on exactly the pillar (Perception) it most matters for | Trusting the prompt constraint alone for attributes (models drift — the canonical map is the deterministic guarantee); substring matching (the reported over-exclusion); a full page reload after regenerate (the action already had the markdown to return); editing an applied migration destructively (dedup is additive and the branch is undeployed); leaving duplicate decision IDs (ambiguous canonical log for future agents) |

| D-063 | 2026-07-05 | Product restructured as Resonance: a funnel-organized umbrella (Upper = Presence, Mid = Position + Perception, Proof = trust rail, Lower = synthetic-panel simulation, value-add = study template packs) over the Parallax measurement engine. Funnel is a PRESENTATION layer (D-051 pattern — additive core mapping, no intent/metric/pillar renames, no data migration for it). Scope through M20 is an internal tool for testing and demos: existing shared-password login stays; multi-user, client portals, payments, and any SaaS surface are post-PoC. Repo, package, and code identifiers keep the Parallax name; only product-facing copy says Resonance. Specified as PRD 8.19-8.22 + M16-M20; execution detail split into `RESONANCE_BUILD_PLAN.md` (per §7's cadence rule); the interim `RESONANCE_MVP.md` proposal is superseded and removed | Operator decision after productization research (PyMC SSR paper arXiv:2510.08338, OSS landscape, competitor gap scan): the uncontested niche is simulation conditioned on measured AI-channel evidence; a funnel narrative resolves the "GEO tool or research tool?" two-category confusion; venture-vs-consulting identity deliberately left open — internal PoC serves both | Standalone synthetic-research product (competes with the paper's authors bare-handed, abandons the audit moat); renaming repo/code identifiers (churn, zero behavior gain); labeling the whole existing audit "upper funnel" (factually wrong — the matrix is bottom-funnel weighted; funnel labels map from pillars instead) |
| D-064 | 2026-07-05 | Lower-funnel architecture: maximal reuse through a `kind` discriminator. Migration 0008 adds `resonance_studies` + `resonance_stimuli`, `matrix_versions.kind` ('audit' default / 'resonance') + `resonance_study_id`, `prompt_cells.stimulus_id` + `panel_persona_key`, and relaxes `prompt_cells.persona_id`/`market_id` to nullable. A study (panel personas x stimuli, one baseline) compiles into a frozen resonance matrix version (C-1/C-4 apply; PM-2/PM-8/PM-9/archetype logic bypassed — stimuli legitimately contain brand names); runs/jobs/responses reuse the existing worker, cost guards, breaker, and C-9 mock discipline unchanged. SSR scores are versioned `extractions` rows (`schema_version='ssr-v1'`, PMF in `extracted_json`, re-score = new version per C-3); anchor statement sets are versioned repo fixtures pinned per study at approval (editing = new version, loader refuses unknown versions). Embeddings are a separate `EmbeddingProvider` capability (`EMBEDDING_PROVIDER` env, OpenAI `text-embedding-3-small` first — DeepSeek has no embeddings endpoint), resolved worker-side per the D-035 split, with spend attributed to the embedding provider in projection, per-run cap, and daily budgets (D-022/D-044 pattern). Mock resonance runs are fixture-backed end-to-end (D-016 hash keying for responses, hand-authored fixture PMFs for scoring) — no live embedding calls in mock or CI. Metrics recompute dispatches on matrix kind into `resonance_variant` / `resonance_variant_persona` / `resonance_delta` scopes (C-5 delete-then-rebuild; means are point estimates, ci columns null per D-023) | A parallel pipeline would re-earn ten milestones of hardened worker/cost/immutability bugs; extraction versioning already models "re-score under a new method version"; the D-022 lesson (invisible second layer of paid calls) applies verbatim to embeddings | Parallel resonance_runs/jobs tables (duplicates the worker); widening `LLMProvider` with embed() (display registry would carry a paid capability, against D-035); storing PMFs in a new table (extractions already version per response); DB-stored anchors (fixtures + version string preserve golden-test determinism); pointing extraction-style scoring at each generation provider (one scoring engine, D-041's logic) |
| D-065 | 2026-07-05 | Simulation guardrails codified as hard constraints C-12 (measured/simulated wall + SIMULATED badge), C-13 (evidence-conditioned by default, GENERIC label otherwise), C-14 (comparative claims only; validated persona axes only; forbidden-phrase enforcement). The value-add layer ships as four study TEMPLATE PACKS (AI-framing repair, promo framing, price presentation, message/claim variants) framed strictly as test-before-you-spend decision support; location/footfall and absolute willingness-to-pay use cases are parked post-PoC pending a calibration anchor, per the validity gradient (relative > absolute, discourse-rich > niche, text > physical, age/income > identity) | The Aaru/EY critique ("survey-replication correlation is not behavior prediction") is the exact attack surface; the product's founding law (never promise guaranteed AI rankings) extends naturally to never promising simulated ROI; packs-not-machinery keeps M20 small | A free-form "optimization" module promising business outcomes (credibility landmine, unbounded scope); shipping location/pop-up simulation v1 (weakest validity axis, strongest incumbents); gender/ethnicity persona conditioning (paper shows LLMs don't replicate those subgroups reliably) |
| D-066 | 2026-07-05 | Operator doc-review round on the M16-M20 spec, verified-then-fixed (D-045 discipline; corrects two details in D-064 without rewriting it). (1) CONFIRMED against migration 0000: `extractions.schema_version` is `integer default 1 not null` — SSR rows keep the integer column and are discriminated by `extracted_json.kind='ssr'` with `ssrVersion:'ssr-v1'` inside the json; never a string in schema_version. (2) CONFIRMED: `intent` is a Postgres enum — migration 0008 must `ALTER TYPE "public"."intent" ADD VALUE 'simulation'` (0004's `provider_down` is the precedent; a just-added enum value cannot be USED in the adding transaction — 0008 only adds it). (3) BONUS finding: `prompt_cells.persona_id`/`market_id` are ALREADY nullable in 0000 — D-064's "relaxes to nullable" step is unnecessary; one build-plan VERIFY item resolved. (4) Persona-axis precision: only age and income are paper-validated; location/behavioral profile demoted to prompt context (C-14 reworded). (5) Anchor sets: target 6 (paper's main setup), >=4 floor; `purchase_intent.v1` carries `calibrated:false` and the report method section must say "uncalibrated" until a human-benchmark validation exists. (6) ΔPI framing pinned: Likert-scale survey-construct mean shift, never purchase probability. (7) Text-only-stimulus limitation made explicit in the report method section (the paper's image setting performed mildly better). Point 5 of the operator's feedback (no invented confidence intervals) required no change — D-023/RS-8 already hold that line | Same verify-then-fix discipline as D-045/D-057/D-062: both schema claims were checked against migration 0000 before agreeing; the methodology points align the docs with what arXiv:2510.08338 actually validated rather than what the OSS skill's looser copy claims | Migrating schema_version to text (pointless churn against an integer default that audit rows rely on); reusing an existing intent value like 'validation' for resonance cells (poisons D-054 frame semantics); presenting location/behavioral profile as validated segmentation (overclaims the paper); inheriting the OSS skill's confidence-interval and price-sweet-spot language (exactly the variance theater D-023 exists to prevent) |
| D-067 | 2026-07-05 | A resonance run selects exactly ONE generation engine-mode (one provider x one mode), enforced server-side in `createRun` for resonance matrices (D-038 pattern) with a single-choice run-form control. Rationale: resonance metric scopes carry no provider dimension, and each model is a DISTINCT synthetic population — the SSR paper reports GPT-4o and Gemini separately with different correlation attainment; pooling engines into one PMF would silently merge two populations, the C-12 failure mode in miniature. Cross-engine comparison is done as separate runs viewed side by side; a provider-scoped resonance dimension is post-PoC if demanded. The audit engine is UNAFFECTED: multi-provider x multi-mode audit runs remain fully supported (form toggles, cartesian planning, per-provider budgets/scopes/degradation) — the DeepSeek-only trial was a key-availability fact, not a code limit | Surfaced by an operator question ("did we build multi-model?"): audit yes end-to-end; the resonance spec inherited the multi-select form without a provider dimension in its scopes — a silent-pooling hole in the day-old spec | Adding provider to resonance scope keys now (scope explosion for an unproven need; single-engine runs answer it); allowing multi-engine with a pooled-population label (a label doesn't fix incomparable ΔPI baselines); restricting the audit side too (nothing wrong with it) |
| D-068 | 2026-07-05 | Pre-build review rulings (operator's code-level review, every finding verified against source before agreeing). (1) The C-12 wall extends to shared UI plumbing IN M17, not M19 — `listCompletedRuns`, `generateReportForRun` + all export/print routes, and `getProjectPipelineState` are all kind-blind today, so the first completed resonance run would leak into audit selectors, let an audit-shaped report render over SSR data, and flip the OX-2 banner; M17 gains step 6b (kind filters, rejection guards, audit-only pipeline booleans) with QA-gate checks. (2) Type model: `Intent` keeps its name and five audit values everywhere (allocator/PM-9/frames keep compiling against it); new `CellIntent = Intent \| "simulation"` for cell parse/display; narrowing only via a shared `isAuditIntent` guard — the union split is the wall, renaming to `AuditIntent` stays optional cosmetics. (3) DB table `audit_runs` keeps its name (renaming = migration churn, zero behavior); kind-aware UI copy says "run" + SIM badge, audit-only surfaces may say "audit run". (4) M16 proceeds first unblocked (presentation-only); the P1 guards are M17 blockers. (5) Study-builder evidence excerpts are model-origin: any markdown preview escapes via `src/core/md.ts` at insertion (D-040 discipline); plain textarea exempt. Also fixed ENGINEERING_SPEC's leftover "validated axes only" persona wording (D-066 consistency) | The kind-discriminator reuse strategy (D-064) concentrates risk exactly here: every query written before `kind` existed is implicitly audit-assuming; the review enumerated the three that face the operator first | Deferring guards to M19 with "resonance reports don't exist yet" reasoning (the leak is audit surfaces INGESTING resonance runs, which exist from M17); widening `Intent` with a sixth value (every exhaustive switch in allocator/frame code would need a case, inviting a default-case hole); renaming `audit_runs` (churn) |
| D-069 | 2026-07-05 | M18 implementation rulings. (1) SSR anchor fixtures are stored as five Likert score buckets with equal sentence counts and transposed into statement sets at scoring time; duplicate score buckets are now rejected. (2) The `(1+cos)/2` rescale is verified at the similarity-matrix boundary, not by expecting a different final PMF, because min-subtraction normalization makes positive affine rescaling PMF-invariant; the golden test instead catches skipped min-subtraction. (3) Resonance metrics use the existing `metrics.metadata_json` column for PMF/stimulus/baseline metadata, so M18 needs no migration. (4) Live resonance cost uses `EMBEDDING_PROVIDER` while audit extraction cost remains attributed to `EXTRACTION_PROVIDER`; `getProviderSpendToday` splits extraction rows by `extracted_json.kind='ssr'`. (5) Settings surfaces the effective embedding engine as read-only deploy config and requires its credential for live Resonance runs | M18 exposed two places where the docs' shorthand could mislead implementers: fixture shape ("six sets") and the rescale test expectation. The DB already had `metadata_json`, so adding a table/column would violate the no-migration plan for no behavioral gain. Spend attribution must preserve D-022's lesson without charging audit extraction to the embedding provider or SSR scoring to the extraction engine | Storing anchors as six duplicate score rows (ambiguous and hard to validate); trying to force resonance metrics into the Four P audit glossary before M19 surfaces define lower-funnel display language; adding a new metric metadata table; pooling SSR spend into generation providers |
| D-070 | 2026-07-05 | Evidence archives prefer native `pg_dump --format=custom`, but missing local Postgres client tools no longer make local archive verification fail by default. `pnpm archive:evidence` now falls back to a marked SQL data snapshot of all public tables, written through the existing DB connection, plus a dump manifest; it exits 0 after the EX-3 bundle and fallback snapshot are written. `ARCHIVE_REQUIRE_PG_DUMP=true` restores the old strict behavior and exits 2 if the native dump is unavailable | M19 requires resonance archives to work before milestone close, and local developer machines may have a reachable database without `pg_dump` on PATH. Failing after the evidence bundle was written made the Resonance path look broken even though the data contract was sound. The fallback preserves an evidence-grade data snapshot while keeping native custom dumps as the production preference | Installing Postgres client tools as a hidden prerequisite for every local verification (fragile and environment-specific); silently skipping the dump while exiting 0 (would weaken D-024); replacing pg_dump entirely (native custom dumps remain more portable and complete) |
| D-071 | 2026-07-05 | Post-M20 multi-agent code review (xhigh, 10 finder angles): 15 verified findings fixed, verify-then-fix (D-045). Correctness: (C-4) resonance stimulus add/update/delete now guard study `draft` state — server actions are RPC endpoints so the UI-disabled form was never the freeze; an approved study's stimuli/evidence were silently rewritable. (C-13) template `measured_ai` studies were unapprovable behind a generic Next error — added a proactive per-stimulus evidence warning. Mock SSR scoring on an unmapped fixture stranded the extraction `pending` forever (stale sweep re-created a pending row each cycle) — now dead-letters immediately. (OX-2) `resolveProjectStage` ignored the resonance pipeline booleans (banner blind to the lower funnel) — now routes there. (D-022) a billed-but-malformed embedding dropped its cost — shape validation moved from the OpenAI embeddings adapter into `liveSsrScore`, which records attempt cost BEFORE validating (mirrors the audit extraction path). `deleteStimulusAction` now try/catches. (C-12) in-app resonance report renders `SimulatedBadge` (print already did via markdown). `metrics.ts readSsrPayload` now rejects all-zero PMFs to match the results reader. Mock resonance detection uses a shared `RESONANCE_PROMPT_MARKER`. Cost projection uses the study's pinned anchor set (D-066). `getExportExtractions` ordered for a deterministic report `embeddingModel` (D-061). Structure: kind→secondary-engine + env-default provider ids consolidated into a leaf `provider-ids.ts` (`secondaryProviderIdForKind`, breaking the budget↔runner cycle), replacing 5 ternaries; createRun resolves matrix kind once (was 3 fetches); one `reportSectionsForKind`/`csvDatasetsForKind` selector replaces 5 duplicated ternaries; one shared C-14 forbidden-phrase list; resonance `pi_mean` routed through core `meanValue`; metric inserts batched. Regression tests added (C-4 freeze, OX-2 resonance stages). No migration. Gates: 300 tests, lint, typecheck, build, mock-e2e 6/6, demo:resonance $0, F7 badge verified in-app + absent on audit report | 10-angle recall-mode review over the whole M16-M20 diff; bugs clustered at edges the milestone gates don't walk — server-action error handling, UI state guards, template-pack first-use — while the pipeline core (worker, cost guards, C-12 metric wall, SSR math, escaping) came through clean; conventions angle found zero violations | Widening the embedding interface for the cost fix (moving validation to the single caller mirrors the audit path); a UI-only disable for the C-4 stimulus guard (the RPC hole is the bug); any DB migration (all fixes are code-level); renaming `Intent`/`audit_runs` (unchanged, D-068) |

| D-072 | 2026-07-06 | Post-M20 hardening pass (a large working-tree delta) reviewed, corrected, documented, and accepted. ACCEPTED and kept: migration 0009 (two CHECK constraints making the audit/resonance cell-shape invariant a DB guarantee — `matrix_versions` kind↔resonance_study_id, `prompt_cells` simulation-vs-audit column shape; verified clean against all historical rows); route `isUuid` param validation; `isReportableRunState` gating exports/reports to `completed` while the report run-selector excludes paused (`includePaused: false`) and the archive script remains the paused/PARTIAL delivery path (coherent split — the report UI is completed-only, evidence archive is completed+paused); evidence archives now redact server-only provider credentials; a global distributed-brute-force rate-limit bucket (kept, not rejected — a rotated-IP attack on an internet-exposed shared-password login is a real threat; C-11 context); worker pre-provider-call spend guard + resume-time cap/budget blocking; `MIN_API_KEY_LENGTH` before `apiKeyLast4`; extraction progress counting latest-version-per-response; up-front rejection of unsupported grounded provider/mode pairs; a `superRefine` duplicate-`panelPersonaKey` guard; `validateSecondaryProviderConfig` (non-throwing, mock-gated) fixing the earlier mock-crash-on-bad-env finding; ~140 new tests. FIXED in review (tree was RED with the DB up — prior validation ran DB-down and skipped the DB-backed tests): (1) `getResponseDetail` gated `kind='audit'` but not run state, so it surfaced a running run's response while sibling loaders returned null — added the `completed|paused` state gate to match `getRunForDashboard` (the delta's own wall test caught it). (2) `resumeRun` reused `checkCostCap` (`<=`, correct for creation-time projection) for the resume guard, so a run sitting exactly AT its cap resumed and spent past it — resume now uses `>=` (already reached). (3) global rate-limit bucket was cleared on every successful login, so the operator's routine logins reset a distributed attacker's accumulated count — `clearFailures` now clears only the caller's bucket (the global bucket self-prunes on its 15-min window); its test was rewritten to assert the global bucket PERSISTS. (4) the embedding model precedence deliberately omits D-020's per-credential `default_model` (a shared OpenAI credential's default_model is a chat model, invalid at /v1/embeddings) — accepted as correct and documented in code rather than "fixed". REJECTED: nothing removed — each hardening item was evaluated for scope-creep and consciously kept with rationale above | The delta correctly targeted the D-071 finding themes (edges the gates don't walk) and added real defense-in-depth, but shipped two incomplete guards and one self-defeating security control under green tests, because validation ran with the DB down (96 DB-backed tests skipped). Verify-then-fix (D-045): re-ran every gate with the DB up, found the two RED tests, fixed the code (never the assertion) except the one test that enshrined the self-clear bug | Ripping out the global rate-limit bucket as single-operator scope creep (the distributed-attack threat is real for a Render-exposed shared password); loosening `isReportableRunState` to include paused (the selector+archive split already handles PARTIAL delivery coherently); "fixing" the embedding model precedence to honor D-020 (a chat default_model at /v1/embeddings is the actual error); weakening `checkCostCap` to `<` (it is correct for projection — only the resume guard needed `>=`) |

| D-073 | 2026-07-06 | Worker visibility + a test-isolation hazard. FIX: a run started in the UI does nothing until the polling worker is running (D-001's two-entrypoint model); locally `pnpm dev` was app-only, so a queued run sat at 0/N with no signal. Added `pnpm dev:all` (app + worker in one command via `scripts/dev-all.sh`, Ctrl-C stops both) mirroring Render's two always-on services; `pnpm dev` stays app-only because the preview harness spawns it and must not start a worker. Added a WORKER OFFLINE banner on the run page: `getRunDetail` now returns `workerOffline` from the RN-9 heartbeat (`isWorkerLikelyOffline` in `core/worker-timing.ts`, shared with Debug's staleness constant), so a queued/running run with no recent heartbeat reads as "worker offline — start pnpm dev:all/pnpm worker" and clears live once a heartbeat arrives. HAZARD (found the hard way): the DB-backed test suite is DESTRUCTIVE against a dev DB that holds real queued runs — several tests call `claimJobs(provider)`, which claims the oldest queued job for that provider ACROSS ALL RUNS, so running `pnpm test` while a live run was queued claimed/locked its jobs and injected test data (no real spend — provider stubbed). Restored the affected run (jobs → queued, test-injected responses removed, run → queued, $0). RULE: never run `pnpm test` against a dev DB with real pending runs; the suite needs a dedicated/ephemeral test DB or per-run-scoped claims (follow-up) | The "hit Run, nothing happens" report was the worker not running, not a pipeline bug; but the deeper defect is that the UI gave zero signal (the RN-9 heartbeat existed only in Debug) — the same terminal-gate-no-earlier-signal shape as PM-9/D-050. The destructive-test discovery is a real operational footgun that just corrupted a live run mid-session | Merging app+worker into `pnpm dev` (breaks the preview harness which spawns `pnpm dev` for UI-only work, and the Render two-service model); running the pipeline inline on the Run request (115 resumable cost-capped paid calls must not run in a request — the job/worker queue is correct); deleting the user's run instead of restoring it |

| D-074 | 2026-07-06 | Dashboard self-heals stale metrics. Symptom: a completed live audit showed an almost-empty dashboard — Presence `mention_rate` n=2 against 40 eligible unbranded samples, `avg_first_position` n=0. Not a pipeline failure: 115 responses, 115 valid extractions, prompts clean (no PM-9 contamination). Root cause: metrics are built ON DEMAND (D-044 deliberately keeps the worker OUT of analysis), and these were computed mid-extraction (when only 2 of 40 unbranded had landed) then never refreshed; the dashboard read stored rows via `listMetrics` without ever recomputing — the one metric surface that didn't (reports/exports/manual-button all do). FIX: `areMetricsStale(runId)` (latest extraction created_at > latest metric computed_at, or extractions-but-no-metrics) and `fetchDashboardData` recomputes when stale before reading (C-5 idempotent, gated so a fresh run recomputes once then reads cached). The analysis surface owns metric freshness, still no worker↔analysis coupling. The operator's affected run was recomputed on the spot (n=2→40). Regression test added | Metrics-on-demand (D-044) left a gap: nothing refreshed metrics after completion for the dashboard, so every live run read stale/empty until a report or manual recompute fired — the operator saw an empty dashboard with no signal to recompute | Recompute-on-completion in the worker (D-044's rejected worker↔analysis coupling); recompute on EVERY dashboard view unconditionally (wasteful — the staleness gate makes it fire once per new extraction wave); a "metrics stale, click recompute" banner (makes the operator do the idempotent work the surface can just do) |

| D-075 | 2026-07-06 | Resonance study builder is a step-by-step wizard, not a flat form. Operator QA: the builder dumped every field on one page in pipeline vocabulary (`measured_ai`, "GENERIC unconditioned", pipe-delimited `label | age | income | location | profile` personas) with no ordering or required markers — while the audit intake is a 7-step wizard and the dashboard uses numbered `01/02` dossier sections. New `StudyWizard` client component (`src/components/resonance/study-wizard.tsx`): 4 steps with a progress rail — 01 Name, 02 Who reacts (structured per-buyer rows: label/age/income/location/habits, replacing the pipe format), 03 What they react to (framings with plain-language kind labels via `KIND_META` + inline help, evidence pickers, the generic toggle explained in plain words), 04 Review & run (readiness checklist in plain language, Approve & simulate gated on it). It DRIVES the existing returning server actions (`updateStudyAction`/`addStimulusAction`/`updateStimulusAction`/`deleteStimulusAction`/`approveStudyAction`), so the C-4 draft-freeze and C-13 evidence guards are reused, not reimplemented; `router.refresh()` reconciles stimulus rows while name/panel/step stay in client state. Approved studies render results-only. HAZARD (repeat of the D-073 class): running `pnpm build` while the operator's `pnpm dev` was up shared/corrupted `.next` and wedged the dev server — never build against a running dev server; verified via typecheck+lint+build instead | The builder never got the wizard treatment the rest of the app has; the flat form + raw enums were the actual UX defect the operator hit | A true multi-step wizard was chosen by the operator over numbered single-page sections or labels-only; new object-typed server actions (reused the existing FormData actions from the client instead, keeping the guards single-sourced); a DB-backed component test (client wizard over already-tested actions — manual QA is the fit, like the rest of the UI surfaces) |
| D-076 | 2026-07-06 | Consistent operator-facing error handling, three durable conventions, no migration. (1) BOUNDARIES: App Router `error.tsx` (broad product fallback), `global-error.tsx` (root-layout fault, renders its own `<html>/<body>` + globals.css, prod-only — the dev overlay pre-empts it), and `projects/[id]/error.tsx` (project-scoped, keeps back-to-project + all-projects navigation) all render one shared `ErrorFallback` (`src/components/error-fallback.tsx`) matching `not-found.tsx`: dossier framing, `--danger` error stamp, accent reserved for the Retry action (V-2), Next `error.digest` shown for support correlation, never a stack/secret/provider payload. Boundaries are kind-AGNOSTIC — audit and resonance share the fallback (no C-12 chrome-switching there). (2) OBSERVABILITY SEAM: `reportError(error, context)` in a leaf `src/observability.ts` (NOT core — it has a console.error side effect, the same sink `src/worker/index.ts` already uses; C-7 keeps core pure), import-safe in both server routes and client components, never throws even if the logger throws; it is the single swap-in point for Sentry later (not wired this sprint — no SaaS dep added, D-045 discipline). (3) CLIENT ASYNC RECOVERY: every fire-and-forget `.then` on a data path gets `.catch`/try — a failed dashboard run-switch keeps the previously loaded run's data on screen with an inline Retry (never blanks), a failed run-progress poll keeps the last detail and lets the interval retry, a failed pause/resume/cancel/recompute shows an inline error and re-enables its button (no stuck spinner), a failed drilldown shows an in-panel Retry/Close, and a failed cost projection surfaces a notice instead of silently dropping the preview (the server re-checks cost cap + daily budgets before any spend regardless). Export routes (markdown/json/csv) wrap only the POST-VALIDATION body in try/catch → a sanitized `{ error: "export failed" }` 500 (the id/run/state guards still return their exact 400/404/409, D-072); CSV `[dataset]` validation was already solid (allowlist + null→404) and was left unchanged. DELIBERATELY NOT DONE: no repo-wide `ActionResult` type sweep (the ad-hoc `{ ok, error }` shapes already work — high churn, ~zero behavior gain; add opportunistically); no export-validation rework (D-072); no Sentry. Tests are DB-free (a pure `reportError` suite + one sanitized-500 test per export route); the boundaries/inline states follow this project's manual-UI-QA convention (environment:"node", no testing-library/jsdom; D-075) | The operator could land on a generic Next error page with no path back and no retry, and several client async paths (dashboard/run-progress/extraction/drilldown/cost-projection) had no `.catch` — a transient failure blanked the view or dropped state with no signal, the same terminal-gate-no-earlier-signal shape as PM-9/D-050/D-073 | A shared ActionResult refactor (churn without behavior change — the shapes work); testing-library + jsdom for component tests (new deps against a project that manually QAs UI); relying on `error.message` in the fallback (Next redacts it to a generic string + digest in production — the digest is what the operator actually gets); putting `reportError` in `src/core` (a side-effecting logger violates C-7's pure-core rule); a fifth try/catch reworking export id/state validation (D-072 already shipped it) |
| D-077 | 2026-07-07 | M21: product copy reorganized around two named epistemic layers — Evidence Layer (audit; the Four P pillars live inside it, Proof stays the trust rail) and Simulation Layer (resonance studies) — resolving the three-taxonomy overlap (pillars / funnel stages / pipeline vocabulary) and the "Resonance" umbrella-vs-sub-tab naming collision. Presentation-only (D-063 precedent): `src/core/funnel.ts`'s label strings and `funnelStampForPillar` rewritten (audit pillars stamp `EVIDENCE LAYER`, proof keeps `TRUST RAIL` unchanged, the dormant `lower` stage label now leads with "Simulation Layer"); the two render sites (`components/semantic/pillar.tsx`, `components/matrix/board.tsx`) needed no code change since they consume the rewritten strings — `board.tsx`'s own `"TRUST RAIL · "` literal is untouched by design. The "Resonance" collision fixed at the subnav tab, the simulation page's h1/empty-state/breadcrumb/results heading, the print + markdown report titles, the report's own `RESONANCE_REPORT_SECTIONS` titles and method-section body text, the SIMULATED badge line, the Settings embedding-engine caption, and the run-form's D-067 caption — all renamed to "Simulation"; the umbrella brand (`layout.tsx` title, `login` h1, `nav.tsx` mark) is explicitly untouched. The glossary's `"funnel stage"` term is redefined around the two-layer split (grepped first — its key is never referenced via `<GlossaryTerm term=...>`, so renaming its definition carries no render risk). No intent/pillar/metric rename, no schema change, no migration; `semantic.ts`'s `PILLARS`/`METRIC_GLOSSARY` constants carry zero diff (only `GLOSSARY_TERMS` strings changed). `LAYERS_AND_EVIDENCE_ONLY_EVALUATION.md` and `M21_M26_BUILD_PLAN.md` are adopted as planning canon alongside this entry (§7 split-file precedent) | A first-time client met three competing vocabularies (pillars, funnel stages, pipeline terms) and saw "Resonance" mean both the whole product and one sub-tab — the epistemic wall C-12 already enforces in code was invisible in the chrome | Renaming the intent taxonomy or `FunnelStage`/upper-mid-lower internal values (breaks D-054 frames + golden data, or churns non-rendered internals for zero gain); a metaphor scheme like Observatory/Lab (needs a legend, risks overpromising); keeping funnel-stage wording (jargon redundant with the Four P pillars); renaming the `resonance` route segment (link/bookmark churn for a copy-only milestone) |
| D-078 | 2026-07-08 | M22, two parts, no migration. PART A (test-DB isolation, closes the D-073 hazard for good): `pnpm test` now runs every DB-backed test against a fresh, ephemeral, embedded-Postgres instance on :5433 — never the shared dev DB on :5432. `scripts/test-db.ts` boots it (via the same `embedded-postgres` dependency `scripts/dev-db.ts` already uses; the darwin dylib-symlink fix was extracted to a shared `scripts/lib/pg-dylib-fix.ts`), migrates it (drizzle's programmatic `migrate()`, not the CLI), and seeds it (`pnpm db:seed` as a child process so it never shares a pool/module cache with the caller). Wired in via vitest's `globalSetup` (`scripts/vitest-global-setup.ts`), chosen over a separate `pnpm test:db` wrapper because it makes the existing `pnpm test`/CI command isolated with zero new steps — `pnpm test:db` is ALSO added as a standalone foreground command (mirrors `pnpm db:dev`'s UX) for manual poking. Fails closed: any startup error still overrides `DATABASE_URL` to a guaranteed-unreachable string rather than leaving it unset, so a broken embedded-PG can never silently fall through to the dev DB's hardcoded fallback — `describe.skipIf(!dbUp)` degrades exactly as it does with no Postgres installed at all. `pnpm-workspace.yaml` gains `@embedded-postgres/linux-x64` in `allowBuilds` (D-060 precedent) since CI (ubuntu-latest) now also boots one. Two DB-backed tests (`dashboard/spotcheck.test.ts`, `report/service.test.ts`) previously hard-failed (not skipped) if the `m4-e2e` fixture project was missing — since that fixture only exists after a manual `pnpm test:mock-e2e` run, not from migrate+seed alone, both were converted to the `!dbUp || !fixture` skip idiom already used elsewhere (`matrix/actions.test.ts`'s `!demoProjectId`) rather than adding `test:mock-e2e` (a slow, worker-spawning integration script) to every `pnpm test` invocation. PROVEN: full suite green twice consecutively (63/65 files, 439/451 tests, identical both runs); dev DB row counts (`audit_runs`, `resonance_studies`, `jobs`, `responses`, `projects`) byte-identical before/after. PART B (evidence-only, kill GENERIC — tightens C-13 from default-with-toggle to a hard rule): `approveAndCompileResonanceStudy`'s approval guard (`src/db/repositories/resonance.ts`) no longer consults `genericUnconditioned` at all — a measured_ai stimulus with real evidence is now unconditionally required, with no flag able to bypass it. The wizard's toggle UI and its RPC-reachable FormData field (`modules/resonance/actions.ts`'s `updateStudyAction`) are removed — closing the C-4-shaped hole where a UI-disabled control is not a guard on its own. A pack-created draft lacking a measured_ai baseline (price_presentation, promo_framing) now shows a plain-language readiness requirement ("Attach a Measured AI framing with at least one real audit response as evidence before approval") instead of a raw server error, satisfying the operator's explicit override that these packs must not become unapprovable dead weight ahead of M23's full coverage contract. OVERRIDING the build-plan/eval's own recommendation: historical rendering is KEPT, not collapsed — `resonanceExportLabel`'s two-value label, the report's GENERIC badge branch, the results-page and studies-list Stamps, and the CSV `genericUnconditioned` column all still read the column on EXISTING rows, so the dev DB's one real approved GENERIC study ("weekday lunch $1 off") keeps rendering truthfully; the dev DB itself is never reseeded or touched (operator's real runs and studies are live data, not disposable fixtures). Three DB tests that used `genericUnconditioned: true` purely as an approval-bypass convenience (not to test the bypass itself) were given a real completed-audit-run evidence fixture instead (`createCompletedEvidenceResponseId` helper in `service.test.ts`); the two genuine GENERIC-allowed assertions were inverted to prove the flag is now dormant for approval; two other `genericUnconditioned: true` sites (`wall.test.ts`, `budget.test.ts`) were verified to be unrelated raw-insert fixtures for C-12/budget tests, left untouched and commented. `src/core/resonance.test.ts`'s existing label-helper test already served as the historical-rendering regression and is now annotated as such. VERIFIED: lint/typecheck/build clean; C-14 forbidden-phrase tests green; grep confirms no orphaned "generic option"/"Generic study" toggle UI copy remains | Part A: the D-073 hazard (a destructive DB-backed test suite corrupting a live dev DB mid-session) needed a structural fix, not a repeated warning — a shared, always-on dev Postgres and a test suite that inserts/deletes/claims jobs across all runs are fundamentally incompatible. Part B: the GENERIC escape hatch was a real C-13 violation surface — any operator (or, post-D-071, any RPC caller) could produce an unconditioned simulation study and have it treated as evidence-grade by every downstream surface that didn't specifically check the flag | Per-run-scoped `claimJobs` filtering instead of full isolation (only narrows the collision, DB-backed tests still write/delete against shared data — evaluated and rejected in favor of total isolation); running `pnpm test:mock-e2e`/`pnpm demo:resonance` inside global setup for full fixture parity (adds tens of seconds and a worker-subprocess-kill dependency to every local test run for two test files' worth of coverage); collapsing `resonanceExportLabel` to one value and dropping the report's GENERIC badge branch (the build-plan/eval's own recommendation — overridden by the lead: reseeding/relabeling away the dev DB's one real historical GENERIC study is not an acceptable trade for a pre-deploy internal tool that still holds real operator data); removing `genericUnconditioned` from `ResonanceStudyPatch`/the repository entirely (would break the historical-fixture pattern several tests legitimately use for non-approval purposes; the unconditional guard already makes the flag inert for approval regardless of whether it can still be set) |

| D-079 | 2026-07-07 | M23, no migration. Coverage contract: a pure `FrameAspect` mapping in `src/core/prompt-templates.ts` (`presence, positioning, perception_attributes, factual_claims, pricing, promotions`) is the producer side of the Evidence-Layer -> Simulation-Layer contract (LAYERS_AND_EVIDENCE_ONLY_EVALUATION.md synthesis section). Every seed row's aspect defaults from its intent (`DEFAULT_FRAME_ASPECTS`) with an explicit per-row `frameAspects` override where a template's evidentiary content diverges from its intent's usual meaning; `frameAspectsForCell(archetype, intent, variantKey)` resolves a matrix cell back to its seed row (falling back to the intent default for an unmatched variantKey, e.g. a future operator-authored template — never throwing). `RESONANCE_STUDY_TEMPLATES` gains `requiredAspect` (the consumer side): `ai_framing_repair`→`perception_attributes`, `message_claim_variants`→`factual_claims`, `price_presentation`→`pricing`, `promo_framing`→`promotions`. `src/core/coverage.ts`'s `evaluatePackCoverage` cross-checks the two, stamped ok/gap, informational only (D-058 sample-budget-panel precedent — never a block), rendered as a new "Simulation coverage" panel on the matrix board directly under the existing sample-budget panel. Price/promo audit templates fill the verified coverage hole (LAYERS_AND_EVIDENCE_ONLY_EVALUATION.md 2c: zero of the 45 default templates mention price/cost/deal/offer/discount): six new opt-in templates (`variantKey` v4/v5, one price + one promo per archetype, living inside the existing `comparison` intent per the plan's own judgment call) seed with `active:false` so the default 40-cell allocation, golden dataset, and `pnpm test:mock-e2e` expectations are provably unchanged (`getMatrixInputs` only ever selects `active:true` rows) — opt-in is enforced structurally, not by a policy note. `scripts/seed.ts`'s idempotency existence-check was fixed to match on the natural key alone (archetype+intent+variantKey) rather than `active:true`, since an active-only check would have silently re-inserted the opt-in rows on every seed run. A matrix-board "Activate {aspect} templates" control (`activateTemplatesForAspect` repo fn + `activateCoverageAspectAction`) flips the matching opt-in rows to active for the project's archetype on an explicit operator click — archetype-scoped (prompt_templates has no per-project dimension), affecting future `generateMatrix`/`addCell` calls for every project sharing the archetype, never existing approved matrices (C-4). Both `price_presentation` and `promo_framing` packs (previously all-`custom` stimuli, unapprovable dead weight under M22/D-078's hard C-13 rule without an operator manually adding a stimulus) now ship a pre-filled `measured_ai` baseline slot ("How AI frames your pricing/current offer today") so pack-created studies prompt evidence attachment per M22's requirement. VERIFIED live against the real dev DB's ledgerfox-demo project (read-only, authenticated curl against a `next build` standalone server on a throwaway port, no worker started): the coverage panel renders `AI-framing repair: Perception attributes — 14 cells ✓` and `Message / claim variants: Factual claims — 8 cells ✓` alongside `Promo framing: Promotions — 0 cells (gap)` and `Price presentation: Pricing — 0 cells (gap)`, each gap with its Activate button — reproducing the eval's predicted gap exactly, before either opt-in pack is ever activated | The verified gap (2c) was real and structural: two of the four value-add study packs could never get real audit-side evidence, and — independently — could never even be approved once M22 made C-13 unconditional, because their own template definitions carried zero `measured_ai` stimuli. The coverage panel makes the first problem visible before a run spends money; the pre-filled baseline slot fixes the second. Opt-in via `active:false` (not a quota/allocator change) was the only mechanism that could structurally guarantee zero effect on the already-verified default-40-cell/mock-e2e/golden contracts, since `getMatrixInputs` already filters on `active:true` | Deriving frame aspect purely from intent with no per-template override (the new price/promo templates deliberately live inside `comparison`, so intent alone cannot distinguish them from a plain comparison template — this is exactly why price/promo aspects require an explicit row-level override, D-079's own worked example); a DB column for frame aspect (the plan explicitly preferred pure core code, and no operator-authored template path exists yet to force one); a true per-project template selection table (schema has no per-project dimension for `prompt_templates`; archetype-scoped activation was accepted as the mechanism the plan's own fallback wording asked for); adding a `measured_ai` baseline to `message_claim_variants` too (that pack has the identical structural gap but the pinned decision named only two packs — left as a documented pre-existing residual, not silently "fixed" beyond scope) |
| D-080 | 2026-07-07 | M24, supersedes D-067 (audit-side statements in D-067 are unaffected — audit multi-provider was always supported; only the resonance restriction is lifted), no migration. A Resonance run now accepts >=1 providers (mode stays locked to exactly 1, since resonance metric scopes carry no mode dimension): the guard in both `src/modules/runner/actions.ts`'s `projectRunCost` and `src/db/repositories/runner.ts`'s `createRun` (the PV-5 script/test backstop) now rejects only `modes.length !== 1`. D-067's core rationale (never pool distinct synthetic populations) is preserved and extended: `recomputeResonanceMetrics` (`src/db/repositories/metrics.ts`) groups eligible SSR samples by `providerId` FIRST, then computes each provider's variant means, persona slices, and ΔPI baseline entirely WITHIN that provider's own samples — a provider's delta subtracts only its OWN baseline mean, never another provider's. Scope keys carry the provider dimension: `resonance_variant`/`resonance_delta` = `<stimulusId>|<providerId>`, `resonance_variant_persona` = `<stimulusId>|<personaKey>|<providerId>`, with `providerId` also written into every row's `metadataJson`. `getResonanceStudyResults` (`src/db/repositories/resonance.ts`) parses the composite keys and returns `providerGroups: ResonanceProviderGroup[]` (one group per engine, each with its own variants/personaRows/deltas/evidence — evidence maps are keyed with the provider suffix too, so one engine's raw responses never appear under another engine's variant card) plus a `providers: string[]` list; a single-provider run yields exactly one group. The results page, `ResonanceReportContext` (now `providerSections` instead of flat arrays), and `resonance_metrics.csv` (new `providerId` column) all render one block per engine — no cross-engine comparison UI or pooled numbers anywhere, matching C-12's "never pool measured and simulated" discipline applied one level down (never pool two simulated populations either). The run-creation form's `singleEngine` prop is renamed `singleMode`; providers render as a normal multi-select for Resonance (mode stays single-choice). No change was needed to job planning, the worker, or cost/budget attribution — `createRun`'s cartesian provider x mode x cell x rep job planner, and the embedding-engine-once cost projection, were already generic across provider counts. A sanctioned one-shot dev-DB sweep (`pnpm recompute:resonance`, touches only the `metrics` table) recomputed all 7 existing dev-DB resonance runs to the new key format; a second pass was byte-identical for every run, and audit-run metrics rows (14,318 rows / 43 runs) were numerically and per-run untouched | D-067 forced exactly one provider because resonance metric scopes had no provider dimension — pooling two engines' PMFs into one variant mean would have silently merged two distinct synthetic populations, the exact C-12 failure mode in miniature. Adding the provider dimension to scope keys and computing every mean/baseline within-provider makes multi-provider honest: each engine is reported as its own population, never pooled, while D-067's underlying "never pool distinct populations" rule is upheld rather than relaxed | Pooling multi-provider PMFs into one aggregate variant (the exact failure mode D-067 was written to prevent — would silently avg two populations, e.g. GPT-4o and Gemini per the SSR paper's own separate-reporting precedent); adding a provider-scoped resonance dimension only to some scopes (inconsistent — every resonance scope type needed it: variant, persona, AND delta, since a delta's baseline lookup must stay within-provider too); inventing a second mock provider id to enable a `createRun`-driven multi-mock-provider E2E test (the mock registry has exactly one provider id by design, C-9; the multi-provider proof was built instead as a direct-DB-fixture integration test, the same established pattern this codebase already uses for C-12/C-4 fixture tests that bypass the RPC layer); keeping `ResonanceStudyResults`'s flat `variants`/`personaRows`/`deltas` arrays alongside the new `providerGroups` (two divergent shapes of the same data invites drift — replaced entirely, every consumer updated in the same pass) |

| D-081 | 2026-07-07 | M25 residue (the only M25 item the operator asked for — deploy/Sentry/grounded-provider validation were skipped by explicit operator decision). Migration 0010 adds a Postgres trigger (`prevent_frozen_prompt_cells_mutation` / `prompt_cells_freeze_trigger`, `BEFORE UPDATE OR DELETE`) rejecting direct mutation of `prompt_cells` rows whose parent `matrix_versions.state` is `'approved'` or `'superseded'` — the C-4 DB backstop behind the existing app-level `assertDraft()` guard in `src/db/repositories/matrix.ts`. Draft-state rows stay fully mutable; INSERT is never blocked (compilation happens while a version is draft). A pre-commit grep audit found the app-level guard is complete (`updateCellText`/`replaceCell`/`deleteCell` all call `assertDraft` first) but ~11 DB-backed test files tear down fixtures by deleting `prompt_cells` directly against versions they had approved moments earlier to exercise the runner/worker/extraction/resonance pipeline (resonance matrix versions are born `"approved"` per D-064, so `src/modules/resonance/service.test.ts` and `src/db/repositories/metrics.resonance.test.ts` always hit this). Blocking those deletes with no exception would have broken teardown suite-wide, so the trigger has a narrow escape hatch: `current_setting('app.bypass_cell_freeze', true) = 'on'`, set only via a transaction-scoped `SET LOCAL` (never plain `SET`, so it can never leak onto a pooled connection's next unrelated query) inside new test-only helpers in `src/db/repositories/matrix.test-helpers.ts` (`forceDeleteMatrixVersionCells`, `forceDeletePromptCellsByIds`, `forceDeleteMatrixVersions`); all 11 test files' cleanup blocks were switched to call them instead of raw `db.delete(promptCells)`. The M3 tamper test (`src/modules/matrix/actions.test.ts`) was extended: after the existing app-level rejection assertion, a raw `db.update`/`db.delete` against the same now-approved cell is asserted to reject too (matched against the trigger's `RAISE EXCEPTION` text on the Postgres error preserved at `DrizzleQueryError.cause`, since the wrapper's own `.message` is just `"Failed query: ..."`). `'discarded'` (the fourth `matrix_state` value) was investigated and deliberately left OUT of the trigger's blocked-state list — no code path anywhere sets it, so broadening scope beyond the pinned `'approved'`/`'superseded'` states would be speculative; the app-level `assertDraft()` treats it as frozen too (blocks on `state !== 'draft'`), a documented inconsistency with zero live behavioral effect since discarded rows can't exist yet. Migration applied clean to both the ephemeral test DB (via `pnpm test`'s global setup) and the dev DB (`pnpm db:migrate`, verified against 458 existing `prompt_cells` rows with no errors, since a `CREATE TRIGGER` doesn't validate historical rows, only future UPDATE/DELETE) | S-039 flagged this residual: the app-level freeze (assertDraft) is real but sits entirely above the DB, so any future code path that forgets to call it (or a raw migration/script/direct-DB fix) could silently mutate reported evidence with no guard at all — exactly the layer C-3's "every reported number traces to stored raw text" doesn't cover for prompt cells, which are frozen but not literally raw-response rows | Leaving C-4 enforcement application-only (the residual this migration exists to close); a hard trigger with no bypass (would have broken ~11 test files' teardown with no non-invasive fix, since Postgres row-level triggers fire on cascaded deletes too — cascading the FK doesn't route around a per-row trigger); a plain session-level `SET` bypass instead of `SET LOCAL` (leaks across a pooled connection to the next unrelated caller — unacceptable for a security-shaped guard); broadening the trigger to also block `'discarded'` (matches assertDraft's stricter rule but the pinned decision named only two states and discarded has no live code path to validate against) |

| D-082 | 2026-07-07 | M26 calibration spike, gated on external human-benchmark data that does not exist yet. Ships two things and flips no flag: (1) `CALIBRATION_PROTOCOL.md` (repo root) — survey design (direct five-point Likert rating from human respondents shown the identical stimulus text, matching panel persona axes per D-066), minimum sample guidance (n>=30 per stimulus aggregate, mirroring D-015; >=8 distinct stimuli as a practical floor for a stable correlation estimate), the paired-data fixture format (JSON: `note` + `studyId`/`anchorSetVersion` + an array of `{stimulusId, ssrProviderId, ssrRunId, humanSampleN, humanPmf, ssrPmf}`), section-6 acceptance thresholds for flipping a NEW anchor set version's `calibrated:true` (Pearson r >= 0.6, mean absolute error <= 0.5 Likert points, mean Wasserstein-1 <= 0.75, every counted stimulus at n>=30, >=80% directional sign agreement on variant-vs-baseline deltas — explicitly labeled as this protocol's OWN floor, not a number quoted from arXiv:2510.08338, since the paper's own reported figure has not been independently re-verified against this table), and an honest-limits section (text-only stimuli; per-engine populations per D-080 — a calibration decision is scoped per generation engine, never pooled; construct validity only, never behavior prediction; no invented intervals, D-023). (2) `src/core/calibration.ts`'s pure `computeCalibrationSummary(pairs)`: given >=2 paired `{stimulusId, humanPmf, ssrPmf}` observations (each PMF validated for length-5 and sum~1), returns per-stimulus and aggregate Pearson r (of per-stimulus means; `null`, never coerced to 0, when either side has zero variance), mean absolute error, and a closed-form 1-Wasserstein distance (sum of absolute CDF differences at the four interior category boundaries of the five-point scale) — no DB, no network, no new dependency. `src/core/calibration.test.ts` hand-verifies a perfect-match case (zero error/distance, r=1) and a deliberately shifted 3-stimulus case (mean absolute error=1.0, mean Wasserstein-1=1.0, r=11/14, worked out in the test's own comments) plus input-validation rejections (PMF shape, PMF sum, negative/non-finite entries, <2 stimuli, duplicate stimulusId). `fixtures/calibration/example-paired.json` is the format's living spec, explicitly marked `"note": "SYNTHETIC EXAMPLE DATA ... not real human survey responses"` and asserted by its own test to never carry a `calibrated` key of its own. `purchase_intent.v1` in `fixtures/ssr/anchor-sets.json` is untouched (still `calibrated:false`, confirmed by grep and by an unrelated `git diff`). `src/core/calibration-protocol.test.ts` extends the C-14 forbidden-phrase check (the same `RESONANCE_TEMPLATE_FORBIDDEN_PHRASES` list resonance-templates.test.ts and report-templates.test.ts already share) to `CALIBRATION_PROTOCOL.md`'s own prose, plus a bare `"guarant"` stem check stricter than the shared list's `"guaranteed uplift"` phrase — caught one instance during drafting (a reference to the project's founding rule that used the word "guaranteed") and reworded it before the test was even written. No script invoking the harness against real data was written — there is no real data yet to run it on, and untested scaffolding for a dataset that doesn't exist is the deliberate stop-line this milestone's spec named explicitly. No UI, no report-template code change: the method section already renders off `ResonanceReportContext.anchorSetCalibrated` (`src/core/report-templates.ts:153` -> `:510`), verified unchanged and cited by file:line in the protocol doc itself | D-066 already drew the line that `purchase_intent.v1` ships uncalibrated until benchmarked; this milestone was asked to build the MACHINERY for that benchmark (protocol + comparison math) without pretending a benchmark has happened — matching this project's differentiator (statistical honesty) and the C-14 discipline against borrowing validity the product hasn't earned. Writing the acceptance thresholds down now, before any real data exists, is the D-043 lesson applied here too: an unwritten gate doesn't happen, and a gate written only after the data arrives invites fitting the bar to the result | Flipping `calibrated:true` on a synthetic self-comparison (the harness scoring its own example fixture against itself would trivially "pass" — that's variance theater, exactly what D-023's anti-invented-precision discipline exists to prevent, and the fixture's own test explicitly asserts it never carries a `calibrated` key); skipping the protocol document and jumping straight to the harness (the D-043 lesson — an unwritten acceptance bar is not a real gate); writing `scripts/calibrate-ssr.ts` now against no real dataset (untested scaffolding, the milestone's own named stop-line); quoting the SSR paper's own reported correlation as this product's acceptance bar without independently re-verifying it against the paper text (the same over-borrowing D-066 already corrected once for anchor-set methodology) |
| D-083 | 2026-07-09 | Hotfix for a real regression in D-081's migration 0010: the `prevent_frozen_prompt_cells_mutation()` trigger function unconditionally `RETURN OLD`. For a `BEFORE UPDATE` row trigger, returning `OLD` instead of `NEW` discards the caller's intended new column values and silently rewrites the row's PREVIOUS values — Postgres raises no exception, so `updateCellText`'s `.returning()` reports one row affected and `saveCellText` returns `{ ok: true }`, but the actual persisted `resolved_text` never changes. Every edit to a DRAFT prompt cell (the common, fully-mutable case — C-4 only freezes approved/superseded) was silently discarded; the operator hit this live: editing a cell and clicking Save always reverted to the pre-edit text with no error. Migration 0011 (`CREATE OR REPLACE FUNCTION`, same function/trigger names) branches on `TG_OP`: UPDATE returns NEW, DELETE returns OLD, in both the frozen-exception and bypass paths. Verified via a transaction-wrapped, rolled-back probe against the real dev DB (draft-cell UPDATE now persists; approved-cell UPDATE still raises the C-4 exception, proving the freeze guard itself is undisturbed) before applying `pnpm db:migrate` for real. The gap that let this ship: M25's own tamper test only exercised the FROZEN case; the existing draft-edit test asserted `ok: true` but never read back the persisted value — strengthened in place rather than duplicated | A bug that produces zero exceptions anywhere in the stack (Postgres, drizzle, the app layer) is invisible to type-checking and to any test that checks "did the call succeed" rather than "did the value actually change" — this is a general lesson for any future row-level trigger, not just this one | Editing already-applied migration 0010 directly (D-062 precedent: never edit an applied migration destructively — it's hash-tracked in `_journal.json`; patch forward instead); a plain `RETURN NEW`-always fix (would let the trigger's own frozen-state RAISE EXCEPTION become unreachable dead code on the exception path — TG_OP branching preserves both paths correctly); adding a brand-new duplicate test instead of fixing the existing under-asserting one (the existing test already covered the right scenario, it just checked the wrong thing) |
| D-084 | 2026-07-09 | M27: post-intake Setup editing, a dedicated row-level `/projects/[id]/setup` page rather than reopening the intake wizard. Migration 0012 (additive, no backfill): nullable `archived_at` on `brands`/`personas`/`markets`, nullable `setup_updated_at` on `projects`. Identity preservation: every edit is an UPDATE keyed by the existing row id; every "remove" archives (`archived_at = now()`, or the pre-existing `factClaimStatus` enum for fact claims since `claims_found.fact_claim_id` FK-blocks a real delete) — except attributes, which have zero FK anywhere (matched by name string only) and get a real delete; the client brand can never be archived (server-enforced). The two-reads trap is resolved by which READ a consumer is, not a flag: `getMatrixInputs` (the sole generation-input read: allocator, addCell/regenerateCell new-combo loops, both PM-9 scan call sites, and the new basics-save warning) filters `archived_at is null`; two new archived-inclusive lookups (`getPersonaLabelsForProject`/`getMarketLabelsForProject`) resolve historical `prompt_cells.persona_id`/`market_id` labels and `regenerateCell`'s own cell-specific persona/market even after archiving. Every other brand/persona/market/attribute consumer (dashboard, findings, extraction's brand-alias resolution, metrics recompute, CSV export, the misinformation register) needed zero code changes — they already read unfiltered, which is the correct archived-INCLUSIVE behavior for historical evidence by construction. `getProjectFactClaims` (extraction.ts) is the one new filter, wiring the previously-unused `factClaimStatus` enum's first real consumer (`status = 'active'`, so an archived fact-sheet row stops being offered as a match target for new claims). PM-9 scans (approval gate, matrix-board badge, and the new basics-save warning) all use the ACTIVE-only roster — gating new-generation validity, not historical protection. Guards: zero active competitors blocks comparison-intent generation with a clear error (server-side, both `generateMatrix` unconditionally and `addCell`/`regenerateCell` scoped to `intent === "comparison"`) rather than silently rendering an empty `{competitor_list}`; every Setup mutation touches `projects.setup_updated_at`; the matrix board warns when a DRAFT's `createdAt` predates the project's `setup_updated_at` (approved versions never show it — frozen evidence, C-4). `projects/new/page.tsx` now redirects a non-draft project id to `/projects/${id}/setup` instead of `/projects` | The operator had no way to view or change competitors/category/personas/markets/facts after intake completed (`projects/new/page.tsx`'s own comment: "Post-completion intake editing is out of MVP scope," flagged unresolved in D-083/S-055). Reopening the wizard was rejected: its completion path (D-026) deletes-and-reinserts normalized rows with new UUIDs, which would orphan `prompt_cells.persona_id`/`market_id` and `brand_mentions.brand_id` FKs the moment any matrix/run/extraction exists against the old rows | Reopening the intake wizard for post-completion edits (the FK-orphaning UUID-churn problem this decision exists to avoid); archiving attributes (no FK anywhere references `attributes.id` — a real delete orphans nothing, and no archive/unarchive UI was built for them); gating PM-9 scans against the archived-inclusive brand roster (would block approval/warn on a competitor's name specifically because it used to be tracked, contradicting the reason an operator would archive it); a flag-threaded single `getMatrixInputs` serving both generation and label needs (the conflict is structural — the function is called from both a generation-input call site and a label-resolution call site with genuinely opposite filtering needs, so splitting the read was more correct than a parameter) |
| D-085 | 2026-07-09 | M28: closes PM-9's "other half" (D-046). Root cause was a business-goal `job_to_be_done` value ("Penetrate traditional DSLR consumer segment") flowing verbatim into buyer-voice template slots. Three fixes, no migration. (1) `findBusinessVoicePhrases` (src/core/matrix.ts, beside `findBrandTerms`/`scanUnbrandedCells`) flags business/brand-goal phrasing — penetrate, capture, grow/expand/gain share, acquire/win customers, convert, dominate, target market, market share, grow revenue, increase adoption, scale to, launch into, plus a bounded-gap "target ... segment" regex — using the same word-boundary `containsPhrase` helper `findBrandTerms` relies on, so a short phrase like "capture" never matches inside "captured" (D-062 finding 3's lesson). Wired as a WARNING (never a block, matching PM-9's own philosophy) at the same moments PM-9 itself warns: intake review only (confirmed PM-9's own brand-term check is review-only, not live-as-you-type, so buyer-voice mirrors that) and Setup basics save (`scanBasicsForBusinessVoice`, a sibling to `scanBasicsForBrandTerms`, both folded into `updateBasicsAction`'s one `warning` slot, space-concatenated when both fire). (2) Every user-facing "Job to be done" label — intake step-forms, Setup, and the warning-message label strings in review.tsx/actions.ts/matrix board.tsx — renamed to "Buyer's goal (in their words)"; the `job_to_be_done` column/Zod-field identifier is untouched (copy-only). Setup's field gained the hint+placeholder it shipped with none of (the M27 regression against intake's own hinted field); both fields now share hint text explicitly contrasting a buyer-goal example ("night street photography") against a brand-goal example ("penetrate the enterprise segment"). (3) Of the 8 template rows (all `discovery` intent, across b2b/consumer_product/consumer_venue) that interpolate `{job_to_be_done}`, 2 were verb-phrase-framed ("...trying to {job_to_be_done}?", "...who wants to {job_to_be_done}?") and were rewritten to noun-phrase-compatible framings ("...working on {job_to_be_done}?", "...focused on {job_to_be_done}?") so a single stored buyer-goal value reads grammatically in every variant; the other 6 rows and all M23 opt-in price/promo rows already were or don't reference the placeholder. New `src/core/prompt-templates.test.ts` pins the zero-verb-phrase-framing regression | Operator-reported bug, root-caused by the lead: a marketing-objective JTBD produced a grammatically and evidentially broken prompt because (a) PM-9 only ever caught brand NAMES, never brand-GOAL language, (b) Setup's basics field regressed to zero guidance in M27, and (c) some template variants assumed a verb phrase while others assumed a noun phrase, so no single value could ever satisfy both | Blocking (not warning) on business-voice language (contradicts D-046's own warn-never-block precedent this milestone explicitly follows); a DB migration or schema rename of `job_to_be_done` (the fix is entirely copy + pure-function + template-text, no stored-data shape changed); adding live-as-you-type buyer-voice scanning to step-forms.tsx (PM-9's own brand check isn't live there either — inventing a new UX pattern the sibling guard doesn't have would be inconsistent, not an improvement); forcing all 8 job_to_be_done template rows into identical wording (variant phrasing diversity is the point of having 3 variants per intent; only the grammar frame needed to converge, not the sentence itself) |
| D-086 | 2026-07-09 | M30 whole-repo cleanup audit: research/classify first (`AUDIT_METHODOLOGY.md`, `PROTECTED_REGISTER.md`), then execute as 6 ordered, separately-committed batches. Durable conventions introduced, now standing practice for every future cleanup pass: a four-tier action label (`Delete` proven-unused-and-unprotected; `Merge` duplicate logic with identical invariants; `Archive/Relabel` a status header on an executed proposal, body never rewritten; `Keep — Protected` matches a Decision Log entry, cite the D-number, no further analysis); a three-class document taxonomy (current-state docs get "update in place," append-only history — Decision Log rows, BUILD_NOTES entries, PRD progress notes — is never rewritten only D-025-truncated, proposal/plan docs get a status header only); `PROTECTED_REGISTER.md` as a standing artifact (29 surfaces the Decision Log explicitly argued into existing, walked from all 85 prior entries) that persists past this audit's close, consulted by name before any future `Delete`/`Merge`/rename; and a `docs/audits/mNN/` convention for where future audits' own working docs should live. Glossary/D-077 correction: D-077's own decision text claimed it had already redefined the glossary's "funnel stage" term around the two-layer split, but the live glossary text still carried the pre-D-077 wording — found and fixed in this audit's funnel-narrative-kill pass (commit `ef04ffb`); noted here rather than editing D-077's immutable row. Concrete cleanups landed: `listRuns` deleted (0 callers, superseded by `listRunsWithProgress`/D-048); 18 of 19 vestigial `.gitkeep` files removed (`public/.gitkeep` kept — its directory has no other tracked content); 3 proposal docs relabeled with accurate status headers (`RESONANCE_BUILD_PLAN.md`, and `M21_M26_BUILD_PLAN.md`/`LAYERS_AND_EVIDENCE_ONLY_EVALUATION.md`, whose prior self-descriptions — "untracked, uncommitted, NOT canon" — were factually false); `BUILD_NOTES.md` truncated per D-025 (58 past-merge session entries removed, one M30 summary entry added); 10 remaining current-state doc-drift items synced to code (README milestone status/scripts list, MASTER_CONTEXT C-13/repo-map/docs-index, PRD routes-list/RS-2/RS-4, ENGINEERING_SPEC schema table/resonance scope-key format); PRD.md backfilled with §§8.23-8.30 requirement IDs for M21-M28. DEFERRED structural-split proposals graduated from the now-deleted `AUDIT_STRUCTURE_MAP.md` (pure-move-only, none executed, no owner assigned — future milestone's choice): `src/components/setup/setup-client.tsx` -> per-section components under `src/components/setup/sections/` (highest confidence — the six sections are already named in D-084's own text); `src/core/report-templates.ts` -> split into `report-templates-audit.ts`/`report-templates-resonance.ts` behind the existing `reportSectionsForKind`/`csvDatasetsForKind` selectors; `src/db/repositories/metrics.ts` -> `metrics-audit.ts`/`metrics-resonance.ts` promoting the file's existing internal `recomputeMetrics`/`recomputeResonanceMetrics` boundary to a file boundary; `src/db/repositories/resonance.ts` -> `resonance-studies.ts`/`resonance-stimuli.ts`/`resonance-results.ts` (moderate confidence, boundary inferred rather than product-named); `src/db/repositories/dashboard.ts` -> `dashboard-runs.ts`/`dashboard-misinformation.ts`/`dashboard-drilldown.ts` (lower priority, smallest of the DB hotspots); `src/db/repositories/runner.ts` (largest file, 1,013 lines) -> job-claiming/lifecycle/breaker/budget clusters mirroring the existing `src/modules/runner/budget.ts`/`degradation.ts` split, but flagged lowest-confidence given its history of subtle correctness bugs (D-045, D-049, D-072) — recommended only as incremental pure-move commits with full tests green before/after each, never a single restructure; `src/worker/index.ts` -> narrowest proposal (extract `processJob` to `process-job.ts`), lowest confidence of all seven since its current single-file shape is the direct result of D-049/D-072's failure-domain-isolation fixes | The audit's own research pass (Pass 0 Protected Register, Passes 1-5 drift/register/inventory/structure) found this codebase unusually clean — of 94 exported symbols across the 7 largest files, exactly one was dead code — so the closing entry records process conventions worth keeping (the four-tier label, the document-class taxonomy, the Protected Register) alongside a short, low-risk list of concrete fixes, rather than a large behavioral cleanup | Executing the 7 structural-split proposals now (the methodology's own rule: structural splits are pure-move-only commits reviewed separately from a docs/dead-code/repo-noise pass; several touch the highest-risk, most-cross-referenced file in the repo, `runner.ts`, where a careless move could reintroduce a silent regression like D-045/D-049/D-072); collapsing the four audit-artifact docs into the Decision Log entry verbatim instead of a concise graduation (would make this single entry enormous for a one-time list a future reader can already get from `git log`/`git show` on the deleted files' last commit); keeping the four working-artifact docs in the repo past close (a second, competing home for facts `MASTER_CONTEXT.md` §9 already owns — the same problem the document-class taxonomy exists to prevent) |
| D-087 | 2026-07-10 | M31 workspace hierarchy, built on branch m31-workspace-hierarchy. Finalized navigation plan (recorded before build so the implementing agent's boot ritual had it): Problem: `/projects/[id]` has no page (404s, no hub) and the 6-tab flat subnav (Setup/Matrix/Runs/Dashboard/Report/Simulation) puts Simulation at the same level as pipeline-stage tabs even though its one page conflates three different stages — defining studies (an input, like the matrix), executing them (already shown under Runs, which lists audit and resonance runs together with a SIMULATED badge), and reading results (a metrics view, like the dashboard). Plan: (1) a hub page at `/projects/[id]/page.tsx` — numbered dossier sections (01 Setup, 02 Runs, 03 Dashboard, 04 Report) each with a one-line status pulled from `getProjectPipelineState` plus run/study counts and the next-action cue; projects-list links and breadcrumbs point here instead of `/matrix`. (2) Subnav collapses to four top-level items: Setup, Runs, Dashboard, Report (`Matrix` and `Simulation` leave the top level; the project-name label links to the hub). (3) Setup becomes a section with three sub-tabs — Inputs | Prompt matrix | Simulation studies — rendered on the existing `/setup`, `/matrix`, `/resonance` routes; the matrix board and the resonance study wizard stay separate pages (never folded into one page/form) because approved matrices are frozen evidence (C-4, DB-enforced by the D-081 trigger) while Setup inputs are freely mutable — merging them would blur that line. (4) The Simulation page narrows to study definition/approval only; results move to a new walled Simulation section on the Dashboard page (summary card per approved study — headline ΔPI per engine per D-080, SIMULATED badge — linking through to full results on the Simulation page; chosen over embedding full per-engine results tables in the dashboard, which was considered and rejected as unnecessary weight for a v1) — the existing audit-only run selector and metric scopes are untouched (C-12: no shared chart, no shared selector, ever). (5) Runs page groups its existing flat list under "Audit runs" / "Simulation runs" headers — no data or query change, `listRunsWithProgress` already returns `matrixKind`. (6) No migration; no route renames (`/matrix` and `/resonance` segments both stay — same D-077 precedent already in `PROTECTED_REGISTER.md` for the `resonance` segment: renaming a route for a copy/nav-only milestone is bookmark/link churn for zero behavior gain); the existing `PROTECTED_REGISTER.md` D-084 note ("Setup leads subnav") and D-068's C-12 UI-plumbing guards apply and must not regress | The operator's own proposal (single mother-menu hub, no-fuss sub-section access, Simulation demoted from a peer tab) was directionally right but under-specified two things: it would have merged mutable Setup inputs with the frozen prompt matrix (a C-4 violation risk) and implied a literally unified metrics view across measured and simulated data (a C-12 violation risk since C-12 is a hard constraint, not a UI preference). The finalized plan keeps the operator's hub-and-demotion intent while routing both risks around the two hard constraints: sub-tabs (not a merged page) under Setup, and a walled summary-with-link-through (not a pooled view) under Dashboard | Folding the prompt matrix board into the Setup page as one form (blurs the mutable-input/frozen-evidence line C-4 exists to enforce); a literally single Dashboard view mixing measured and simulated metrics/charts/selectors (direct C-12 violation — measured and simulated must never share a chart, selector, or aggregate); renaming the `matrix`/`resonance` route segments to match the new nav grouping (D-077 already rejected this exact churn for `resonance`; same reasoning extends to `matrix`); embedding full per-engine simulation results tables directly in the dashboard instead of a summary-with-link-through (heavier build for this branch, deferred, not rejected outright — revisit if operator wants richer inline sim data post-M31) |

| D-088 | 2026-07-10 | M32 operator-workflow UI architecture, planned on top of M31: replace stacked horizontal global/project/setup navigation and the duplicated stage banner with one responsive left sidebar, project switcher, nested Setup/Execution/Results hierarchy, and URL-addressable local views. Project data is read-only by default; explicit Edit/Add/Save changes/Cancel semantics replace always-open input forms. Long work surfaces become one-view-at-a-time routes: Setup sections, Matrix overview/pillars, Run overview/events/extraction/metrics, Dashboard overview/pillars/Simulation, Report outline sections, and a new `/resonance/[studyId]` workspace for study overview/design/runs/results/evidence. The Simulation library no longer renders every study's raw results; detail results select one provider at a time and evidence is deduplicated/paginated. No migration and no route-segment rename: `/setup`, `/matrix`, and `/resonance` stay separate; M31's horizontal subnav and same-page `#study` result presentation are superseded, while existing hash anchors remain meaningful. The Dashboard Simulation view remains structurally walled from audit selectors, charts, aggregates, and DTOs (C-12); approved matrices and approved studies remain immutable (C-4/C-13) | The operator workflow walk exposed a common structural failure: global actions, project sections, setup subsections, creation forms, execution state, results, and raw evidence were rendered as simultaneous horizontal choices or one long vertical document. A sidebar plus URL-addressable local views makes hierarchy and reversible navigation explicit without altering the measurement model | Keeping the stacked horizontal bars and restyling only their appearance; merging Setup inputs with frozen matrices; retaining one `/resonance` document that eagerly loads every study and duplicates evidence by variant/persona; pooling audit and simulation data for a unified dashboard; renaming existing route segments; treating generic Save controls and always-open textboxes as an adequate edit model |
| D-089 | 2026-07-10 | M33 planned (not yet built): verification and finish pass on `m32-workflow-ui`, first of a four-workstream internal-stakeholder-demo-readiness arc (M33 verify/finish -> M34 hero data/story -> M35 brand alignment -> M36 demo ops/rehearsal). Reason for existing: M32 shipped on green lint/typecheck/test/build but never received the interactive browser walk its own build plan (M32_BUILD_PLAN.md section 5) required — `preview_start` hit a sandbox failure and the walk was deferred (BUILD_NOTES S-063/S-064). Two research passes plus direct code review (not a browser session) found, beyond M32's own known-unverified items (dirty-edit prompt never wired; study-detail evidence filters expose engine-only despite the repository layer already supporting stimulus/persona filters): the mobile sidebar drawer sets `role="dialog" aria-modal="true"` but is hand-rolled with zero focus management (no focus-in on open, no Tab trap, no focus restore on close) while every other overlay in the app correctly inherits this from Radix primitives — a real, untested contradiction of M32's own accessibility acceptance line; zero `loading.tsx`/`Suspense` boundaries exist anywhere in the M31/M32 segmented-route surfaces, sharpest on Dashboard where an approved study's Simulation summary blocks first paint on a parallelized-but-unindicated staleness recompute; and Setup's edit forms have no on-page unsaved-changes signal at all, not just a missing cross-navigation guard. `M33_BUILD_PLAN.md` is the execution playbook (mandatory first step: read the operator's own manual-test findings from BUILD_NOTES and fold them in before starting); it embeds an explicit decision gate after the verification walk — isolated bugs get fixed in place, but if the walk finds structural rot rather than unfinished-but-sound work, the plan requires stopping and escalating a revert-to-M31 call to the lead rather than patching around it, since M31 is deliberately preserved on its own branch as that fallback. No migration, no IA change, no route rename — M33 fixes what M31/M32 built, it does not redesign it | An internal-stakeholder demo needs a UI that has actually been driven by a human or a keyboard, not just one that typechecks; shipping a full navigation rebuild past its own required QA gate was the single largest risk identified in the demo-readiness assessment, and the focus-trap/loading/unsaved-signal gaps are exactly the class of defect a green test suite cannot catch | Silently patching the newly-found gaps into a future milestone instead of naming them now (the point of surfacing them before the operator's own walk is to let the two findings compound, not race each other); deciding the revert-to-M31 question unilaterally in this planning pass (no browser session has run this turn; only the operator's hand-test and static code reading informed this entry, so the actual go/no-go call is deliberately left to the verification walk itself); redesigning the IA while already touching this code (M33's whole purpose is finishing what M31/M32 already decided, not re-opening it) |
| D-090 | 2026-07-10 | Codified the threshold for when a milestone gets its own `M<N>_BUILD_PLAN.md` file, added to §7's file-split rule. A build-plan doc is earned only when its execution playbook clears the existing split test — multi-milestone, OR multi-phase with per-phase acceptance criteria, OR realistically spanning several sessions/agents, OR ~300+ lines. Otherwise the plan lives in the milestone's Decision Log entry plus its `BUILD_NOTES.md` handoff entry, as M27–M31 did; the default is no file. History that prompted this, established by reviewing every plan doc's size and creation commit: the two arc plans cleared the test cleanly (`RESONANCE_BUILD_PLAN.md` 284 lines / M16–M20; `M21_M26_BUILD_PLAN.md` 537 lines / M21–M26); M27–M31 correctly had none (M31's plan lived in D-087 + BUILD_NOTES S-060); M32 (206-line, six-phase UI rebuild) is defensible on the "different cadence / procedural checklist" half of the test; `M33_BUILD_PLAN.md` (76 lines, single milestone) sits below the threshold and was created by pattern-matching M32 rather than by earning it — it is the boundary case this rule exists to stop recurring, and would not be created under the rule. Its disposition (fold back into D-089 + BUILD_NOTES, or grandfather) is left open and does not affect the rule | The per-milestone build-plan file was becoming a reflexive habit rather than a size-driven choice, reviving exactly the doc-sprawl and "one fact, one home" drift that §7 / D-005 / D-086 exist to prevent; naming the threshold turns "does this milestone get a doc?" from a per-agent re-derivation into a testable rule, and keeps the genuine benefit (a walkable, status-headerable checklist that survives BUILD_NOTES truncation) reserved for the milestones large enough to need it | A build-plan doc per milestone by default (the sprawl this prevents); never writing build-plan docs and folding all execution detail into Decision Log rows (loses the walkable-checklist benefit for genuinely large, multi-phase, multi-session milestones — the append-only Decision Log can't be checked off and BUILD_NOTES is truncated at merge); leaving the decision uncodified (the status quo that produced the M33 boundary case); retroactively deleting M33_BUILD_PLAN.md as part of this rule (rule-making and cleanup are separate acts — the disposition is the operator's call, per the M33 point left open in the assessment) |
| D-091 | 2026-07-10 | `M33_BUILD_PLAN.md` deleted; its 7-phase execution detail folded into `BUILD_NOTES.md` S-065's `NEXT` field instead, per the operator's own resolution of the disposition D-090 deliberately left open. D-089's text still names the now-deleted file as "the execution playbook" — that line is left as written (D-086's precedent: an old Decision Log row's stale internal reference is corrected by a later entry, never by editing the immutable row itself) and is superseded by this entry for that one detail only; every other fact in D-089 (why M33 exists, the three newly-found gaps, the fix-vs-escalate decision gate, no-migration/no-IA-change scope) is unaffected and still governs. The `M33_BUILD_PLAN.md` row was removed from §7's Documents index (current-state, corrected in place, not append-only). PRD's M33 tracker row was corrected the same way; its existing progress-note bullet referencing the file was left as written and a new bullet added above it, matching this same pattern | M33 (76 lines, single milestone) never cleared the D-090 threshold — it was created by pattern-matching M32 rather than earning it, and D-090 already named it the boundary case the rule exists to stop recurring. Once the threshold existed, leaving the file in place with no justification would have contradicted the rule the operator had just asked to be codified | Editing D-089's or the old PRD progress-note's text directly (rewrites decided-immutable history for a purely administrative correction — the D-077/D-086 precedent exists for exactly this situation); grandfathering the file instead of folding it back (available per D-090, but the operator chose fold-back); silently deleting the file with no Decision Log trace (the file's existence and deletion are both real events in the project's history and belong in the log, however small) |
| D-092 | 2026-07-10 | Reviewed an external repo-quality audit (a delivery-process and engineering review, not an agent of this project) and recorded a response. Three of its sharpest claims were independently verified against source before trusting them: the "2 stimulusi" pluralization bug is real (`src/app/projects/[id]/resonance/page.tsx:88`, `{stimuli.length} stimulus{stimuli.length === 1 ? "" : "i"}`); `vitest.config.ts:19` really does set `fileParallelism: false`; `scripts/test-db.ts` really does deliberately convert embedded-Postgres startup failure into a fail-closed skip rather than a hard error, correct for a local machine but wrong for a CI required-check. Verdict: the audit's core thesis — that the gap between "code complete" and "verified" is this project's real constraint, not runtime correctness — is correct and matches M32's own lesson (D-089) exactly; its "prepare for 4x change volume" framing and much of its 30-day response describe team-scaling machinery (parallel workstreams with file ownership, a separate integration-owner role for canonical docs, a staging environment, coverage floors, a dashboard bundle budget, load testing, dependency/security scanning, import-boundary tooling beyond the already-clean C-7 boundary) this project has not adopted, has no team to run, and has not yet deployed once. ADOPTED as new session-ritual hard rules (§8, both stated there): a UI-touching milestone cannot be marked Done until its interactive verification actually ran and is evidenced in BUILD_NOTES — deferred verification is "Code complete — unverified"; and commit at each phase-green boundary within a milestone, not one commit at close (M32's 60-file single commit was the audit's clearest structural complaint). ADOPTED, folded into M33's existing execution list per D-090/D-091 (BUILD_NOTES `S-065`'s `NEXT` field, not a new build-plan file): CI must fail hard, not skip, when embedded Postgres cannot start under `CI=true` (local `pnpm test` keeps D-078's fail-to-skip discipline, which is correct for a developer machine and wrong for a required gate); the stimulusi typo fix; one Playwright smoke spec over the critical operator journey (login -> hub -> matrix -> run detail -> dashboard -> study results) plus an `axe` accessibility pass on those pages, explicitly a floor and not a coverage program. ADOPTED, folded into `RELEASE_CHECKLIST.md`'s pre-deploy gates (binds at first deploy, not before): worker SIGTERM drains in-flight paid calls against a deadline instead of exiting immediately; `/health` gains a database-readiness check, not liveness-only; observability wiring is confirmed either actually installed or the stack-table claim is corrected — corrected now, see below. ADOPTED IN PRINCIPLE, explicitly not scheduled into any milestone: two-agent parallel sessions become permissible only after per-branch `BUILD_NOTES.md` files and per-worktree `.next` isolation exist (the one-session rule's actual cause is shared mutable dev infrastructure — D-073/D-075's incidents — not social contention, so infrastructure isolation is the real prerequisite, not a file-ownership chart) AND only after the new Done-requires-the-walk gate is live. CONVERGES with existing plan, no new decision needed: the audit's hotspot pure-move-split list and ordering (leave `runner.ts`/worker until last) matches D-086's already-recorded list exactly. DOC-DRIFT FIX (not a new decision, a factual correction): `MASTER_CONTEXT.md` §5's stack snapshot and `DEVELOPMENT_GUIDELINES.md`'s stack table both listed pino and Sentry as if wired; neither is a `package.json` dependency, only `run_events` is real, and D-076/D-081 already documented this as a deliberate deferral ("not wired this sprint," "skipped by explicit operator decision") — both tables corrected to say so plainly instead of reading as a present-tense implementation claim, which is exactly how the audit read them. REJECTED: a staging Render environment, coverage-percentage floors, a dashboard bundle-size budget, load testing, dependency/security scanning, import-boundary tooling beyond C-7 (M30 found zero violations against the existing boundary), and a separate integration-owner role for updating canonical docs after feature PRs merge (would break the exact property the audit itself praised most — decisions recorded by the session that made them, with rationale) | The audit actually drove the app in a browser and found three real, previously-undocumented defects, one of which (the drawer focus-trap gap) independently corroborated D-089's own code-review finding — that convergence is what made the rest of the audit worth taking seriously rather than dismissing on its scaling-framing overreach. But its throughput assumptions describe a company this project is not and has not decided to become; adopting the full 30-day response verbatim would trade the project's actual differentiator — MASTER_CONTEXT's whole design, every decision captured with rationale where it was made — for scaling machinery with no scale yet to serve | Adopting the full 30-day response verbatim (imports team-shaped delivery process this project hasn't earned or needed, and several items directly contradict already-deliberate decisions like D-076/D-081's Sentry deferral); dismissing the audit entirely because its scaling framing overreached (would discard three real, independently-verified defects and the one rule — Done requires the walk — that most directly addresses M32's own root cause); silently patching the pino/Sentry doc-table drift with no Decision Log trace (the audit read it as a live implementation claim, which is a real readability defect worth recording, not just quietly fixing); treating "adopted in principle" parallel sessions as scheduled work (infrastructure isolation and the verification gate are real prerequisites, not yet built — recording the shape now without inventing a milestone for it) |

| D-093 | 2026-07-10 | M33 mobile sidebar drawer rebuilt on `@radix-ui/react-dialog` (same primitive as `AppDialog`), restyled full-height/left-anchored, rather than hand-rolling a second focus trap. Focus moves into the drawer on open, Tab cannot leave while open, Escape/overlay/close restore focus to the hamburger trigger. Source-contract unit test asserts the Radix import and forbids a bare `role="dialog" aria-modal="true"` shell | Radix already owned focus management for every other overlay; duplicating trap/restore logic was the exact defect D-089 named | Hand-rolling a second trap (would reintroduce the M32 gap under a different shape); leaving the drawer as a visual-only overlay with `aria-modal` (false accessibility claim) |
| D-094 | 2026-07-10 | M34 Baseline Framing Integrity — planned (execution playbook `M34_BUILD_PLAN.md`, earns a standalone file under D-090: multi-phase, ships migration 0013 + a second worker state machine, multi-session, >300 lines). Outcome of a three-round strategy reassessment (SSR paper read directly; two research sweeps on the academic validity base and the competitive/OSS landscape; verify-then-fix against the repo): the Simulation Layer is refocused as **audit-linked framing repair** — "the decision layer between an AI-brand audit and human/in-market validation" — not broad synthetic consumer research (which the evidence rates weakest and which invites comparison with $1B-valuation incumbents). M34 builds the Evidence-Layer foundation that gate requires. Core rulings, all committed to build: (1) new `representation` audit intent (sixth), a genuinely-branded-but-non-evaluative prompt class measuring how AI describes the named brand under six minimally-leading prompt variants — **partially supersedes D-068** ("widening `Intent` with a sixth value" was rejected there for `simulation`; `representation` is a real audit intent, unlike `simulation`, and `Record<Intent,…>`/exhaustive switches make the blast radius compiler-forced — the audit-vs-simulation type wall stays). (2) A 4th D-054 prompt-frame class `neutral_branded`; representation cells count toward NO planted-signal metric (mention/SoV/position/comparative/sentiment/attribute/citation/stability all exclude it), but DO feed factual-claim extraction + accuracy (a claim is not planted by naming the brand) — implemented as an exhaustive `(metricKey,intent)` policy map, never an unlisted default branch; PM-9 gains a `neutral_branded` branded-exemption. (3) Two never-pooled evidence lanes: `organic_in_context` (spontaneous mention in existing unbranded cells) and `neutral_elicited` (the representation prompts); "bias-free"/"unbiased"/"vanilla AI opinion" are forbidden vocabulary (test-enforced). (4) A **blind** frame-extraction state machine (parallel to the standard extractor, mirroring its retry/dead-letter/stale-sweep/budget discipline, D-045/D-049 patterns) that receives ONLY raw text + observed brand name + a fixed schema — never the fact sheet, desired attributes, competitors, original prompt, or operator labels — so discovery cannot be contaminated by hope (D-054's philosophy pushed down to the extraction layer). (5) **Conservative, no-override eligibility**: a neutral baseline needs the frame to win ≥5 of 6 prompt variants (≥3 of 5 responses each), be uniquely top, survive leave-one-variant-out, with no tie and no unresolved contradictory cluster; an organic baseline needs ≥5 qualifying cells (≥3/5 spontaneous each) surviving leave-one-cell-out; every failure yields an explicit abstention state (`extraction_incomplete|sparse|recurring_only|prompt_sensitive|volatile|divergent`) and NO operator override exists. Prevalence is descriptive (`6 variants × 5 generations`) — **no Wilson/confidence interval or independence claim on correlated draws** (the exact RS-8 error, rejected one layer down). (6) The Simulation measured baseline is the eligible cluster's **medoid verbatim stored response** (lowest mean cosine distance to co-members, UUID tie-break, immutable, never trimmed/rewritten) — NOT an authored synthesis; a separate human-readable summary is report-only, never the stimulus. This keeps the stimulus measured, not invented — the moat. (7) **C-15** (new hard constraint) enforces all of the above in the approval RPC + repository, lands LAST (Phase 6) so merging M34 doesn't brick new B2C studies before a baseline can exist; B2B gets no framing baseline and no new Simulation approval. (8) A **Phase-0 retrospective feasibility gate** freezes the numeric thresholds as versioned `framing-protocol.v1` parameters ONLY after proving on stored Insta360/Heytea data that ≥1 real baseline passes and ≥1 unstable case abstains — a zero-pass result STOPS the rollout before C-15, no auto-loosening (the D-043 "write the gate before the data" discipline, plus a real stop-line). (9) **Partially supersedes D-079**: the "Simulation coverage" panel is replaced for framing-repair by a readiness state (`not run|extraction incomplete|review required|eligible|blocked`); price/promo packs stay protected code + historical rendering but are methodology-gated (not newly client-ready) until an offer-specific baseline protocol exists. (10) Migration 0013 additive, no backfill; historical studies render `LEGACY BASELINE`. Scope explicitly EXCLUDES (deferred, not dropped): the `purchase_intent.v1`→single-validated-construct redesign, any human calibration, decision-gate/implementation-brief generation, B2B simulation, and the D-089 M34→M36 hero-data/brand/demo arc (M34 REPLACES the informal hero-data milestone; existing demos wear `LEGACY BASELINE`; a new hero study is generated only after M34 passes) | The strategy work established that comparative, text-based, familiar-category, aggregate-level framing tests are the best-evidenced LLM-simulation task (Hewitt et al. r=.85–.90 on textual treatment effects; the SSR paper's >90% rank-correlation attainment), while absolute/novel/subgroup claims are the worst — and that the one open competitive position is the measured-evidence→simulated-fix loop nobody else runs. But a simulation is only as honest as the framing it reacts to: C-13's "cite a stored response id" proved too weak (one cited response is not a prevalent or stable framing), so the baseline needed a real integrity gate before the strategic repositioning could rest on it. Every ruling here was chosen to make the baseline provably measured, prevalent, and stable — or to abstain loudly — because that integrity is what the whole audit-linked-repair thesis sells | Keeping C-13's single-cited-response baseline (too weak — the gap the whole milestone closes); an authored synthesis as the Simulation stimulus (re-introduces "invented, not measured" — the exact thing the strategy differentiates against; medoid-verbatim keeps it real and now provably representative); a Wilson interval over 6×5 correlated draws (the RS-8 independence error one level down — prevalence stays descriptive); operator override of a failed eligibility rule (defeats the no-override integrity guarantee — abstention states are the escape hatch, not a bypass); enforcing C-15 early (would brick new B2C studies the moment M34 merged — Phase-6-last makes the product pause deliberate and dated); auto-loosening thresholds if Phase 0 finds zero passing baselines (would fit the bar to the result — a zero-pass world means the method needs rethinking, escalate); widening `Intent` without arguing the D-068 supersession (the register requires the D-number and the enumerated blast radius); reusing the Stability Index as frame stability (it is shortlist consistency — a separate computation, never relabeled, BF-13); folding the construct redesign / human calibration / demo arc into M34 (each is its own milestone; M34 is foundation, not everything downstream of it) |
| D-095 | 2026-07-11 | M34 Phase 0 executed twice, NO-GO both times, escalated per the gate's own stop-line; the lead's ruling redirects the protocol rather than the thresholds. Run 1 (framing-protocol.v1): instrument failures, not method results — whole-payload Zod voiding on one >240-char `evidence_quote` killed 5/60 neutral responses (`extraction_incomplete` before eligibility ever ran; the D-011 lesson re-learned), unbounded label vocabulary (213 labels/264 frames), and the sole organic dataset invalid (Heytea stored `category_archetype=b2b` — the D-052 defect — and BF-24 excludes B2B). Run 2 (framing-protocol.v2: per-frame salvage with 240-char exact-prefix truncation, label-form rules in the blind prompt, deterministic conjunct-sort+plural-fold normalization, 90s call deadlines — D-039 parity — and Heytea structurally gate-excluded; every numeric threshold byte-identical to v1): the first REAL verdicts — neutral DeepSeek `prompt_sensitive` (2/6), neutral OpenAI `volatile` (1/6), Insta360 organic a correct `sparse` abstention, negative control abstains. Diagnosis, verified by three nested re-scoring tests: the eligibility unit is malformed — `dimension::label` as frame identity fragments one concept the extractor legitimately tags `category` in some responses and `offering` in others ("action camera" is both), and per-dimension scoring does NOT rescue it (1/6) because fragmentation is cross-dimension; separately, the six prompts are not six paraphrases (v4 steers `offering`, v5 steers `audience`), so the rule confuses multidimensionality with instability. An exploratory hand-collapse recovering 4/6 (DeepSeek) and 5/6 (OpenAI) is recorded as DIAGNOSTIC ONLY — the agent's initial "framing proven stable / OpenAI passes" reading was an overclaim, withdrawn: the collapse rule was designed on the same Insta360 responses it scored (circular). RULING — approved: the timeout fix; per-frame salvage; Phase-2 production code mirroring the existing extraction state machine verbatim (three harness bugs re-learned product lessons: payload voiding↔D-011, missing deadline↔D-039, fixture clobbering↔evidence immutability); the continued NO-GO; prompt-set reassessment; concept/dimension-separation research. NOT approved: majority-vote dimension resolution (polysemy is signal to model — a concept carries a reported DISTRIBUTION of dimensions, never a forced winner); hand-authored synonym collapse as production logic; declaring any scope eligible; freezing C-15. framing-protocol.v3 requirements (research → freeze → held-out test): two-level concept+dimension-distribution ontology; admission prompts = open paraphrases only with v4/v5 demoted to diagnostic probes (admission-set size and win threshold re-specified TOGETHER, pre-registered); rebuilt four-control suite (positive synonym-consolidation, negative distinct-concepts, polysemy, over-merge — the current rotating fixture's labels could be legitimately merged by a coarser model, making the negative control accidentally stable); brand-level dev/holdout split — Insta360 is development data forever (it drove the redesign), validation needs ≥1 unseen stable-category B2C brand + ≥1 genuinely-fragmented brand + adversarial fixture; a commercial-value layer distinguishing identity frames from associative frames and defining the actionable framing gap (stable association conflicting with intended positioning or omitting a verified truth) — "AI consistently says Insta360 makes 360 cameras" is defensible but commercially trivial. Full record: docs/audits/m34/phase0-reassessment.md; evidence fixtures framing-protocol.v1/.v2 both preserved; ~$0.60 total spend | The Phase-0 gate exists precisely to catch this before C-15 enforcement: two cheap runs bought the finding that the method cannot yet distinguish conceptual stability from dimensional variation, and the exploratory evidence justifies redesigning the measurement model — not the conclusion that any framing is stable. Loosening 5→6 thresholds would not even have helped (top wins were 2/6 and 1/6) and would have been bar-fitting; the failure is in the unit of measurement, which is a methodology decision the plan reserves for the lead | Relaxing the ≥5-of-6 rule (bar-fitting, and mathematically useless here); majority-vote dimension resolution (hides the polysemy the two-level ontology should report); promoting the exploratory hand-collapse to production clustering (circular — designed and scored on the same data); declaring OpenAI eligible on the 5/6 hand-collapse (same circularity); validating v3 on Insta360 (development data — it shaped the redesign); keeping v4/v5 in the admission set (a coherent brand would keep failing because different questions correctly elicit different facets); freezing C-15 in this state (would enforce an eligibility rule known to be malformed) |
| D-096 | 2026-07-11 | Second Phase-0 ruling (extends D-095): risk-disposition triage of the full defect list, verified-then-recorded (772-frame quote audit reproduced exactly: only 484/772 evidence quotes are literal substrings, 266 match only after normalization, 22 match nothing — the extractor "promises exact quotes but validates only type and length"; organic extraction-version fan-out confirmed in code though not yet manifested in data; resume identity = provider×variant×rep only; completeness denominators derived from frame-bearing hits so valid `no_frame`/`uncertain` responses vanish; the negative fixture is built inside the analyzer and bypasses the embedding/clustering path entirely). SIX INSTRUMENT FIXES REQUIRED BEFORE ANY v3 RUN: (1) PRD §8.32 BF-10..14 marked DO-NOT-IMPLEMENT-AS-WRITTEN (done — planning record stands, frozen v3 supersedes on freeze); (2) immutable run manifests — prompt hash, raw-text hash, provider/model, decoding, extractor version, embedding model, protocol version; any mismatch = new run, never resume; (3) response-state denominators — `no_frame|uncertain|insufficient_evidence` stay in the denominator; (4) evidence quotes resolved to verified source OFFSETS, unsupported frames rejected — never trust model-reproduced quotation text; (5) organic evidence pinned to exactly one declared audit run + the triggering standard-extraction VERSION; (6) all four v3 controls (synonym-consolidation, distinct-concept, polysemy, over-merge) run as immutable inputs through the REAL clustering/eligibility path, never analyzer-internal shortcuts. REVIEW-INTEGRITY RULE: relabel-to-same merging currently changes cluster identity before eligibility — reviewer merging can manufacture eligibility; v3 blinds review to counts/outcomes and locks label mappings BEFORE scoring. METHODOLOGY WORK (focused, dev-data only): framing-profile-vs-dominant-frame experiment (several independently stable concepts + per-concept dimension distributions + identity/associative layers + one explicit target association for correction; a tie must not automatically mean instability; the medoid represents the stable profile while the Simulation intervention names the association being repaired); a SMALL blinded human-coded gold set validating the coding instrument (support/aboutness/quote-mapping/synonym-resolution/over-merge/dimension-reasonableness — explicitly NOT a calibration milestone); a preregistered ablation of the shared "say so if uncertain" clause (possible prompt-induced style stability); new `entity_ambiguous` abstention state. PRODUCT: PRD gains a post-blind gap-analysis stage before Phase 1 — actionable framing gap tied to verified facts + intended positioning (stable category recognition alone is commercially trivial). C-15 ROLLOUT AMENDED (partially supersedes D-094 point 7): frozen v3 must pass controls + human-coded checks + held-out brands, then M34 ships in SHADOW/ADVISORY mode measuring eligibility rate, abstention reasons, review time, and actionable-gap rate across several consumer audits; C-15 hard-enforces only if the workflow produces useful baselines often enough to support the commercial promise; explicit invalidation on any model/prompt/extractor/clustering/embedding version change; automated behavioral-drift detection deferred to a later monitoring milestone; until enforcement, new C-13 studies render a `PRE-M34 BASELINE` disclosure rather than silently passing as M34-equivalent. SECURITY/OPS QUEUED: untrusted-data envelope + adversarial fixtures for raw AI text entering extraction/Simulation prompts (before production integration); embedding+frame-extraction spend added to projection/cap/ledger/daily budgets (before Phase 3 — C-2 gap confirmed); raw Phase-0 artifacts gitignored with SHA-256 MANIFEST.md committed in their place (done — full model-origin text never enters the repo). NOT EXPANDED (do not reopen inside M34): Wilson/respondent-style claims, B2B baselines, price/promo protocols, purchase-intent calibration, universal frame ontology, automated drift monitoring, any large human-validation study, re-running v2 to seek a pass | The Phase-0 stop-line bought a defect list cheap; triaging it into fix-now / research / defer keeps M34 alive without pretending its measurement foundation is done. The quote-verification finding is the sharpest: an evidence-integrity product whose own evidence quotes are 37% non-literal cannot ship a quote-anchored baseline, and offsets are cheap. Shadow-first C-15 exists because a single held-out brand passing should not activate a product-wide hard gate whose usefulness rate is unmeasured | Hard-enforcing C-15 straight from one held-out pass (D-094's original Phase-6 cut — an unmeasured gate could brick the product on a rule that rarely yields usable baselines); treating the 22 unmatched quotes as ignorable noise (they are the proof the exact-quote contract is unenforced); full automated drift monitoring now (deferred — version-change invalidation covers the near-term risk); a broad human calibration study inside M34 (the gold set validates the coding instrument only); re-running v2 hoping for a pass (bar-fitting by repetition); committing raw model text to git (data governance — hashes anchor the evidence instead) |
| D-097 | 2026-07-11 | M34 framing-protocol.v3 recovery gate is NO-GO and v3 is rejected-not-frozen. The D-096 instrument repairs were implemented and test-locked in the research harness; all four controls passed, but the preregistered development ablation found Insta360 `unstable_profile` without the shared uncertainty clause and eligible only with it. The registered clause rule therefore selects the no-clause arm because the stable-concept sets differed and terminal-abstention improvement was only 3.3pp (<10pp). Human gold and Crocs/Xiaomi were deliberately not run; migration 0013 and production Phases 1–6 stay blocked. Exact-offset enforcement rejected 81/39 unsupported frame observations, and final blind mapping cost about $1.02 for two 30-response arms plus suffered provider JSON/time-out failures — both are productization failures, not footnotes. Any v4 needs a materially lower-variance coding instrument and bounded human-checkable mapping workflow; merely repeating v3, promoting the clause pass, lowering 5/6, or manually merging after scoring is forbidden | The gate was designed to detect prompt-induced stability before scarce held-outs or production enforcement. The fact that one shared uncertainty sentence creates the only pass means the measurement is not yet robust to minimally-leading elicitation. Exact offsets and review cost additionally show the present coding workflow is too brittle and expensive to defend commercially | Promoting the clause-arm result; loosening 5/6; post-score manual merges; running Crocs/Xiaomi anyway; starting migration 0013; calling the passing control suite sufficient evidence |
| D-098 | 2026-07-11 | Post-v3 scope ruling: **A + B** (extends D-095/D-096/D-097). **(A) framing-protocol.v4 authorized** — design brief at `docs/audits/m34/framing-protocol-v4-brief.md` — built around the two-failure diagnosis (elicitation sensitivity AND coding-instrument variance are coupled; fixing one without the other fails): extraction shrinks to **span-offset finding** (server-validated character offsets into immutable raw text; free-form labels lose all identity role — this removes label explosion at the source); concept mapping moves to the **operator as blinded mapper** (deduplicated shuffled span texts, no counts/variant/provider/arm metadata, concept map locked and versioned BEFORE scoring, preregistered span budget with `mapping_overflow` abstention on overflow, sampled second-pass self-consistency check; LLM mapping is out as mapper-of-record — the v3 mapper cost ~$1.12/two-arm cycle with transport failures and chunking artifacts); elicitation carries **zero shared instruction** (bare open paraphrases; hedging detected at extraction via stance tags; `entity_ambiguous` + existing abstention states replace the uncertainty clause's job); and the ablation that caught v3 is promoted to a **permanent clause-agreement freeze gate** for every future protocol version (stable-concept sets must AGREE across instruction-wording arms — divergence means the instrument measures the instruction; redesign, never arm-pick). Preregistered gate order: controls → clause-agreement ablation → human-coded gold set → mapper-reliability floor → FREEZE → sealed held-outs → production. **Crocs/Xiaomi carry over to v4 unburned**: heldout-register rule 5 burns held-outs only when held-out RESULTS prompt the protocol change, and v3 was rejected on development data alone. Mapping/review/extraction/embedding spend enters C-2 accounting at harness level (binding, not queued — v3's partially-unreconcilable spend is the last such discovery). **(B) Product descope, effective immediately** — the representation lane + blind extraction + verified offsets + abstention states ship as a **DESCRIPTIVE "framing profile"** Evidence-Layer output with explicit abstention labels and NO eligibility/baseline/certification claim; concept-level statements render only as clearly-attributed operator interpretation until v4 validates the mapper; C-15 stays blocked; new C-13 studies keep the `PRE-M34 BASELINE` disclosure; the D-089/D-094 deferred demo arc is **unblocked on a descriptive-output basis** (demos may use harness-grade framing profiles labeled EXPERIMENTAL/DESCRIPTIVE). This partially amends D-094: M34's production scope splits — descriptive-profile productization may proceed once v4 freezes its extraction contract, while eligibility machinery and C-15/Phase-6 remain gated on held-out validation + the D-096 shadow rollout; **migration 0013 stays blocked until v4 freeze** because the frame-extraction schema it ships must be the v4 span-offset shape, not v1's label-identity shape. Held-out seal discipline reconciling A with B: Crocs/Xiaomi ordinary audits MAY be generated early for Option-B value, but the framing pipeline never runs on them and framing patterns are not inspected before v4 freeze; every access is recorded in `heldout-register.md`. Also committed this session: the two verified ops loose ends (shell-free preview launcher in `.claude/launch.json`; Settings credential-verify success notice in `credentials-panel.tsx`) | Two protocol generations failed honestly and cheaply (~$1.7 total), which is the gate system working — but a third cycle needs a materially different instrument, not iteration, and D-097 already named its shape (lower-variance coding, bounded human-checkable mapping). Meanwhile the working machinery (span extraction, offsets, abstention states, manifests) has standalone descriptive audit value that shouldn't stay hostage to certification research: honesty-first descriptive output preserves commercial motion (demo arc, audit deliverables) without productizing the fragile gate — the exact failure the stop-line exists to prevent | Continuing the production build on v3's method (would productize a measurement known to manufacture stability through prompt wording); full park (discards working machinery for the fragile gate's sins and strands the demo arc indefinitely); LLM mapper-of-record with better prompts (the variance and cost are structural — three model passes multiply both; human-blinded mapping is cheaper AND checkable); burning Crocs/Xiaomi and selecting new held-outs (unnecessary — the register's own rule 5 only burns on held-out-driven changes); shipping migration 0013 now against the v1 label-identity schema (guaranteed churn — v4's span-offset contract is already known to replace it) |
| D-099 | 2026-07-11 | M34 descoped from automated framing certification to **human-reviewed framing evidence** (extends D-098; partially supersedes D-094 and retires the v4 protocol program). Core diagnosis, endorsed by the lead: M34 tried to automate an interpretive research judgment and convert the automation into a hard validity gate; every protocol version relocated rather than removed the judgment (v1/v2 label fragmentation 213/264 → v3 LLM-mapping explosion + clause sensitivity → v4 span fragmentation 771 dedup'd vs a 60 budget). FACTUAL CORRECTION recorded: v4 did NOT fail a completed freeze gate — it stopped during calibration (CAL), demonstrating operational infeasibility and excessive granularity (771/60 spans, 12/70 malformed envelopes, $0.669 spent), not a completed scientific-validation failure; the prereg never left `ratified-pre-cal`. SPLIT: **M34A — Framing Evidence** (ships): fixed bare representation prompts (the CAL-2-validated non-steering v4 set, pinned per protocol version, verbatim in each deliverable's methodology/evidence section — not headline surfaces); full provenance (raw response, prompt wording, model/version, mode, date, exact offset-verified spans — server-validated, 1.2% drop rate proven); blind discovery (analyst codes a blinded subset with NO positioning/fact-sheet/attributes/frequencies; project codebook of typically 5–10 associations; other/ambiguous/none valid); **codebook locked + timestamped before counting, positioning revealed only after lock (reveal timestamped)**; full-sample coding with AI proposing spans and a human accepting/rejecting each (every accepted observation links to a literal span; abstention states incl. `entity_ambiguous` kept); **consistency checks with strict terminology** — intra-rater consistency (same analyst re-codes ~20% later), machine discrepancy check (LLM comparison, labeled as such, never an independent coder), inter-rater reliability ONLY when a second human codes — never combined into one "agreement" number, single-analyst engagements say so plainly; **recurrence matrix** (association × responses n/N, prompt spread, model/mode scope, review status) — descriptive only, complete denominators, no Wilson/CI/independence claims; **gap classification** against revealed positioning + verified fact sheet (reinforced / missing / misframed / unsupported / non-actionable) as the commercial output; simulation handoff per amended C-15 (verbatim response + immutable evidence snapshot rendered wherever results render). **M34B — automated eligibility: deferred, not cancelled** — revisit only against accumulated human-coded projects; coded corpora stored structurally per-project (codebook, spans, accept/reject decisions) but treated as an OPTION not automatically a moat: cross-project reuse requires explicit contractual permission, de-identification, tenant separation, defined retention, versioned codebooks, and no silent training use. KEPT structural gates (amendment 2 — a hard rule above human judgment still guards provenance even though it cannot certify semantics): raw-response existence, exact spans, complete denominators, lock-before-count, reveal-after-lock, reviewer disclosure, version pinning, stimulus-evidence linkage. REMOVED: autonomous concept clustering, cosine thresholds, LLM blind mapping, the 5/6+LOO+unique-top eligibility law, automatic medoid selection, the requirement that every brand have one eligible baseline, hard C-15 admission enforcement (superseded by the C-15 provenance/disclosure rule), the second production extraction state machine solely for framing (span extraction becomes a best-effort assist; a failed extraction degrades to human reading raw text), and the clause-ablation freeze gate (no freeze to gate — the v3 lesson survives as: zero shared instruction in prompts + pinned wording + verbatim disclosure). HELD-OUTS: Crocs/Xiaomi released from scientific seal (their purpose — validating a universal automated ontology — is cancelled); repurposable as fresh **workflow pilots** (usability, bounded review time, actionable-gap yield) stated honestly as such, with Insta360/HEYTEA used first for workflow development. v4 artifacts: protocol retired-not-frozen; salvaged into M34A — bare prompt set, offset validation, provenance controls, harness resilience; **v5 is not started**. M34A's execution plan lives in this entry + BUILD_NOTES (below the D-090 threshold); `M34_BUILD_PLAN.md` gets a superseded status header, body preserved | Four protocol generations of evidence showed the certification gate itself was the failure generator — freezing interpretation is the impossible step, and more machinery hides judgment rather than removing it. The client decision (which narrative gap to correct and test next) needs auditable recurring evidence, not a certified singular truth. Honest visibility (recurrence matrix + snapshots + labeled review methods) is stronger than pseudo-objective thresholds, and structural provenance gates keep the evidence auditable without laundering judgment into eligibility law | Building v5 (another autonomous consolidation protocol — the invariant failure would relocate again); visibility alone without structural gates (the lead's amendment: provenance gates stay enforced in code); presenting intra-rater or LLM agreement as inter-rater reliability (terminology strictly separated); treating the coded corpus as automatically reusable training data (confidentiality conditions precede any reuse); burning Crocs/Xiaomi as if they still validated an instrument (they test workflow usability now, honestly labeled); deleting the v4 artifacts (retired with salvage value: validated prompts, offset verification, resilience fixes) |

| D-100 | 2026-07-11 | M34A harness implements the D-099 workflow without resurrecting semantic certification: a blind discovery packet contains raw text only and its response-id key is separate; codebook lock and positioning/fact-sheet reveal have ordered timestamps; full-sample coding requires a response review for every denominator response, exact spans for accepted associations, and separate (never blended) intra-rater / machine-discrepancy / inter-rater records; the recurrence matrix is descriptive only; C-15 snapshot is copied and SHA-anchored. Harness-side C-2 accounting reserves then settles each paid call. If a call is interrupted between reservation and immutable raw-artifact checkpoint, it becomes an explicit `generation_unavailable` response with null raw text: it remains in the denominator, cannot be blindly coded or used for Simulation, and is never silently retried. Offset span assistance is optional; a skipped or failed assist never excludes the stored raw response from human coding | D-099's honesty claim fails if operational interruption either deletes an attempted sample or causes unrecorded duplicate spending. The development run demonstrated both risks in the actual terminal environment; a retained unavailable denominator and conservative reservation are more honest than retrying until a clean-looking collection appears | Auto-retrying a paid-but-uncheckpointed call (duplicate/billed evidence ambiguity); silently dropping it from denominator; calling skipped span assist an extraction failure or requiring it for human coding; treating review-method checks as one blended agreement statistic; using these inputs for automated eligibility or Crocs/Xiaomi validation |

## 10. Current state

Milestone status has exactly one home: the milestone tracker and progress notes in `PRD.md` section 11. Do not record status here.

## 11. Cross-platform wiring

This file stays canonical and tool-agnostic. Tool-specific files are thin pointers only:

- `CLAUDE.md`: import/pointer to this file.
- `AGENTS.md`: one-line instruction to read this file.
- `.cursor/rules/parallax.mdc`: always-on pointer to this file.

Verify each tool's current instruction-file behavior in official docs before relying on it.

## 12. Glossary

cell: one resolved prompt. engine-mode: provider plus grounding mode. rep: one repeated sample. run: execution of an approved matrix across selected engine-modes and repetitions. extraction: structured record parsed from one raw response. dead-letter: a failed job or extraction stored for review. golden dataset: hand-labeled fixtures with exact expected extractions and metrics. mock mode: seeded fixture-backed provider mode. stability index: mean pairwise Jaccard of top-5 brand sets across reps. lost-shortlist cell: high-intent cell where a competitor appears in >=60% of samples and the client appears in <=20%. fact sheet: operator-entered ground truth. matrix version: immutable approved prompt-cell snapshot. pillar: one of the Four P's — Presence, Position, Perception, Proof — the client question a prompt/metric/report chapter answers (D-051, M11). archetype: a project's buyer-language category (b2b, consumer_product, consumer_venue) selecting its prompt-template pack (D-052, M11).

representation (intent): the sixth audit intent (M34, D-094/D-099) — a minimally-leading, brand-named but non-evaluative prompt measuring how AI describes the named client brand; maps to Perception, feeds no planted-signal metric, and is PM-9-exempt (it contains `{client_brand}` by design); the M34A prompt set is fixed, bare (zero shared instruction), pinned per protocol version, and CAL-2-validated as non-steering. framing lane: one of two never-pooled evidence sources — `organic_in_context` (spontaneous mention in existing unbranded responses; optional supporting evidence, never required per brand) or `neutral_elicited` (representation prompts; the primary lane); neither is ever called "bias-free"/"unbiased"/"vanilla" (forbidden vocabulary). codebook: the small project-specific set of framing associations (typically 5–10) an analyst develops on a blinded subset, then locks (versioned, timestamped) BEFORE full-sample counting and BEFORE the client's intended positioning is revealed (reveal timestamped) — D-099. coded association: one codebook entry observed in a response, valid only when linked to an exact offset-verified evidence span; other/ambiguous/none and `entity_ambiguous` are valid outcomes. recurrence matrix: the descriptive M34A output — association × (responses n/N, prompt spread, model/mode scope, review status); complete denominators, no Wilson/CI/independence claims. gap classification: reinforced / missing / misframed / unsupported / non-actionable, judged against the revealed positioning + verified fact sheet — the actionable framing gap is M34A's commercial output. framing baseline (M34A sense): a verbatim stored response linked to reviewed coded evidence, handed to Simulation with an immutable evidence snapshot per C-15; low-recurrence baselines carry accurate labels (`SINGLE OBSERVED INSTANCE`). Consistency-check terms are never conflated (D-099): intra-rater consistency (same analyst re-codes later) ≠ machine discrepancy check (LLM comparison, labeled) ≠ inter-rater reliability (second human only). The D-094-era eligibility machinery (5/6+LOO admission, unique-top, automatic medoid selection, abstention-certification states) is RETIRED (D-099) — those terms are historical. frame stability remains distinct from the shortlist Stability Index and is never conflated with it (BF-13's caution survives the descope).

Evidence Layer / Simulation Layer: the two named epistemic layers organizing the product (D-077), superseding the earlier upper/mid/lower funnel framing (D-063): the Evidence Layer is the audit engine, home to the Four P pillars, with Proof as the trust rail (never a pillar or a "stage"); the Simulation Layer is the resonance study system. funnel stage: the internal, non-rendered `FunnelStage` type (`"upper" | "mid" | "lower"`, `src/core/funnel.ts`) that the pillar-to-layer mapping is still built on under the hood (D-077 kept these values — they are implementation detail, not product vocabulary). resonance study: a Simulation Layer study definition — panel personas x stimulus variants with a baseline (D-064). stimulus variant: one framing shown to the synthetic panel (`measured_ai | corrected | repositioned | custom`); `measured_ai` quotes stored audit responses (C-13). panel persona: a simulation-side buyer profile — age and income band (paper-validated axes) plus location and behavioral profile (prompt context, not validated segmentation; D-066) — distinct from audit personas (CM-3). SSR: Semantic Similarity Rating — free-text reaction to Likert PMF via embedding similarity against anchor sets. anchor set: a versioned repo fixture of 5 reference statements per Likert point with a `calibrated` flag (v1 ships uncalibrated); studies pin their anchor version at approval. PMF: probability mass function over the 5-point scale; means are point estimates (no invented intervals, D-023). delta (ΔPI): a variant's Likert-scale mean purchase-intent shift vs the study baseline — a survey-construct shift, never purchase probability; the headline simulation metric.

"Validation" is overloaded; always qualify it: validation (intent) is one of the five prompt intents; validation run (`live_validation`, "validation-only") is a cheap k=2 pipeline dry-run that is never client-ready evidence; schema validation is Zod input/output checking. The provider id `google` is the Gemini provider — prose says Gemini, code says `google`.
